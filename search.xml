<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>RecyclerView回收复用机制</title>
    <url>/2020/08/07/RecyclerView%E5%9B%9E%E6%94%B6%E5%A4%8D%E7%94%A8%E6%9C%BA%E5%88%B6/</url>
    <content><![CDATA[<h1 id="一、复用"><a href="#一、复用" class="headerlink" title="一、复用"></a>一、复用</h1><h2 id="1、滑动时的复用入口"><a href="#1、滑动时的复用入口" class="headerlink" title="1、滑动时的复用入口"></a>1、滑动时的复用入口</h2><h3 id="1-1-获取缓存并复用的流程"><a href="#1-1-获取缓存并复用的流程" class="headerlink" title="1.1 获取缓存并复用的流程"></a>1.1 获取缓存并复用的流程</h3><ul>
<li><p>onTouchEvent </p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">case</span> MotionEvent.ACTION_MOVE: &#123;</span><br><span class="line">    <span class="keyword">if</span> (scrollByInternal(</span><br><span class="line">                            canScrollHorizontally ? dx : <span class="number">0</span>,</span><br><span class="line">                            canScrollVertically ? dy : <span class="number">0</span>,</span><br><span class="line">                            vtev)) &#123;</span><br><span class="line">                        getParent().requestDisallowInterceptTouchEvent(<span class="keyword">true</span>);</span><br><span class="line">                    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p>scrollByInternal</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span> (x != <span class="number">0</span>) &#123;</span><br><span class="line">              consumedX = mLayout.scrollHorizontallyBy(x, mRecycler, mState);</span><br><span class="line">              unconsumedX = x - consumedX;</span><br><span class="line">          &#125;</span><br><span class="line">          <span class="keyword">if</span> (y != <span class="number">0</span>) &#123;</span><br><span class="line">              consumedY = mLayout.scrollVerticallyBy(y, mRecycler, mState);</span><br><span class="line">              unconsumedY = y - consumedY;</span><br><span class="line">          &#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p>scrollVerticallyBy</p>
<p>直接看LinearLayoutManager里面重写的方法。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span> (mOrientation == HORIZONTAL) &#123;</span><br><span class="line">            <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> scrollBy(dy, recycler, state);</span><br></pre></td></tr></table></figure>
</li>
<li><p>scrollBy</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">final</span> <span class="keyword">int</span> consumed = mLayoutState.mScrollingOffset</span><br><span class="line">                + fill(recycler, mLayoutState, state, <span class="keyword">false</span>);</span><br></pre></td></tr></table></figure>
</li>
<li><p>fill</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">layoutChunk(recycler, state, layoutState, layoutChunkResult);</span><br></pre></td></tr></table></figure>
</li>
<li><p>layoutChunk</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">View view = layoutState.next(recycler);</span><br><span class="line">addView(view);</span><br></pre></td></tr></table></figure>

<p>在这里获取到缓存的view并addView添加，完成复用。</p>
</li>
</ul>
<h3 id="1-2-RecyclerView缓存机制"><a href="#1-2-RecyclerView缓存机制" class="headerlink" title="1.2 RecyclerView缓存机制"></a>1.2 RecyclerView缓存机制</h3><p>采用四级缓存：（目的是为了提高性能，具体体现在mCacheViews和RecyclerViewPool）</p>
<ul>
<li>mChangeScrap与 mAttachedScrap (用来缓存还在屏幕内的 ViewHolder )</li>
<li>mCachedViews (用来缓存移除屏幕之外的 ViewHolder )</li>
<li>mViewCacheExtension (开发给用户的自定义扩展缓存，需要用户自己管理 View 的创建和缓存 )</li>
<li>RecycledViewPool (ViewHolder 缓存池)</li>
</ul>
<h3 id="1-3-复用如何调用缓存"><a href="#1-3-复用如何调用缓存" class="headerlink" title="1.3 复用如何调用缓存"></a>1.3 复用如何调用缓存</h3><ul>
<li><p>进入next方法看看</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function">View <span class="title">next</span><span class="params">(RecyclerView.Recycler recycler)</span> </span>&#123;</span><br><span class="line">            <span class="keyword">if</span> (mScrapList != <span class="keyword">null</span>) &#123;</span><br><span class="line">                <span class="keyword">return</span> nextViewFromScrapList();</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">final</span> View view = recycler.getViewForPosition(mCurrentPosition);</span><br><span class="line">            mCurrentPosition += mItemDirection;</span><br><span class="line">            <span class="keyword">return</span> view;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p>getViewForPosition</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function">View <span class="title">getViewForPosition</span><span class="params">(<span class="keyword">int</span> position, <span class="keyword">boolean</span> dryRun)</span> </span>&#123;</span><br><span class="line">            <span class="keyword">return</span> tryGetViewHolderForPositionByDeadline(position, dryRun, FOREVER_NS).itemView;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p>tryGetViewHolderForPositionByDeadline</p>
<p>分几种情况去获取ViewHolder</p>
<ul>
<li>getChangedScrapViewForPosition – mChangeScrap 与动画相关</li>
<li>getScrapOrHiddenOrCachedHolderForPosition  – mAttachedScrap 、mCachedViews </li>
<li>getScrapOrCachedViewForId  – mAttachedScrap 、mCachedViews </li>
<li>mViewCacheExtension.getViewForPositionAndType – 自定义缓存 </li>
<li>getRecycledViewPool().getRecycledView – 从缓冲池里面获取</li>
</ul>
<p>总体流程：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function">ViewHolder <span class="title">tryGetViewHolderForPositionByDeadline</span><span class="params">(<span class="keyword">int</span> position,</span></span></span><br><span class="line"><span class="function"><span class="params">               <span class="keyword">boolean</span> dryRun, <span class="keyword">long</span> deadlineNs)</span> </span>&#123;</span><br><span class="line">    <span class="comment">//先从mChangedScrap中获取</span></span><br><span class="line">    holder = getChangedScrapViewForPosition(position);</span><br><span class="line">    <span class="comment">//如果没有获取到，再从mAttachedScrap 、mCachedViews中获取</span></span><br><span class="line">    <span class="keyword">if</span> (holder == <span class="keyword">null</span>) &#123;</span><br><span class="line">               holder = getScrapOrHiddenOrCachedHolderForPosition(position, dryRun);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">//如果没有获取到，这里还有一个方法也是从mAttachedScrap 、mCachedViews中获取</span></span><br><span class="line">    <span class="keyword">if</span>(holder == <span class="keyword">null</span>)&#123;</span><br><span class="line">        holder = getScrapOrCachedViewForId(mAdapter.getItemId(offsetPosition),</span><br><span class="line">                       type, dryRun);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">//如果还是未获取到，而且自定义了缓存，就从自定义缓存中获取</span></span><br><span class="line">    <span class="keyword">if</span> (holder == <span class="keyword">null</span> &amp;&amp; mViewCacheExtension != <span class="keyword">null</span>)&#123;</span><br><span class="line">        <span class="keyword">final</span> View view = mViewCacheExtension</span><br><span class="line">                           .getViewForPositionAndType(<span class="keyword">this</span>, position, type);</span><br><span class="line">         <span class="keyword">if</span> (view != <span class="keyword">null</span>) &#123;</span><br><span class="line">         	holder = getChildViewHolder(view);</span><br><span class="line">         &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">//如果还是未获取到，就从缓冲池里获取</span></span><br><span class="line">    <span class="keyword">if</span> (holder == <span class="keyword">null</span>) &#123;</span><br><span class="line">        holder = getRecycledViewPool().getRecycledView(type);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">//如果最后都未获取到缓存，就调用方法createViewHolder</span></span><br><span class="line">     <span class="keyword">if</span> (holder == <span class="keyword">null</span>) &#123;</span><br><span class="line">         holder = mAdapter.createViewHolder(RecyclerView.<span class="keyword">this</span>, type);</span><br><span class="line">     &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">//holder创建后，绑定布局</span></span><br><span class="line">    bound = tryBindViewHolderByDeadline(holder, offsetPosition, position, deadlineNs);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>进入createViewHolder后，通过子类自定义实现的adapter，重写onCreateViewHolder，来创建viewHolder。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">final</span> VH <span class="title">createViewHolder</span><span class="params">(ViewGroup parent, <span class="keyword">int</span> viewType)</span> </span>&#123;</span><br><span class="line">           Trace.beginSection(TRACE_CREATE_VIEW_TAG);</span><br><span class="line">           <span class="keyword">final</span> VH holder = onCreateViewHolder(parent, viewType);</span><br><span class="line">           holder.mItemViewType = viewType;</span><br><span class="line">           Trace.endSection();</span><br><span class="line">           <span class="keyword">return</span> holder;</span><br><span class="line">       &#125;</span><br></pre></td></tr></table></figure>

<p>绑定布局流程，进入onBindViewHolder</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">mAdapter.bindViewHolder(holder, offsetPosition);</span><br></pre></td></tr></table></figure>

<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">final</span> <span class="keyword">void</span> <span class="title">bindViewHolder</span><span class="params">(VH holder, <span class="keyword">int</span> position)</span> </span>&#123;</span><br><span class="line">           onBindViewHolder(holder, position, holder.getUnmodifiedPayloads());</span><br><span class="line">       &#125;</span><br></pre></td></tr></table></figure>



</li>
</ul>
<h2 id="2、布局时的复用入口"><a href="#2、布局时的复用入口" class="headerlink" title="2、布局时的复用入口"></a>2、布局时的复用入口</h2><ul>
<li>RecyclerView.OnLayout</li>
<li>dispatchLayout()</li>
<li>dispatchLayoutStep2()</li>
<li>mLayout.onLayoutChildren(mRecycler, mState)</li>
<li>fill(recycler, mLayoutState, state, false)</li>
</ul>
<p>接下来就和之前的步骤相同了。</p>
<h1 id="二、回收（缓存）"><a href="#二、回收（缓存）" class="headerlink" title="二、回收（缓存）"></a>二、回收（缓存）</h1><h2 id="1、LinearLayoutManager-onLayoutChildren中缓存"><a href="#1、LinearLayoutManager-onLayoutChildren中缓存" class="headerlink" title="1、LinearLayoutManager.onLayoutChildren中缓存"></a>1、LinearLayoutManager.onLayoutChildren中缓存</h2><p>进入ViewHolder中：</p>
<ul>
<li>detachAndScrapAttachedViews(recycler)</li>
<li>scrapOrRecycleView(recycler, i, v)</li>
<li>recycler.recycleViewHolderInternal(viewHolder) 以及recycler.scrapView(view)两种方式</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title">scrapOrRecycleView</span><span class="params">(Recycler recycler, <span class="keyword">int</span> index, View view)</span> </span>&#123;</span><br><span class="line">           <span class="keyword">if</span> (viewHolder.isInvalid() &amp;&amp; !viewHolder.isRemoved()</span><br><span class="line">                   &amp;&amp; !mRecyclerView.mAdapter.hasStableIds()) &#123;</span><br><span class="line">               recycler.recycleViewHolderInternal(viewHolder);</span><br><span class="line">           &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">               recycler.scrapView(view);</span><br><span class="line">           &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<h3 id="1-1-recycler-scrapView"><a href="#1-1-recycler-scrapView" class="headerlink" title="1.1 recycler.scrapView"></a>1.1 recycler.scrapView</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">scrapView</span><span class="params">(View view)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">if</span>（。。。）&#123;</span><br><span class="line">        mAttachedScrap.add(holder);</span><br><span class="line">    &#125;<span class="keyword">else</span>&#123;</span><br><span class="line">        mChangedScrap.add(holder);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>在这里只对mAttachedScrap、mChangedScrap进行缓存，直接添加进集合。</p>
<h3 id="1-2-recycler-recycleViewHolderInternal"><a href="#1-2-recycler-recycleViewHolderInternal" class="headerlink" title="1.2 recycler.recycleViewHolderInternal"></a>1.2 recycler.recycleViewHolderInternal</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">boolean</span> cached = <span class="keyword">false</span>;</span><br><span class="line"><span class="keyword">if</span> (forceRecycle || holder.isRecyclable()) &#123;</span><br><span class="line">    <span class="comment">//这里如果viewHolder发生改变，就不会进入if语句</span></span><br><span class="line">    <span class="keyword">if</span> (mViewCacheMax &gt; <span class="number">0</span> &amp;&amp; !holder.hasAnyOfTheFlags(ViewHolder.FLAG_INVALID</span><br><span class="line">                                | ViewHolder.FLAG_REMOVED</span><br><span class="line">                                | ViewHolder.FLAG_UPDATE</span><br><span class="line">                                | ViewHolder.FLAG_ADAPTER_POSITION_UNKNOWN)) &#123;</span><br><span class="line">        <span class="keyword">int</span> cachedViewSize = mCachedViews.size();</span><br><span class="line">    	<span class="keyword">if</span> (cachedViewSize &gt;= mViewCacheMax &amp;&amp; cachedViewSize &gt; <span class="number">0</span>) &#123;</span><br><span class="line">        	<span class="comment">//mViewCacheMax的值为2</span></span><br><span class="line">        	<span class="comment">//如果mCachedViews大小大于2，就把第一位的viewHolder从mCachedViews里移除，放入缓存池中</span></span><br><span class="line">    		recycleCachedViewAt(<span class="number">0</span>);<span class="comment">//--&gt; 进入方法会调用addViewHolderToRecycledViewPool(viewHolder, true)</span></span><br><span class="line">    		cachedViewSize--;</span><br><span class="line">    	&#125;</span><br><span class="line">        <span class="comment">//往mCachedViews里添加新的viewHolder缓存</span></span><br><span class="line">    	mCachedViews.add(targetCacheIndex, holder);</span><br><span class="line">    	cached = <span class="keyword">true</span>;</span><br><span class="line">    &#125;</span><br><span class="line">	<span class="comment">//相当于如果viewHolder改变，就会把viewHolder直接添加到缓存池中</span></span><br><span class="line">    <span class="keyword">if</span> (!cached) &#123;</span><br><span class="line">    	addViewHolderToRecycledViewPool(holder, <span class="keyword">true</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;<span class="keyword">else</span>&#123;</span><br><span class="line">	 mViewInfoStore.removeViewHolder(holder);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>这个过程可以用下图表示：</p>
<img src="/2020/08/07/RecyclerView%E5%9B%9E%E6%94%B6%E5%A4%8D%E7%94%A8%E6%9C%BA%E5%88%B6/%E7%BC%93%E5%AD%98.png" class title="缓存">



<p>缓存池中一个类型的viewHolder最大可以存放五个。</p>
<p>如果不需要缓存或者缓存池该类型的数组已满，那么就直接把这个viewHolder丢弃，因为缓存池中存放的是viewHolder的类型，不存入也不影响。</p>
<p>最后在addViewHolderToRecycledViewPool方法中将viewHolder放入缓存池后，把该viewHolder清空。</p>
<p>addViewHolderToRecycledViewPool - &gt; getRecycledViewPool().putRecycledView(holder) -&gt;  scrap.resetInternal()</p>
<p>在这个方法中把viewHolder的标记重置，完成清空。</p>
<p>由此可以看出，在这个方法中，对mCachedViews、RecyclerViewPool进行处理。</p>
<h2 id="2、LinearLayoutManager-onLayoutChildren的fill中缓存"><a href="#2、LinearLayoutManager-onLayoutChildren的fill中缓存" class="headerlink" title="2、LinearLayoutManager.onLayoutChildren的fill中缓存"></a>2、LinearLayoutManager.onLayoutChildren的fill中缓存</h2><p>会从fill方法中调用 recycler.recycleViewHolderInterna方法，所以在fill这里只会处理mCachedViews和缓存池的缓存。</p>
<h1 id="三、自定义layoutManager"><a href="#三、自定义layoutManager" class="headerlink" title="三、自定义layoutManager"></a>三、自定义layoutManager</h1><p>调用detachAndScrapAttachedViews(recycler)进行回收复用</p>
<p>调用recycler.getViewForPosition(i)进行缓存</p>
]]></content>
  </entry>
  <entry>
    <title>handler</title>
    <url>/2020/10/19/handler/</url>
    <content><![CDATA[<h1 id="Handler工作流程"><a href="#Handler工作流程" class="headerlink" title="Handler工作流程"></a>Handler工作流程</h1><img src="/2020/10/19/handler/handler%E5%B7%A5%E4%BD%9C%E6%9C%BA%E5%88%B6.png" class title="handler工作机制">

<p>​    就像这张图一样，handler只是负责发送消息、取出消息，把消息放在传送带上，传送带就是MessageQueue，相当于一个容器（一个用单链表实现的优先级队列，根据事件处理时间来进行优先级判断），而传送带的动力就是Looper，在安卓程序启动的时候会在jvm里找到main方法入口（ActivityThread中），在main方法里进行主线程Looper的prepare和loop，在loop中无限循环，不停的调用queue.next()，取出队头的消息，当messageQueue内存不够了，继续入队时就会系统卡死，系统的消息也无法入队响应，而当messageQueue为空时，就会阻塞当前线程，等待下一个消息入队，再唤醒。</p>
<h1 id="细节"><a href="#细节" class="headerlink" title="细节"></a>细节</h1><p>从handler的使用入手，我们使用的时候，是创建一个handler，重写handleMessage方法，然后在主线程sendMessage。</p>
<p>所以开始和结尾就分别为sendMessage和handleMessage，我们从这里入手去看源码。</p>
<ul>
<li><p>handler.sendMessage() —&gt;handler.sendMessageAtTime() —&gt;enqueueMessage</p>
<p>所有的send、post方法最终都会调用进入sendMessageAtTime方法，然后进行消息的入队操作enqueueMessage，所以这就能印证handler的作用就是把消息放在传送带上，也就是把消息入队。</p>
<img src="/2020/10/19/handler/handleMessage.png" class title="handleMessage">
</li>
<li><p>queue.enqueueMessage()</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">for</span> (;;) &#123;</span><br><span class="line">	prev = p;</span><br><span class="line">    p = p.next;</span><br><span class="line">    <span class="keyword">if</span> (p == <span class="keyword">null</span> || when &lt; p.when) &#123;</span><br><span class="line">    	<span class="keyword">break</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span> (needWake &amp;&amp; p.isAsynchronous()) &#123;</span><br><span class="line">        needWake = <span class="keyword">false</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line">msg.next = p; <span class="comment">// invariant: p == prev.next</span></span><br><span class="line">prev.next = msg;</span><br></pre></td></tr></table></figure>

<p>​    这里是入队的一系列操作，可以看出这个队列是由一个单链表实现的，而且会根据事件处理事件进行<strong>插入排序</strong>，所以这个队列是一个优先级队列。</p>
</li>
<li><p>Looper.loop() ，Looper.prepareMainLooper()</p>
<p>入队结束后，所有的事情都交给Looper来进行传动，从程序启动main方法入手。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">Looper.prepareMainLooper();</span><br><span class="line">Looper.loop();</span><br></pre></td></tr></table></figure>

<ul>
<li><p>Looper.prepareMainLooper();</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">//Looper</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">prepareMainLooper</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        prepare(<span class="keyword">false</span>);</span><br><span class="line">        <span class="keyword">synchronized</span> (Looper<span class="class">.<span class="keyword">class</span>) </span>&#123;</span><br><span class="line">            <span class="keyword">if</span> (sMainLooper != <span class="keyword">null</span>) &#123;</span><br><span class="line">                <span class="keyword">throw</span> <span class="keyword">new</span> IllegalStateException(<span class="string">"The main Looper has already been prepared."</span>);</span><br><span class="line">            &#125;</span><br><span class="line">            sMainLooper = myLooper();</span><br><span class="line">        &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">static</span> <span class="meta">@Nullable</span> <span class="function">Looper <span class="title">myLooper</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> sThreadLocal.get();</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">prepare</span><span class="params">(<span class="keyword">boolean</span> quitAllowed)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">if</span> (sThreadLocal.get() != <span class="keyword">null</span>) &#123;</span><br><span class="line">            <span class="keyword">throw</span> <span class="keyword">new</span> RuntimeException(<span class="string">"Only one Looper may be created per thread"</span>);</span><br><span class="line">        &#125;</span><br><span class="line">        sThreadLocal.set(<span class="keyword">new</span> Looper(quitAllowed));</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">//ThreadLocal</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">set</span><span class="params">(T value)</span> </span>&#123;</span><br><span class="line">        Thread t = Thread.currentThread();</span><br><span class="line">        ThreadLocalMap map = getMap(t);</span><br><span class="line">        <span class="keyword">if</span> (map != <span class="keyword">null</span>)</span><br><span class="line">            map.set(<span class="keyword">this</span>, value);</span><br><span class="line">        <span class="keyword">else</span></span><br><span class="line">            createMap(t, value);</span><br><span class="line">  &#125;</span><br></pre></td></tr></table></figure>

<p>​    从这里可以看出，Looper的准备其实就是在主线程的ThreadLocal的ThreadLocalMap里添加了一个key为ThreadLocal，value为Looper的键值对。</p>
<p>​    并且static final ThreadLocal<Looper> sThreadLocal = new ThreadLocal<Looper>();ThreadLocal是静态常量，所以只有一个。</Looper></Looper></p>
<p>​    在set的时候如果map不为空，就会抛出异常，所以Looper也无法更改。</p>
<p>​    ThreadLocal全系统就只有一个，而Looper可以有多个，但是ThreadLocal可以有多个ThreadLocalMap，每个Map的key都是唯一的ThreadLocal，value就是对应线程的Looper。</p>
<p>​    因此可以解释为什么一个线程只能有一个Looper和一个ThreadLocalMap了。</p>
</li>
<li><p>Looper.loop();</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">for</span> (;;) &#123;</span><br><span class="line">	Message msg = queue.next()；</span><br><span class="line">    <span class="keyword">if</span> (msg == <span class="keyword">null</span>) &#123;</span><br><span class="line">      <span class="comment">// No message indicates that the message queue is quitting.</span></span><br><span class="line">      <span class="keyword">return</span>;</span><br><span class="line">    &#125;</span><br><span class="line">  msg.target.dispatchMessage(msg);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>​    在loop中会进入一个无限循环，不停的取出MessageQueue的队头消息，这个循环只有在msg为空的情况下才会跳出循环，而主线程的loop方法只有在程序退出时会结束。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">dispatchMessage</span><span class="params">(Message msg)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">if</span> (msg.callback != <span class="keyword">null</span>) &#123;</span><br><span class="line">            handleCallback(msg);</span><br><span class="line">        &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">            <span class="keyword">if</span> (mCallback != <span class="keyword">null</span>) &#123;</span><br><span class="line">                <span class="keyword">if</span> (mCallback.handleMessage(msg)) &#123;</span><br><span class="line">                    <span class="keyword">return</span>;</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">          handleMessage(msg);</span><br><span class="line">        &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>然后进入dispatchMessage，调入到handleMessage，流程结束。</p>
</li>
</ul>
</li>
</ul>
<h1 id="注意点"><a href="#注意点" class="headerlink" title="注意点"></a>注意点</h1><h2 id="消息沉睡唤醒机制"><a href="#消息沉睡唤醒机制" class="headerlink" title="消息沉睡唤醒机制"></a>消息沉睡唤醒机制</h2><p>​    在消息入队和消息出队时，都会有特殊情况的出现，比如队列为空，队列为满，虽然并没有处理队列为满的情况，但是在消息把内存占满的情况下。系统消息无法入队，就会造成手机卡死，所以handler不能用来做定时提醒。</p>
<p>​    在两种情况下会造成阻塞：</p>
<ul>
<li>队列里没有消息时会无限阻塞，直到又有消息入队时会唤醒。</li>
<li>队列里出队的消息需要延时执行，会阻塞到执行时间。</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">int</span> nextPollTimeoutMillis = <span class="number">0</span>;</span><br><span class="line"><span class="keyword">for</span>(;;)&#123;</span><br><span class="line">    <span class="comment">//沉睡</span></span><br><span class="line">    nativePollOnce(ptr, nextPollTimeoutMillis);</span><br><span class="line">    <span class="comment">//进入循环遍历和等待操作</span></span><br><span class="line">    <span class="keyword">synchronized</span>(<span class="keyword">this</span>)&#123;</span><br><span class="line">        <span class="keyword">if</span> (msg != <span class="keyword">null</span>) &#123;</span><br><span class="line">                    <span class="keyword">if</span> (now &lt; msg.when) &#123;</span><br><span class="line">                        <span class="comment">// Next message is not ready.  Set a timeout to wake up when it is ready.</span></span><br><span class="line">                        nextPollTimeoutMillis = (<span class="keyword">int</span>) Math.min(msg.when - now, Integer.MAX_VALUE);</span><br><span class="line">                    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                        <span class="comment">// Got a message.</span></span><br><span class="line">                        mBlocked = <span class="keyword">false</span>;</span><br><span class="line">                        <span class="keyword">if</span> (prevMsg != <span class="keyword">null</span>) &#123;</span><br><span class="line">                            prevMsg.next = msg.next;</span><br><span class="line">                        &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                            mMessages = msg.next;</span><br><span class="line">                        &#125;</span><br><span class="line">                        msg.next = <span class="keyword">null</span>;</span><br><span class="line">                        <span class="keyword">if</span> (DEBUG) Log.v(TAG, <span class="string">"Returning message: "</span> + msg);</span><br><span class="line">                        msg.markInUse();</span><br><span class="line">                        <span class="keyword">return</span> msg;</span><br><span class="line">                    &#125;</span><br><span class="line">                &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                    <span class="comment">// No more messages.</span></span><br><span class="line">                    nextPollTimeoutMillis = -<span class="number">1</span>;</span><br><span class="line">                &#125;</span><br><span class="line">		&#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>​    这段代码是在一个无限循环中进行的，nextPollTimeoutMillis初始值为0，nativePollOnce沉睡，如果消息执行时间比现在的时间晚，就回计算出间隔时间，并且沉睡这么久，如果队列为空，就会赋值为-1，无限沉睡。</p>
<p>​    唤醒函数是在插入新的消息时调用。</p>
<h2 id="线程安全"><a href="#线程安全" class="headerlink" title="线程安全"></a>线程安全</h2><p>​    handler和Looper内部并没有关注线程安全，而是全部交给MessageQueue入队和出队时处理。</p>
<p>​    在出队时，有一个内置对象锁，锁的是messageQueue对象，所以不同线程的handler发送和取出消息时，都会阻塞。</p>
<p>​    同理，在入队时也会有一个内置对象锁，锁的是messageQueue对象。</p>
<h2 id="子线程创建Handler"><a href="#子线程创建Handler" class="headerlink" title="子线程创建Handler"></a>子线程创建Handler</h2><p>​    如果在子线程中创建handler，需要自己执行Looper.prepare,Looper.loop函数，并且在队列为空时需要调用Looper.quit结束loop中的循环。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">//Looper</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">quit</span><span class="params">()</span> </span>&#123;</span><br><span class="line">	mQueue.quit(<span class="keyword">false</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">//MessageQueue</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">quit</span><span class="params">(<span class="keyword">boolean</span> safe)</span> </span>&#123;</span><br><span class="line">    <span class="comment">//在主线程中不允许quit操作</span></span><br><span class="line">	<span class="keyword">if</span> (!mQuitAllowed) &#123;</span><br><span class="line">    	<span class="keyword">throw</span> <span class="keyword">new</span> IllegalStateException(<span class="string">"Main thread not allowed to quit."</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">synchronized</span> (<span class="keyword">this</span>) &#123;</span><br><span class="line">    	<span class="keyword">if</span> (mQuitting) &#123;</span><br><span class="line">           <span class="keyword">return</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="comment">//把 mQuitting设置为true</span></span><br><span class="line">        mQuitting = <span class="keyword">true</span>;</span><br><span class="line"></span><br><span class="line">        <span class="comment">//移除消息队列中所有消息</span></span><br><span class="line">        <span class="keyword">if</span> (safe) &#123;</span><br><span class="line">        	removeAllFutureMessagesLocked();</span><br><span class="line">        &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">            removeAllMessagesLocked();</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="comment">//唤醒next方法中的沉睡线程</span></span><br><span class="line">        <span class="comment">// We can assume mPtr != 0 because mQuitting was previously false.</span></span><br><span class="line">        nativeWake(mPtr);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>​    此时再进入next中的for循环，在<strong>if (mQuitting) {dispose();return null;}</strong>处返回null，再回到Looper.loop中，<strong>if (msg == null)  return;</strong>结束for循环，当前线程的Looper就停止运作。</p>
<h2 id="内存泄露"><a href="#内存泄露" class="headerlink" title="内存泄露"></a>内存泄露</h2><ul>
<li><p>为什么使用Handler会造成内存泄露？</p>
<p>​    因为内部类调用外部类的方法或者变量，导致内部类持有外部类引用，匿名内部类持有Activity.this，而Message持有Handler，MessageQueue又持有Message，MessageQueue又是在Looper构造函数时创建的，每一个线程就只有一个MessageQueue，而Looper在线程还存活时就一直存在，所以当Activity需要被回收时，根据JVM可达性分析，GC下activity无法被回收，导致内存溢出。</p>
<p><strong>Looper -&gt; MessageQueue -&gt; Message  -&gt; Handler -&gt;Activity.this</strong></p>
</li>
<li><p>为什么其他地方的内部类并不会有这种情况？</p>
<p>因为handler和Activity生命周期不同，而其它地方生命周期相同，可以同时回收。</p>
</li>
</ul>
<h2 id="Message怎么创建"><a href="#Message怎么创建" class="headerlink" title="Message怎么创建"></a>Message怎么创建</h2><ul>
<li><p>一般是利用message.obtain方法创建</p>
</li>
<li><p>如果频繁的new Message()，会导致内存抖动，gc频繁造成卡顿，有可能OOM</p>
</li>
<li><p>如何处理？</p>
<p>​    在loop中，msg.dispatchMessage被调用之后，会调用msg.recycleUnchecked()，把这个消息插入一个单链表的头部，在调用obtain时，将新的消息赋值给头部结点，将其取出。</p>
<p>​    这样的好处就是把内存空间一直保留着，不会产生内存碎片，及内存抖动。</p>
</li>
</ul>
<h2 id="loop的死循环阻塞为什么不会导致ANR"><a href="#loop的死循环阻塞为什么不会导致ANR" class="headerlink" title="loop的死循环阻塞为什么不会导致ANR"></a>loop的死循环阻塞为什么不会导致ANR</h2><ul>
<li>ANR是当事件一段时间无响应时，通过handler新建一个ANR事件，去进行提醒</li>
<li>ANR发生是因为事件一段时间无响应造成的</li>
<li>而ANR本身也是一个事件</li>
<li>而loop里的阻塞是因为没有事情干了，把CPU释放出来沉睡休息，完全没有关联</li>
</ul>
<h1 id="同步屏障"><a href="#同步屏障" class="headerlink" title="同步屏障"></a>同步屏障</h1><img src="/2020/10/19/handler/%E5%90%8C%E6%AD%A5%E5%B1%8F%E9%9A%9C.png" class title="同步屏障">

<p>​    如果有需要立刻执行的消息，就可以先调用message.postSyncBarrier()方法发送同步屏障，插入到对头，这条消息的target为null。</p>
<p>​    然后再把需要立刻执行的消息setAsynchronous(true)设置为异步消息，再send，然后在next方法中：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span> (msg != <span class="keyword">null</span> &amp;&amp; msg.target == <span class="keyword">null</span>) &#123;</span><br><span class="line">	<span class="comment">// Stalled by a barrier.  Find the next asynchronous message in the queue.</span></span><br><span class="line">    <span class="keyword">do</span> &#123;</span><br><span class="line">    	prevMsg = msg;</span><br><span class="line">        msg = msg.next;</span><br><span class="line">    &#125; <span class="keyword">while</span> (msg != <span class="keyword">null</span> &amp;&amp; !msg.isAsynchronous());</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>​    如果target==null，就直接找到最近的异步消息，立刻执行。</p>
<p>​    这个机制在ui更新上应用很多，ui每16ms就会刷新一次，就是发送同步屏障和异步消息来实现。</p>
<h1 id="HandlerThread"><a href="#HandlerThread" class="headerlink" title="HandlerThread"></a>HandlerThread</h1><p>用于方便的把Handler在子线程中使用，其继承于Thread，并且保证了线程安全。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">run</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        mTid = Process.myTid();</span><br><span class="line">        Looper.prepare();</span><br><span class="line">        <span class="keyword">synchronized</span> (<span class="keyword">this</span>) &#123;</span><br><span class="line">            mLooper = Looper.myLooper();</span><br><span class="line">            notifyAll();</span><br><span class="line">        &#125;</span><br><span class="line">        Process.setThreadPriority(mPriority);</span><br><span class="line">        onLooperPrepared();</span><br><span class="line">        Looper.loop();</span><br><span class="line">        mTid = -<span class="number">1</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">public</span> Looper <span class="title">getLooper</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">if</span> (!isAlive()) &#123;</span><br><span class="line">            <span class="keyword">return</span> <span class="keyword">null</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        </span><br><span class="line">        <span class="comment">// If the thread has been started, wait until the looper has been created.</span></span><br><span class="line">        <span class="keyword">synchronized</span> (<span class="keyword">this</span>) &#123;</span><br><span class="line">            <span class="keyword">while</span> (isAlive() &amp;&amp; mLooper == <span class="keyword">null</span>) &#123;</span><br><span class="line">                <span class="keyword">try</span> &#123;</span><br><span class="line">                    wait();</span><br><span class="line">                &#125; <span class="keyword">catch</span> (InterruptedException e) &#123;</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> mLooper;</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<p>通过对象锁和wait、notifyAll保证线程安全。</p>
<p><strong>使用：</strong></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">HandlerThread handlerThread = <span class="keyword">new</span> HandlerThread(<span class="string">"xxxxxx"</span>);</span><br><span class="line">handlerThread.start();</span><br><span class="line">mHandler = <span class="keyword">new</span> Handler(handlerThread.getLooper());</span><br><span class="line">mHandler.sendMessage();</span><br></pre></td></tr></table></figure>

<h1 id="IntentService"><a href="#IntentService" class="headerlink" title="IntentService"></a>IntentService</h1><p>用于后台服务处理一些异步消息。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">onCreate</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">super</span>.onCreate();</span><br><span class="line">        HandlerThread thread = <span class="keyword">new</span> HandlerThread(<span class="string">"IntentService["</span> + mName + <span class="string">"]"</span>);</span><br><span class="line">        thread.start();</span><br><span class="line"></span><br><span class="line">        mServiceLooper = thread.getLooper();</span><br><span class="line">        mServiceHandler = <span class="keyword">new</span> ServiceHandler(mServiceLooper);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line"><span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">onStart</span><span class="params">(@Nullable Intent intent, <span class="keyword">int</span> startId)</span> </span>&#123;</span><br><span class="line">        Message msg = mServiceHandler.obtainMessage();</span><br><span class="line">        msg.arg1 = startId;</span><br><span class="line">        msg.obj = intent;</span><br><span class="line">        mServiceHandler.sendMessage(msg);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">final</span> <span class="class"><span class="keyword">class</span> <span class="title">ServiceHandler</span> <span class="keyword">extends</span> <span class="title">Handler</span> </span>&#123;</span><br><span class="line">        <span class="function"><span class="keyword">public</span> <span class="title">ServiceHandler</span><span class="params">(Looper looper)</span> </span>&#123;</span><br><span class="line">            <span class="keyword">super</span>(looper);</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="meta">@Override</span></span><br><span class="line">        <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">handleMessage</span><span class="params">(Message msg)</span> </span>&#123;</span><br><span class="line">            onHandleIntent((Intent)msg.obj);</span><br><span class="line">            stopSelf(msg.arg1);</span><br><span class="line">        &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>重写时重写onHandleIntent方法，进行异步任务，</p>
<p>在OnCreate时利用HandlerThread获取handler，然后在onStart时发送消息，在处理消息时调用onHandleIntent，并且停止服务。</p>
]]></content>
  </entry>
  <entry>
    <title>binder</title>
    <url>/2020/10/22/binder/</url>
    <content><![CDATA[<h1 id="Binder是什么？"><a href="#Binder是什么？" class="headerlink" title="Binder是什么？"></a>Binder是什么？</h1><ul>
<li>一种进程间通信机制</li>
<li>驱动(/dev/binder)</li>
<li>Binder.java -&gt;实现IBinder进行进程间通信</li>
</ul>
<img src="/2020/10/22/binder/Binder.png" class title="Binder">

<h1 id="进程间通信"><a href="#进程间通信" class="headerlink" title="进程间通信"></a>进程间通信</h1><ul>
<li><p>为什么要使用多进程？</p>
<p>因为一个进程只会分配几百兆的内存，而且进程间是隔离的，可以起到扩大内存以及进程安全的作用。</p>
</li>
<li><p>Binder有什么优势？</p>
<p>Linux进程通信机制：Binder、内存共享、Socket等等，</p>
<table>
<thead>
<tr>
<th></th>
<th>Binder</th>
<th>共享内存</th>
<th>Socket</th>
</tr>
</thead>
<tbody><tr>
<td>性能</td>
<td>只用拷贝一次</td>
<td>拷贝0次</td>
<td>拷贝两次</td>
</tr>
<tr>
<td>特点</td>
<td>基于C/S架构，易用性高</td>
<td>控制复杂，易用性差（需要处理同步问题）</td>
<td>基于C/S架构，但是是通用接口，传输效率低，开销大</td>
</tr>
<tr>
<td>安全性</td>
<td>为每个APP分配UID，支持实名和匿名</td>
<td>依赖上层协议，访问接入点是开放的，不安全</td>
<td>依赖上层协议，访问接入点是开放的，不安全</td>
</tr>
</tbody></table>
</li>
<li><p>Binder怎么做到一次拷贝？</p>
<ul>
<li><p>进程间如何进行通信？</p>
<ul>
<li><p>内存空间：</p>
<img src="/2020/10/22/binder/%E5%86%85%E5%AD%98%E7%A9%BA%E9%97%B4.png" class title="内存空间">

<p>​    内存被操作系统划分成两块：用户空间和内核空间，用户空间是用户程序代码运行的地方，内核空间是内核代码运行的地方。为了安全，它们是隔离的，即使用户的程序崩溃了，内核也不受影响。</p>
<p>​    并且能够直接操作的都是虚拟空间，每个虚拟空间都会映射一个物理空间，作为真正能够存储东西的地方。</p>
</li>
<li><p>传统IPC传输数据</p>
<p>​    通过发送方对用户空间的内容进行拷贝，然后copy_from_user放到内核空间，然后接收方再从内核空间copy_to_user拷贝到接收方的用户空间。</p>
<p>​    会进行两次拷贝。</p>
</li>
</ul>
</li>
<li><p>Binder传输数据</p>
<img src="/2020/10/22/binder/Binder%E4%BC%A0%E8%BE%93%E6%95%B0%E6%8D%AE.png" class title="Binder传输数据">

<p>​    发送方通过<strong>copy_from_user</strong>（这个方法是在驱动层调用的）把数据从用户空间拷贝到内核空间，接收方通过mmap让他的用户空间指向的物理内存为内核空间指向的物理内存，这样一来，只要操作内核空间，接收方也会收到。</p>
</li>
<li><p>mmap原理</p>
<p>​    Linux通过将一个虚拟内存区域与一个磁盘上的对象关联起来，以初始化这个虚拟内存区域的内容，这个过程称为内存映射(memory mapping)。</p>
<p>​    所有的系统资源管理都是在内核空间中完成的。比如读写磁盘文件，分配回收内存，从网络接口读写数据等等。用户空间通过系统调用让内核空间完成这些功能。</p>
<img src="/2020/10/22/binder/%E5%86%99%E6%96%87%E4%BB%B6%E6%B5%81%E7%A8%8B.png" class title="写文件流程">

</li>
</ul>
</li>
</ul>
<h1 id="驱动"><a href="#驱动" class="headerlink" title="驱动"></a>驱动</h1><img src="/2020/10/22/binder/%E9%A9%B1%E5%8A%A8.png" class title="驱动">

<p>驱动流程：</p>
<img src="/2020/10/22/binder/%E9%A9%B1%E5%8A%A8%E6%B5%81%E7%A8%8B.png" class title="驱动流程">

<h2 id="1、binder-init"><a href="#1、binder-init" class="headerlink" title="1、binder_init"></a>1、binder_init</h2><ul>
<li><p>设备驱动入口函数：device_initcall(binder_init);</p>
</li>
<li><p>创建名为binder的单线程工作队列：binder_deferred_workqueue = create_singlethread_workqueue(“binder”);</p>
</li>
<li><p>为binder设备分配内存：binder_device = kzalloc(sizeof(*binder_device), GFP_KERNEL);</p>
</li>
<li><p>初始化设备：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">binder_device-&gt;miscdev.fops = &amp;binder_fops;</span><br><span class="line">binder_device-&gt;miscdev.minor = MISC_DYNAMIC_MINOR;</span><br><span class="line">binder_device-&gt;miscdev.name = name;</span><br><span class="line"></span><br><span class="line">binder_device-&gt;context.binder_context_mgr_uid = INVALID_UID;</span><br><span class="line">binder_device-&gt;context.name = name;</span><br></pre></td></tr></table></figure>
</li>
<li><p>binder驱动用的是misc设备，没有硬件，只是一个内存，注册简单。</p>
<p>misc驱动注册：ret = misc_register(&amp;binder_device-&gt;miscdev);</p>
</li>
<li><p>将hlist节点添加到binder_devices为表头的设备链表：hlist_add_head(&amp;binder_device-&gt;hlist, &amp;binder_devices);</p>
</li>
</ul>
<p><strong>总结一下就分为三大步：</strong></p>
<ul>
<li>分配内存：1~3</li>
<li>初始化设备：4~5</li>
<li>放入binder_devices链表：6</li>
</ul>
<h2 id="2、binder-open"><a href="#2、binder-open" class="headerlink" title="2、binder_open"></a>2、binder_open</h2><ul>
<li><p>为binder_proc结构体在kernel分配内存空间：proc = kzalloc(sizeof(*proc), GFP_KERNEL);</p>
</li>
<li><p>将当前进程的task保存到binder进程的tsk：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">get_task_struct(current);</span><br><span class="line">proc-&gt;tsk = current; </span><br><span class="line">INIT_LIST_HEAD(&amp;proc-&gt;todo); <span class="comment">// 初始化todo列表</span></span><br><span class="line">init_waitqueue_head(&amp;proc-&gt;wait); <span class="comment">// 初始化wait队列 </span></span><br><span class="line">proc-&gt;default_priority = task_nice(current); <span class="comment">// 将当前进程的nice值转换为进程优先级</span></span><br></pre></td></tr></table></figure>
</li>
<li><p>同步锁，因为binder支持多线程访问：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">binder_lock(__func__);</span><br><span class="line"></span><br><span class="line">binder_stats_created(BINDER_STAT_PROC); <span class="comment">// binder_proc对象创建数加1 </span></span><br><span class="line">hlist_add_head(&amp;proc-&gt;proc_node, &amp;binder_procs); <span class="comment">// 将proc_node节点添加到binder_procs的队列头部 </span></span><br><span class="line">proc-&gt;pid = current-&gt;group_leader-&gt;pid; <span class="comment">// 进程pid </span></span><br><span class="line">INIT_LIST_HEAD(&amp;proc-&gt;delivered_death); <span class="comment">// 初始化已分发的死亡通知列表 </span></span><br><span class="line">filp-&gt;private_data = proc; <span class="comment">// 将这个binder_proc与filp关联起来，这样下次通过filp就能找 到这个proc了</span></span><br><span class="line"></span><br><span class="line">binder_unlock(__func__); <span class="comment">// 释放同步锁</span></span><br></pre></td></tr></table></figure>

</li>
</ul>
<p><strong>总结一下就分为以下几步：</strong></p>
<ul>
<li>创建binder_proc对象：1</li>
<li>保存当前进程信息：2</li>
<li>将这个binder_proc放到binder_procs链表头部：3</li>
<li>将这个binder_proc与filp关联起来，这样下次通过filp就能找 到这个proc了：3</li>
</ul>
<h2 id="3、binder-mmap"><a href="#3、binder-mmap" class="headerlink" title="3、binder_mmap"></a>3、binder_mmap</h2><ul>
<li><p>内核的虚拟内存：struct vm_struct *area;</p>
</li>
<li><p>进程的虚拟内存vma，保证映射内存不大于4M</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span> ((vma-&gt;vm_end - vma-&gt;vm_start) &gt; SZ_4M)</span><br><span class="line">	vma-&gt;vm_end = vma-&gt;vm_start + SZ_4M;</span><br></pre></td></tr></table></figure>

<p>注意：驱动的映射内存不超过4M，但是Binder层不超过1M-8K，所以intent传递最大时1M-8K，因为intent是通过Binder来传递。</p>
</li>
</ul>
<p><strong>总结：</strong></p>
<ul>
<li>通过用户空间的虚拟内存大小分配一块内核的虚拟内存</li>
<li>分配了一块物理内存  — 4KB</li>
<li>把这块物理内存分别映射到用户空间的虚拟内存和内核的虚拟内存—1M</li>
</ul>
<h2 id="4、binder-ioctl"><a href="#4、binder-ioctl" class="headerlink" title="4、binder_ioctl"></a>4、binder_ioctl</h2><ul>
<li><p>根据当前进程的pid，从binder_proc中查找binder_thread, 如果当前线程已经加入到proc的线程队列则直接返回， 如果不存在则创建binder_thread，并将当前线程添加到当前的proc 。</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">thread = binder_get_thread(proc);</span><br></pre></td></tr></table></figure>
</li>
<li><p>进行binder的读写操作</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">static</span> <span class="keyword">int</span> <span class="title">binder_ioctl_write_read</span><span class="params">(struct file *filp, <span class="keyword">unsigned</span> <span class="keyword">int</span> cmd, <span class="keyword">unsigned</span> <span class="keyword">long</span> arg, struct binder_thread *thread)</span></span>&#123;</span><br><span class="line">    <span class="comment">// 3150 把用户空间数据ubuf拷贝到bwr </span></span><br><span class="line">    <span class="keyword">if</span> (copy_from_user(&amp;bwr, ubuf, <span class="keyword">sizeof</span>(bwr))) &#123; </span><br><span class="line">        <span class="keyword">if</span> (bwr.write_size &gt; <span class="number">0</span>) &#123; </span><br><span class="line">            <span class="comment">// 当写缓存中有数据，则执行binder写操作 </span></span><br><span class="line">            ret = binder_thread_write(proc, thread, bwr.write_buffer, bwr.write_size, &amp;bwr.write_consumed); </span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">if</span> (bwr.read_size &gt; <span class="number">0</span>) &#123; </span><br><span class="line">            <span class="comment">// 当读缓存中有数据，则执行binder读操作 </span></span><br><span class="line">            ret = binder_thread_read(proc, thread, bwr.read_buffer, bwr.read_size, &amp;bwr.read_consumed, filp-&gt;f_flags &amp; O_NONBLOCK); </span><br><span class="line">            <span class="comment">// 进程todo队列不为空,则唤醒该队列中的线程 </span></span><br><span class="line">            <span class="keyword">if</span> (!list_empty(&amp;proc-&gt;todo)) wake_up_interruptible(&amp;proc-&gt;wait); </span><br><span class="line">        &#125;</span><br><span class="line">        <span class="comment">//把内核空间数据bwr拷贝到ubuf </span></span><br><span class="line">        <span class="keyword">if</span> (copy_to_user(ubuf, &amp;bwr, <span class="keyword">sizeof</span>(bwr)))</span><br></pre></td></tr></table></figure>




</li>
</ul>
<h1 id="Service-manager"><a href="#Service-manager" class="headerlink" title="Service_manager"></a>Service_manager</h1><h2 id="1、作用"><a href="#1、作用" class="headerlink" title="1、作用"></a>1、作用</h2><p>​    管理系统服务，将系统服务的Ibinder递交给客户端，就是一个管家的身份，SM本身也是一个系统服务。</p>
<p>​    因为系统服务很多，客户端直接去拿会很麻烦，所以有了Service_manager，他的handler值为0，客户端只用去找handler值为0的服务就好。</p>
<img src="/2020/10/22/binder/SM%E4%BD%9C%E7%94%A8.png" class title="SM作用">

<h2 id="2、注册"><a href="#2、注册" class="headerlink" title="2、注册"></a>2、注册</h2><img src="/2020/10/22/binder/SM%E6%95%B4%E4%BD%93%E6%B5%81%E7%A8%8B.png" class title="SM整体流程">

<ul>
<li><p>ServiceManager是由init进程通过解析init.rc文件而创建的，其所对应的可执行程序servicemanager，</p>
<p>所对应的源文件是service_manager.c，进程名为servicemanager。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">service servicemanager /system/bin/servicemanager </span><br><span class="line">    <span class="class"><span class="keyword">class</span> <span class="title">core</span> </span></span><br><span class="line"><span class="class">    <span class="title">user</span> <span class="title">system</span> </span></span><br><span class="line"><span class="class">    <span class="title">group</span> <span class="title">system</span> </span></span><br><span class="line"><span class="class">    <span class="title">critical</span> </span></span><br><span class="line"><span class="class">    <span class="title">onrestart</span> <span class="title">restart</span> <span class="title">healthd</span> </span></span><br><span class="line"><span class="class">    <span class="title">onrestart</span> <span class="title">restart</span> <span class="title">zygote</span> </span></span><br><span class="line"><span class="class">    <span class="title">onrestart</span> <span class="title">restart</span> <span class="title">media</span> </span></span><br><span class="line"><span class="class">    <span class="title">onrestart</span> <span class="title">restart</span> <span class="title">surfaceflinger</span> </span></span><br><span class="line"><span class="class">    <span class="title">onrestart</span> <span class="title">restart</span> <span class="title">drm</span></span></span><br></pre></td></tr></table></figure>
</li>
<li><p>会进入main方法，启动ServiceManager的入口函数是 service_manager.c 中的main()方法</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">frameworks/native/cmds/servicemanager/service_manager.c </span><br><span class="line">    <span class="comment">// 354 </span></span><br><span class="line">    <span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">(<span class="keyword">int</span> argc, <span class="keyword">char</span> **argv)</span> </span></span><br><span class="line"><span class="function">    <span class="comment">// 358 打开 binder驱动，申请 128k字节大小的内存空间---见后面小节 </span></span></span><br><span class="line"><span class="function">    bs </span>= binder_open(<span class="number">128</span>*<span class="number">1024</span>); </span><br><span class="line">	<span class="comment">// 364 设为守护进程，成为 binder大管理者---见后面小节 </span></span><br><span class="line">	<span class="keyword">if</span> (binder_become_context_manager(bs))</span><br><span class="line">    <span class="comment">// 391 进入无限循环，处理client端发来的请求---见后面小节 </span></span><br><span class="line">    binder_loop(bs, svcmgr_handler);</span><br></pre></td></tr></table></figure>

<ul>
<li><p>binder_open</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">frameworks/native/cmds/servicemanager/binder.c </span><br><span class="line"></span><br><span class="line"><span class="function">struct binder_state *<span class="title">binder_open</span><span class="params">(<span class="keyword">size_t</span> mapsize)</span></span>&#123;</span><br><span class="line">    </span><br><span class="line">	<span class="comment">// 98 这个结构体记录了 service_manager 中有关于 binder 的所有信息 </span></span><br><span class="line">	<span class="class"><span class="keyword">struct</span> <span class="title">binder_state</span> *<span class="title">bs</span>;</span></span><br><span class="line">	<span class="comment">// 107 打开 binder驱动，得到文件描述符</span></span><br><span class="line">	bs-&gt;fd = <span class="built_in">open</span>(<span class="string">"/dev/binder"</span>, O_RDWR);</span><br><span class="line">	<span class="comment">// 123</span></span><br><span class="line">	bs-&gt;mapsize = mapsize;<span class="comment">// service_manager自己设置的，大小为 128kb </span></span><br><span class="line">	<span class="comment">/*通过系统调用，mmap内存映射，mmap必须是 page的整数倍(即 4kb的整数倍)</span></span><br><span class="line"><span class="comment"> 	*把SM与binder驱动的虚拟空间映射到相同的物理空间</span></span><br><span class="line"><span class="comment"> 	*/</span></span><br><span class="line">	bs-&gt;mapped = mmap(<span class="literal">NULL</span>, mapsize, PROT_READ, MAP_PRIVATE, bs-&gt;fd, <span class="number">0</span>);</span><br><span class="line">    </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p>binder_become_context_manager</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">frameworks/native/cmds/servicemanager/binder.c</span><br><span class="line"><span class="comment">// 146 </span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">binder_become_context_manager</span><span class="params">(struct binder_state *bs)</span> </span>&#123; </span><br><span class="line">	<span class="keyword">return</span> ioctl(bs-&gt;fd, BINDER_SET_CONTEXT_MGR, <span class="number">0</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>进入binder_ioctl</p>
<ul>
<li><p>binder_ioctl</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">case</span> BINDER_SET_CONTEXT_MGR:</span><br><span class="line">		ret = binder_ioctl_set_ctx_mgr(filp);</span><br><span class="line">		<span class="keyword">if</span> (ret)</span><br><span class="line">			<span class="keyword">goto</span> err;</span><br><span class="line">		<span class="keyword">break</span>;</span><br></pre></td></tr></table></figure>
</li>
<li><p>binder_ioctl_set_ctx_mgr</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">//保证只创建一次 mgr_node对象，不为 null就直接返回</span></span><br><span class="line"><span class="keyword">if</span> (context-&gt;binder_context_mgr_node)&#123;&#125;</span><br><span class="line"><span class="comment">//uid是否有效，当前是无效的</span></span><br><span class="line"><span class="keyword">if</span> (uid_valid(context-&gt;binder_context_mgr_uid)) &#123;</span><br><span class="line">    </span><br><span class="line">&#125;<span class="keyword">else</span>&#123;</span><br><span class="line">    <span class="comment">//设置当前线程 euid作为 service_manager的 uid</span></span><br><span class="line">    context-&gt;binder_context_mgr_uid = curr_euid;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// 创建 service_manager实体</span></span><br><span class="line">context-&gt;binder_context_mgr_node = binder_new_node(proc, <span class="number">0</span>, <span class="number">0</span>);</span><br><span class="line"><span class="comment">//将 binder_context_mgr_node的强弱引用各加 1</span></span><br><span class="line">context-&gt;binder_context_mgr_node-&gt;local_weak_refs++;</span><br><span class="line">context-&gt;binder_context_mgr_node-&gt;local_strong_refs++;</span><br><span class="line">context-&gt;binder_context_mgr_node-&gt;has_strong_ref = <span class="number">1</span>;</span><br><span class="line">context-&gt;binder_context_mgr_node-&gt;has_weak_ref = <span class="number">1</span>;</span><br></pre></td></tr></table></figure>
</li>
<li><p>binder_new_node</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">static</span> struct binder_node *<span class="title">binder_new_node</span><span class="params">(struct binder_proc *proc,</span></span></span><br><span class="line"><span class="function"><span class="params">					   <span class="keyword">binder_uintptr_t</span> ptr,</span></span></span><br><span class="line"><span class="function"><span class="params">					   <span class="keyword">binder_uintptr_t</span> cookie)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="class"><span class="keyword">struct</span> <span class="title">rb_node</span> **<span class="title">p</span> = &amp;<span class="title">proc</span>-&gt;<span class="title">nodes</span>.<span class="title">rb_node</span>;</span></span><br><span class="line">	<span class="class"><span class="keyword">struct</span> <span class="title">rb_node</span> *<span class="title">parent</span> = <span class="title">NULL</span>;</span></span><br><span class="line">	<span class="class"><span class="keyword">struct</span> <span class="title">binder_node</span> *<span class="title">node</span>;</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">//首次进来为空</span></span><br><span class="line">	<span class="keyword">while</span> (*p) &#123;</span><br><span class="line">		</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="comment">// 给新创建的binder_node 分配内核空间</span></span><br><span class="line">	node = kzalloc(<span class="keyword">sizeof</span>(*node), GFP_KERNEL);</span><br><span class="line">    <span class="comment">//将新创建的 node对象添加到 proc红黑树</span></span><br><span class="line">	rb_link_node(&amp;node-&gt;rb_node, parent, p);</span><br><span class="line">	rb_insert_color(&amp;node-&gt;rb_node, &amp;proc-&gt;nodes);</span><br><span class="line">    </span><br><span class="line">    <span class="comment">//初始化binder_node</span></span><br><span class="line">	node-&gt;debug_id = ++binder_last_id;</span><br><span class="line">	node-&gt;proc = proc;</span><br><span class="line">	node-&gt;ptr = ptr;</span><br><span class="line">	node-&gt;cookie = cookie;</span><br><span class="line">	node-&gt;work.type = BINDER_WORK_NODE;</span><br><span class="line">	INIT_LIST_HEAD(&amp;node-&gt;work.entry);</span><br><span class="line">	INIT_LIST_HEAD(&amp;node-&gt;async_todo);</span><br><span class="line"></span><br><span class="line">	<span class="keyword">return</span> node;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
</ul>
</li>
<li><p>binder_loop</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">binder_loop</span><span class="params">(struct binder_state *bs, binder_handler func)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    bwr.write_size = <span class="number">0</span>;<span class="comment">//初始化为0</span></span><br><span class="line">    bwr.write_consumed = <span class="number">0</span>;</span><br><span class="line">    bwr.write_buffer = <span class="number">0</span>;</span><br><span class="line"></span><br><span class="line">    readbuf[<span class="number">0</span>] = BC_ENTER_LOOPER;<span class="comment">//读写要初始化的命令</span></span><br><span class="line">    binder_write(bs, readbuf, <span class="keyword">sizeof</span>(<span class="keyword">uint32_t</span>));<span class="comment">//设置线程的looper状态为循环状态</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">for</span> (;;) &#123;</span><br><span class="line">        bwr.read_size = <span class="keyword">sizeof</span>(readbuf);<span class="comment">//不为0，进入binder_thread_read</span></span><br><span class="line">        bwr.read_consumed = <span class="number">0</span>;</span><br><span class="line">        bwr.read_buffer = (<span class="keyword">uintptr_t</span>) readbuf;</span><br><span class="line"></span><br><span class="line">        <span class="comment">//不断的binder读数据，没有数据会进入休眠状态</span></span><br><span class="line">        res = ioctl(bs-&gt;fd, BINDER_WRITE_READ, &amp;bwr);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<ul>
<li><p>binder_write</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">binder_write</span><span class="params">(struct binder_state *bs, <span class="keyword">void</span> *data, <span class="keyword">size_t</span> len)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">binder_write_read</span> <span class="title">bwr</span>;</span></span><br><span class="line">    <span class="keyword">int</span> res;</span><br><span class="line"></span><br><span class="line">    bwr.write_size = len;<span class="comment">//大于0，进入binder_thread_write</span></span><br><span class="line">    bwr.write_consumed = <span class="number">0</span>;</span><br><span class="line">    bwr.write_buffer = (<span class="keyword">uintptr_t</span>) data;<span class="comment">//此处 data为 BC_ENTER_LOOPER</span></span><br><span class="line">    bwr.read_size = <span class="number">0</span>;<span class="comment">//为0，read不会进去</span></span><br><span class="line">    bwr.read_consumed = <span class="number">0</span>;</span><br><span class="line">    bwr.read_buffer = <span class="number">0</span>;</span><br><span class="line">    <span class="comment">//设置线程的looper状态为循环状态</span></span><br><span class="line">    res = ioctl(bs-&gt;fd, BINDER_WRITE_READ, &amp;bwr);</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">return</span> res;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p>binder_thread_write</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">static</span> <span class="keyword">int</span> <span class="title">binder_thread_write</span><span class="params">(struct binder_proc *proc,</span></span></span><br><span class="line"><span class="function"><span class="params">			struct binder_thread *thread,</span></span></span><br><span class="line"><span class="function"><span class="params">			<span class="keyword">binder_uintptr_t</span> binder_buffer, <span class="keyword">size_t</span> <span class="built_in">size</span>,</span></span></span><br><span class="line"><span class="function"><span class="params">			<span class="keyword">binder_size_t</span> *consumed)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="comment">//获取命令，即BC_ENTER_LOOPER</span></span><br><span class="line">    <span class="keyword">if</span> (get_user(cmd, (<span class="keyword">uint32_t</span> __user *)ptr))</span><br><span class="line">			<span class="keyword">return</span> -EFAULT;</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">case</span> BC_ENTER_LOOPER:</span><br><span class="line">			binder_debug(BINDER_DEBUG_THREADS,</span><br><span class="line">				     <span class="string">"%d:%d BC_ENTER_LOOPER\n"</span>,</span><br><span class="line">				     proc-&gt;pid, thread-&gt;pid);</span><br><span class="line">			<span class="keyword">if</span> (thread-&gt;looper &amp; BINDER_LOOPER_STATE_REGISTERED) &#123;</span><br><span class="line">				thread-&gt;looper |= BINDER_LOOPER_STATE_INVALID;</span><br><span class="line">				binder_user_error(<span class="string">"%d:%d ERROR: BC_ENTER_LOOPER called after BC_REGISTER_LOOPER\n"</span>,</span><br><span class="line">					proc-&gt;pid, thread-&gt;pid);</span><br><span class="line">			&#125;</span><br><span class="line">    		<span class="comment">//设置线程的looper状态</span></span><br><span class="line">			thread-&gt;looper |= BINDER_LOOPER_STATE_ENTERED;</span><br><span class="line">			<span class="keyword">break</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p>binder_thread_read</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">static</span> <span class="keyword">int</span> <span class="title">binder_thread_read</span><span class="params">(struct binder_proc *proc,</span></span></span><br><span class="line"><span class="function"><span class="params">			      struct binder_thread *thread,</span></span></span><br><span class="line"><span class="function"><span class="params">			      <span class="keyword">binder_uintptr_t</span> binder_buffer, <span class="keyword">size_t</span> <span class="built_in">size</span>,</span></span></span><br><span class="line"><span class="function"><span class="params">			      <span class="keyword">binder_size_t</span> *consumed, <span class="keyword">int</span> non_block)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (*consumed == <span class="number">0</span>) &#123;</span><br><span class="line">        <span class="comment">//设置命令为BN_NOOP</span></span><br><span class="line">		<span class="keyword">if</span> (put_user(BR_NOOP, (<span class="keyword">uint32_t</span> __user *)ptr))</span><br><span class="line">			<span class="keyword">return</span> -EFAULT;</span><br><span class="line">		ptr += <span class="keyword">sizeof</span>(<span class="keyword">uint32_t</span>);</span><br><span class="line">	&#125;</span><br><span class="line">    <span class="comment">//wait_for_proc_work为true</span></span><br><span class="line">    wait_for_proc_work = thread-&gt;transaction_stack == <span class="literal">NULL</span> &amp;&amp;</span><br><span class="line">				list_empty(&amp;thread-&gt;todo);</span><br><span class="line">    <span class="comment">//准备好的线程个数+1</span></span><br><span class="line">    <span class="keyword">if</span> (wait_for_proc_work)</span><br><span class="line">		proc-&gt;ready_threads++;</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">if</span> (wait_for_proc_work) &#123;</span><br><span class="line">        <span class="keyword">if</span> (non_block) &#123;<span class="comment">//非阻塞操作，service_manager是阻塞的，所以 if不命中</span></span><br><span class="line">			<span class="keyword">if</span> (!binder_has_proc_work(proc, thread))</span><br><span class="line">				ret = -EAGAIN;</span><br><span class="line">		&#125; <span class="keyword">else</span><span class="comment">//进入else，开始等待</span></span><br><span class="line">			ret = wait_event_freezable_exclusive(proc-&gt;wait, binder_has_proc_work(proc, thread));</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
</ul>
</li>
</ul>
</li>
<li><p>注册总结</p>
<ul>
<li>打开驱动，内存映射（设置大小 128K）</li>
<li>设置 SM 为大管家 — sm  作用  为了管理系统服务<ul>
<li>创建 binder_node 结构体对象</li>
<li>proc -&gt; binder_node</li>
<li>创建  work  和 todo -&gt; 类似 messageQueue</li>
</ul>
</li>
<li>BC_ENTER_LOOPER 命令<ul>
<li>写入状态Loop</li>
<li>去读数据：binder_thread_read：ret = wait_event_freezable_exclusive(proc-&gt;wait, binder_has_proc_work(proc, thread)); 进入等待</li>
</ul>
</li>
</ul>
</li>
</ul>
<h2 id="3、获取"><a href="#3、获取" class="headerlink" title="3、获取"></a>3、获取</h2><h3 id="3-1-native层"><a href="#3-1-native层" class="headerlink" title="3.1 native层"></a>3.1 native层</h3><p>获取Service Manager是通过defaultServiceManager()方法来完成。</p>
<ul>
<li><p>defaultServiceManager</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function">sp&lt;IServiceManager&gt; <span class="title">defaultServiceManager</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="comment">/* 单例模式，如果不为空直接返回 */</span> </span><br><span class="line">    <span class="keyword">if</span> (gDefaultServiceManager != <span class="literal">NULL</span>) <span class="keyword">return</span> gDefaultServiceManager; </span><br><span class="line">    </span><br><span class="line">     AutoMutex _l(gDefaultServiceManagerLock); </span><br><span class="line">     <span class="comment">/* 创建或者获取 SM时，SM可能未准备就绪，这时通过 sleep 1秒后，循环尝试获取直到成功 */</span> </span><br><span class="line">     <span class="keyword">while</span> (gDefaultServiceManager == <span class="literal">NULL</span>) &#123; </span><br><span class="line">         <span class="comment">/* 分为三块分析 */</span></span><br><span class="line">         gDefaultServiceManager = interface_cast&lt;IServiceManager&gt;( </span><br><span class="line">             ProcessState::self()-&gt;getContextObject(<span class="literal">NULL</span>)); </span><br><span class="line">         <span class="keyword">if</span> (gDefaultServiceManager == <span class="literal">NULL</span>) </span><br><span class="line">             sleep(<span class="number">1</span>); </span><br><span class="line">     &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<ul>
<li><p>ProcessState::self</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function">sp&lt;ProcessState&gt; <span class="title">ProcessState::self</span><span class="params">()</span> </span>&#123; </span><br><span class="line">    <span class="comment">/* 单例模式 */</span> <span class="keyword">if</span> (gProcess != <span class="literal">NULL</span>) &#123;</span><br><span class="line">        <span class="keyword">return</span> gProcess;</span><br><span class="line">    &#125;</span><br><span class="line">    gProcess = <span class="keyword">new</span> ProcessState; <span class="comment">// 实例化 ProcessState </span></span><br><span class="line">    <span class="keyword">return</span> gProcess;</span><br></pre></td></tr></table></figure>
</li>
<li><p>ProcessState::ProcessState</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">ProcessState::ProcessState() : mDriverFD(open_driver())&#123;</span><br><span class="line">    <span class="comment">// 358 采用内存映射函数 mmap，给 binder分配一块大小为 (1M-8K)的虚拟地址空间,用来接收事务</span></span><br><span class="line">    mVMStart = mmap(<span class="number">0</span>, BINDER_VM_SIZE, PROT_READ, MAP_PRIVATE | MAP_NORESERVE, mDriverFD, <span class="number">0</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<ul>
<li><p>open_driver</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">static</span> <span class="keyword">int</span> <span class="title">open_driver</span><span class="params">()</span></span>&#123;</span><br><span class="line">    <span class="comment">// 313 打开 /dev/binder设备，建立与内核的 Binder驱动的交互通道 </span></span><br><span class="line">    <span class="keyword">int</span> fd = <span class="built_in">open</span>(<span class="string">"/dev/binder"</span>, O_RDWR); </span><br><span class="line">    <span class="comment">// 328 通过 ioctl设置 binder驱动，能支持的最大线程数</span></span><br><span class="line">    <span class="keyword">size_t</span> maxThreads = DEFAULT_MAX_BINDER_THREADS;</span><br><span class="line">    result = ioctl(fd, BINDER_SET_MAX_THREADS, &amp;maxThreads);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
</ul>
</li>
<li><p>ProcessState::getContextObject</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function">sp&lt;IBinder&gt; <span class="title">ProcessState::getContextObject</span><span class="params">(<span class="keyword">const</span> sp&lt;IBinder&gt;&amp; <span class="comment">/*caller*/</span>)</span> </span>&#123; </span><br><span class="line">    <span class="comment">// 参数为0，获取service_manager服务 </span></span><br><span class="line">    <span class="keyword">return</span> getStrongProxyForHandle(<span class="number">0</span>); </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<ul>
<li><p>getStrongProxyForHandle</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 179</span></span><br><span class="line"><span class="function">sp&lt;IBinder&gt; <span class="title">ProcessState::getStrongProxyForHandle</span><span class="params">(<span class="keyword">int32_t</span> handle)</span></span>&#123; </span><br><span class="line">	<span class="comment">// 查找 handle对应的资源项</span></span><br><span class="line">	handle_entry* e = lookupHandleLocked(handle); </span><br><span class="line">	<span class="comment">// 192 当handle值所对应的IBinder不存在或弱引用无效时 </span></span><br><span class="line">	<span class="keyword">if</span> (b == <span class="literal">NULL</span> || !e-&gt;refs-&gt;attemptIncWeak(<span class="keyword">this</span>)) &#123; </span><br><span class="line">    	<span class="comment">// 214 通过ping操作测试binder是否准备就绪 </span></span><br><span class="line">    	<span class="keyword">status_t</span> status = IPCThreadState::self()-&gt;transact( <span class="number">0</span>, IBinder::PING_TRANSACTION, data, <span class="literal">NULL</span>, <span class="number">0</span>); </span><br><span class="line">    	<span class="comment">// 220 创建BpBinder对象 </span></span><br><span class="line">    	b = <span class="keyword">new</span> BpBinder(handle);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p>BpBinder::BpBinder</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 89 </span></span><br><span class="line">BpBinder::BpBinder(<span class="keyword">int32_t</span> handle) : mHandle(handle) &#123; </span><br><span class="line">    <span class="comment">/* 支持强弱引用计数，OBJECT_LIFETIME_WEAK表示目标对象的生命周期受弱指针控制 */</span> 			            	extendObjectLifetime(OBJECT_LIFETIME_WEAK); </span><br><span class="line">    <span class="comment">/* handle所对应的 bindle弱引用 + 1 */</span> </span><br><span class="line">    IPCThreadState::self()-&gt;incWeakHandle(handle);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
</ul>
</li>
<li><p>interface_cast</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">template</span>&lt;<span class="keyword">typename</span> INTERFACE&gt; <span class="keyword">inline</span> sp&lt;INTERFACE&gt; <span class="title">interface_cast</span><span class="params">(<span class="keyword">const</span> sp&lt;IBinder&gt;&amp; obj)</span> </span>&#123; </span><br><span class="line">    <span class="comment">// 等价于：IServiceManager::asInterface </span></span><br><span class="line">    <span class="keyword">return</span> INTERFACE::asInterface(obj); </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<ul>
<li><p>IServiceManager::asInterface</p>
<p>​    对于asInterface()函数，通过搜索代码，你会发现根本找不到这个方法是在哪里定义这个函数的, 其实是</p>
<p>通过模板函数来定义的。</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 74 </span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> DECLARE_META_INTERFACE(INTERFACE) </span></span><br><span class="line"><span class="keyword">static</span> <span class="keyword">const</span> android::String16 descriptor;  </span><br><span class="line">    static android::sp&lt;I##INTERFACE&gt; asInterface(</span><br><span class="line">        <span class="keyword">const</span> android::sp&lt;android::IBinder&gt;&amp; obj); </span><br><span class="line">	<span class="function"><span class="keyword">virtual</span> <span class="keyword">const</span> android::String16&amp; <span class="title">getInterfaceDescriptor</span><span class="params">()</span> <span class="keyword">const</span></span>; </span><br><span class="line">	I##INTERFACE();</span><br><span class="line">	<span class="keyword">virtual</span> ~I##INTERFACE();</span><br><span class="line">	<span class="comment">// 83 </span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> IMPLEMENT_META_INTERFACE(INTERFACE, NAME) </span></span><br><span class="line">	<span class="keyword">const</span> android::String16 I##INTERFACE::descriptor(NAME); </span><br><span class="line">	<span class="keyword">const</span> android::String16&amp; </span><br><span class="line">        I##INTERFACE::getInterfaceDescriptor() <span class="keyword">const</span> &#123;  </span><br><span class="line">    <span class="keyword">return</span> I##INTERFACE::descriptor;</span><br><span class="line">    &#125;</span><br><span class="line">	android::sp&lt;I##INTERFACE&gt; I##INTERFACE::asInterface(</span><br><span class="line">        <span class="keyword">const</span> android::sp&lt;android::IBinder&gt;&amp; obj)</span><br><span class="line">    &#123;</span><br><span class="line">        android::sp&lt;I##INTERFACE&gt; intr; </span><br><span class="line">        <span class="keyword">if</span> (obj != <span class="literal">NULL</span>) &#123;</span><br><span class="line">            intr = <span class="keyword">static_cast</span>&lt;I##INTERFACE*&gt;(</span><br><span class="line">                obj-&gt;queryLocalInterface(</span><br><span class="line">                    I##INTERFACE::descriptor).<span class="built_in">get</span>()); </span><br><span class="line">            <span class="keyword">if</span> (intr == <span class="literal">NULL</span>) &#123;</span><br><span class="line">                intr = <span class="keyword">new</span> Bp##INTERFACE(obj);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> intr;</span><br><span class="line">    &#125;</span><br><span class="line">	I##INTERFACE::I##INTERFACE()&#123;&#125; </span><br><span class="line">	I##INTERFACE::~I##INTERFACE()&#123;&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p>DECLARE_META_INTERFACE</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">DECLARE_META_INTERFACE(ServiceManager)&#123;</span><br><span class="line">    <span class="keyword">static</span> <span class="keyword">const</span> android::String16 descriptor; </span><br><span class="line">    <span class="function"><span class="keyword">static</span> android::sp&lt; IServiceManager &gt; <span class="title">asInterface</span><span class="params">(<span class="keyword">const</span> android::sp&lt;android::IBinder&gt;&amp; obj)</span> 	<span class="keyword">virtual</span> <span class="keyword">const</span> android::String16&amp; <span class="title">getInterfaceDescriptor</span><span class="params">()</span> <span class="keyword">const</span></span>; </span><br><span class="line">    IServiceManager (); </span><br><span class="line">    <span class="keyword">virtual</span> ~IServiceManager(); </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>该过程主要是声明asInterface(),getInterfaceDescriptor()方法。</p>
</li>
<li><p>IMPLEMENT_META_INTERFACE</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">IMPLEMENT_META_INTERFACE(ServiceManager,<span class="string">"android.os.IServiceManager"</span>)&#123;</span><br><span class="line">    <span class="function"><span class="keyword">const</span> android::String16 <span class="title">IServiceManager::descriptor</span><span class="params">(“android.os.IServiceManager”)</span></span>; </span><br><span class="line">    <span class="function"><span class="keyword">const</span> android::String16&amp; <span class="title">IServiceManager::getInterfaceDescriptor</span><span class="params">()</span> <span class="keyword">const</span> </span></span><br><span class="line"><span class="function">    </span>&#123; </span><br><span class="line">        <span class="keyword">return</span> IServiceManager::descriptor; </span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function">android::sp&lt;IServiceManager&gt; <span class="title">IServiceManager::asInterface</span><span class="params">(<span class="keyword">const</span> android::sp&lt;android::IBinder&gt;&amp; obj)</span> </span></span><br><span class="line"><span class="function">    </span>&#123; </span><br><span class="line">        android::sp&lt;IServiceManager&gt; intr; </span><br><span class="line">        <span class="keyword">if</span>(obj != <span class="literal">NULL</span>) &#123; </span><br><span class="line">            intr = <span class="keyword">static_cast</span>&lt;IServiceManager *&gt;( </span><br><span class="line">                obj-&gt;queryLocalInterface(IServiceManager::descriptor).<span class="built_in">get</span>()); </span><br><span class="line">            <span class="keyword">if</span> (intr == <span class="literal">NULL</span>) &#123; </span><br><span class="line">                <span class="comment">// 等价于 new BpServiceManager(BpBinder) </span></span><br><span class="line">                intr = <span class="keyword">new</span> BpServiceManager(obj); </span><br><span class="line">            &#125; </span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> intr; </span><br><span class="line">    &#125;</span><br><span class="line">    IServiceManager::IServiceManager () &#123; &#125; </span><br><span class="line">    IServiceManager::~ IServiceManager() &#123; &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
</ul>
</li>
<li><p>BpServiceManager</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">frameworks/native/libs/binder/IServiceManager.cpp </span><br><span class="line">    <span class="comment">// 126 </span></span><br><span class="line">    <span class="class"><span class="keyword">class</span> <span class="title">BpServiceManager</span> :</span> <span class="keyword">public</span> BpInterface&lt;IServiceManager&gt; </span><br><span class="line">        <span class="comment">// 129 </span></span><br><span class="line">        BpServiceManager(<span class="keyword">const</span> sp&lt;IBinder&gt;&amp; impl) : BpInterface&lt;IServiceManager&gt;(impl)</span><br></pre></td></tr></table></figure>

<ul>
<li><p>BpInterface::BpInterface</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 134 </span></span><br><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">typename</span> INTERFACE&gt; </span><br><span class="line"><span class="keyword">inline</span> BpInterface&lt;INTERFACE&gt;::BpInterface(<span class="keyword">const</span> sp&lt;IBinder&gt;&amp; remote) : BpRefBase(remote)</span><br></pre></td></tr></table></figure>
</li>
<li><p>BpRefBase</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 241 mRemote指向 new BpBinder(0)，从而 BpServiceManager能够利用 Binder进行通过通信 </span></span><br><span class="line">BpRefBase::BpRefBase(<span class="keyword">const</span> sp&lt;IBinder&gt;&amp; o) : mRemote(o.<span class="built_in">get</span>()), mRefs(<span class="literal">NULL</span>), mState(<span class="number">0</span>)</span><br></pre></td></tr></table></figure>

</li>
</ul>
</li>
</ul>
</li>
</ul>
<h3 id="3-2-Java层"><a href="#3-2-Java层" class="headerlink" title="3.2 Java层"></a>3.2 Java层</h3><h2 id="4、AIDL"><a href="#4、AIDL" class="headerlink" title="4、AIDL"></a>4、AIDL</h2><ul>
<li><p>Stub</p>
<p>​    这个类是实现类的一个内部类，是客户端与服务端沟通的桥梁，在客户端通过<strong>asInterface方法</strong>获取到Binder对象或者Binder代理对象，会进行一个判断，如果在同一个进程，就会获取Binder对象，如果不为一个进程，就会获取Binder代理对象。</p>
</li>
<li><p>Proxy</p>
<p>​    这个是Stub的内部类，Binder的代理对象就是它，它负责处理跨进程的操作，重写了aidl接口内的方法，并且操作时调用transact方法进行跨进程。</p>
</li>
<li><p>transact</p>
<p>​    这个方法用于处理跨进程，调用这个方法时用户进程会挂起。</p>
</li>
<li><p>data、reply</p>
<p>​    传递数据时，会有data、reply两种，data是把需要传出去的数据封装，reply是用于接收传回来的数据。</p>
</li>
</ul>
]]></content>
  </entry>
  <entry>
    <title>事件分发二级联动</title>
    <url>/2020/07/28/%E4%BA%8B%E4%BB%B6%E5%88%86%E5%8F%91%E4%BA%8C%E7%BA%A7%E8%81%94%E5%8A%A8/</url>
    <content><![CDATA[<h2 id="1-事件分发流程"><a href="#1-事件分发流程" class="headerlink" title="1 事件分发流程"></a>1 事件分发流程</h2><img src="/2020/07/28/%E4%BA%8B%E4%BB%B6%E5%88%86%E5%8F%91%E4%BA%8C%E7%BA%A7%E8%81%94%E5%8A%A8/%E5%9B%BE%E7%89%872.png" class title="分发流程">

<ul>
<li><p>从控件回调回activity开始分发，调用dispatchTouchEvent</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">boolean</span> <span class="title">dispatchTouchEvent</span><span class="params">(MotionEvent ev)</span> </span>&#123;</span><br><span class="line">      <span class="keyword">if</span> (ev.getAction() == MotionEvent.ACTION_DOWN) &#123;</span><br><span class="line">          onUserInteraction();</span><br><span class="line">      &#125;</span><br><span class="line">      <span class="keyword">if</span> (getWindow().superDispatchTouchEvent(ev)) &#123;</span><br><span class="line">          <span class="keyword">return</span> <span class="keyword">true</span>;</span><br><span class="line">      &#125;</span><br><span class="line">      <span class="keyword">return</span> onTouchEvent(ev);</span><br><span class="line">  &#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p>进入PhoneWindow中</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">boolean</span> <span class="title">superDispatchTouchEvent</span><span class="params">(MotionEvent event)</span> </span>&#123;</span><br><span class="line">       <span class="keyword">return</span> mDecor.superDispatchTouchEvent(event);</span><br><span class="line">   &#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p>进入DecorView中</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">boolean</span> <span class="title">superDispatchTouchEvent</span><span class="params">(MotionEvent event)</span> </span>&#123;</span><br><span class="line">       <span class="keyword">return</span> <span class="keyword">super</span>.dispatchTouchEvent(event);</span><br><span class="line">   &#125;</span><br></pre></td></tr></table></figure>

<p>此时调用父类的dispatchTouchEvent方法，而DecorView的父类是FragmentLayout，并没有重写dispatchTouchEvent方法，所以交给它的父类ViewGroup来调用。</p>
</li>
<li><p>从而进一步就进入了View和ViewGroup的树形结构</p>
<img src="/2020/07/28/%E4%BA%8B%E4%BB%B6%E5%88%86%E5%8F%91%E4%BA%8C%E7%BA%A7%E8%81%94%E5%8A%A8/%E5%9B%BE%E7%89%871.png" class title="树形结构">

</li>
</ul>
<h3 id="1-1-事件处理（View）"><a href="#1-1-事件处理（View）" class="headerlink" title="1.1 事件处理（View）"></a>1.1 事件处理（View）</h3><ul>
<li><p>view的dispatchTouchEvent方法</p>
</li>
<li><p>关键代码</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">   <span class="keyword">if</span> (onFilterTouchEventForSecurity(event)) &#123;</span><br><span class="line">         <span class="keyword">if</span> ((mViewFlags &amp; ENABLED_MASK) == ENABLED &amp;&amp; handleScrollBarDragging(event)) &#123;</span><br><span class="line">             result = <span class="keyword">true</span>;</span><br><span class="line">         &#125;</span><br><span class="line">         <span class="comment">/**这一段最关键</span></span><br><span class="line"><span class="comment">         ListenerInfo li = mListenerInfo;</span></span><br><span class="line"><span class="comment">         if (li != null &amp;&amp; li.mOnTouchListener != null</span></span><br><span class="line"><span class="comment">                 &amp;&amp; (mViewFlags &amp; ENABLED_MASK) == ENABLED</span></span><br><span class="line"><span class="comment">                 &amp;&amp; li.mOnTouchListener.onTouch(this, event)) &#123;</span></span><br><span class="line"><span class="comment">             result = true;</span></span><br><span class="line"><span class="comment">         &#125;</span></span><br><span class="line"><span class="comment">**/</span></span><br><span class="line">         <span class="keyword">if</span> (!result &amp;&amp; onTouchEvent(event)) &#123;</span><br><span class="line">             result = <span class="keyword">true</span>;</span><br><span class="line">         &#125;</span><br><span class="line">     &#125;</span><br></pre></td></tr></table></figure>

<p>​    其中mListenerInfo是只要调用了setOnTouchLisener就会初始化的。</p>
<p>​    判断中的前三个条件都为true，进行到第四个条件的判断，只要OnTouch方法返回的是true，那么result就为true，否则为false。</p>
<p>​    而result为true时，在下一个if语句中，就无法调用到onTouchEvent，无法在其中调用onClick方法，所以这就是为什么onTouch返回值为true时，onClick事件被拦截。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">performClickInternal() -&gt; performClick() -&gt; li.mOnClickListener.onClick(<span class="keyword">this</span>);</span><br></pre></td></tr></table></figure>
</li>
<li><p>当然满足这一切的条件时clickable要为true</p>
</li>
</ul>
<h3 id="1-2-事件分发（ViewGroup）"><a href="#1-2-事件分发（ViewGroup）" class="headerlink" title="1.2 事件分发（ViewGroup）"></a>1.2 事件分发（ViewGroup）</h3><img src="/2020/07/28/%E4%BA%8B%E4%BB%B6%E5%88%86%E5%8F%91%E4%BA%8C%E7%BA%A7%E8%81%94%E5%8A%A8/%E5%9B%BE%E7%89%873.png" class title="事件分发例子">

<h3 id="1-2-1-ACTION-DOWN"><a href="#1-2-1-ACTION-DOWN" class="headerlink" title="1.2.1 ACTION_DOWN"></a>1.2.1 ACTION_DOWN</h3><p>首先会清除所有标记</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span> (actionMasked == MotionEvent.ACTION_DOWN) &#123;</span><br><span class="line">             <span class="comment">// Throw away all previous state when starting a new touch gesture.</span></span><br><span class="line">             <span class="comment">// The framework may have dropped the up or cancel event for the previous gesture</span></span><br><span class="line">             <span class="comment">// due to an app switch, ANR, or some other state change.</span></span><br><span class="line">             cancelAndClearTouchTargets(ev);</span><br><span class="line">             resetTouchState();</span><br><span class="line">         &#125;</span><br></pre></td></tr></table></figure>



<h4 id="1-2-1-1-拦截"><a href="#1-2-1-1-拦截" class="headerlink" title="1.2.1.1 拦截"></a>1.2.1.1 拦截</h4><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// Check for interception.</span></span><br><span class="line"><span class="keyword">final</span> <span class="keyword">boolean</span> intercepted;</span><br><span class="line"><span class="keyword">if</span> (actionMasked == MotionEvent.ACTION_DOWN</span><br><span class="line">        || mFirstTouchTarget != <span class="keyword">null</span>) &#123;</span><br><span class="line">    <span class="keyword">final</span> <span class="keyword">boolean</span> disallowIntercept = (mGroupFlags &amp; FLAG_DISALLOW_INTERCEPT) != <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">if</span> (!disallowIntercept) &#123;</span><br><span class="line">        intercepted = onInterceptTouchEvent(ev);</span><br><span class="line">        ev.setAction(action); <span class="comment">// restore action in case it was changed</span></span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        intercepted = <span class="keyword">false</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125; <span class="keyword">else</span> &#123;</span><br><span class="line">    <span class="comment">// There are no touch targets and this action is not an initial down</span></span><br><span class="line">    <span class="comment">// so this view group continues to intercept touches.</span></span><br><span class="line">    intercepted = <span class="keyword">true</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>​    可以看到intercepted被赋值为onInterceptTouchEvent(ev)的返回值。</p>
<p>​    如果被拦截，则intercepted为true</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span> (!canceled &amp;&amp; !intercepted)</span><br></pre></td></tr></table></figure>

<p>​    无法进入这个if语句，去进行与分发相关的操作。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span> (mFirstTouchTarget == <span class="keyword">null</span>) &#123;</span><br><span class="line">    <span class="comment">// No touch targets so treat this as an ordinary view.</span></span><br><span class="line">    handled = dispatchTransformedTouchEvent(ev, canceled, <span class="keyword">null</span>,</span><br><span class="line">            TouchTarget.ALL_POINTER_IDS);</span><br></pre></td></tr></table></figure>

<p>​    在这一个if语句里进行事件的分发或者处理，进入dispatchTransformedTouchEvent方法。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span> (child == <span class="keyword">null</span>) &#123;</span><br><span class="line">    handled = <span class="keyword">super</span>.dispatchTouchEvent(event);</span><br><span class="line">&#125; <span class="keyword">else</span> &#123;</span><br><span class="line">    handled = child.dispatchTouchEvent(event);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>​    在dispatchTransformedTouchEvent里可以看到，因为调用时child此参数传入为null，所以直接调用父类View的dispatchTouchEvent方法，去处理这个事件，即这个事件被拦截在这个ViewGroup。</p>
<h4 id="1-2-1-2-不拦截"><a href="#1-2-1-2-不拦截" class="headerlink" title="1.2.1.2 不拦截"></a>1.2.1.2 不拦截</h4><p>​    如果不拦截，则intercepted为false。</p>
<p>​    进入第二个if语句。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span> (actionMasked == MotionEvent.ACTION_DOWN</span><br></pre></td></tr></table></figure>

<p>如果为ACTION_DOWN就进入这个if语句</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span> (newTouchTarget == <span class="keyword">null</span> &amp;&amp; childrenCount != <span class="number">0</span>)</span><br></pre></td></tr></table></figure>

<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">final</span> ArrayList&lt;View&gt; preorderedList = buildTouchDispatchChildList()</span><br></pre></td></tr></table></figure>

<p>​    进入这个if语句，并且通过buildTouchDispatchChildList()方法构造一个数组用于存放ViewGroup中的View，并且写在上面的view会放在数组后面，拿也是从数组后面开始拿。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">for</span> (<span class="keyword">int</span> i = childrenCount - <span class="number">1</span>; i &gt;= <span class="number">0</span>; i--)</span><br></pre></td></tr></table></figure>

<p>​    进入for循环，遍历当前ViewGroup的所有子View。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span> (!canViewReceivePointerEvents(child)</span><br><span class="line">        || !isTransformedTouchPointInView(x, y, child, <span class="keyword">null</span>)) &#123;</span><br><span class="line">    ev.setTargetAccessibilityFocus(<span class="keyword">false</span>);</span><br><span class="line">    <span class="keyword">continue</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>两个条件：</p>
<ul>
<li><pre><code class="java"><span class="function"><span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">boolean</span> <span class="title">canViewReceivePointerEvents</span><span class="params">(@NonNull View child)</span> </span>{
    <span class="keyword">return</span> (child.mViewFlags &amp; VISIBILITY_MASK) == VISIBLE
            || child.getAnimation() != <span class="keyword">null</span>;
}
&lt;!--￼<span class="number">15</span>--&gt;

这个表示点击区域是否是View所在的区域。
</code></pre>
</li>
</ul>
<p>如果满足其中任意一个，就continue，直接遍历到下一个child。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span> (newTouchTarget != <span class="keyword">null</span>) &#123;</span><br><span class="line">    <span class="comment">// Child is already receiving touch within its bounds.</span></span><br><span class="line">    <span class="comment">// Give it the new pointer in addition to the ones it is handling.</span></span><br><span class="line">    newTouchTarget.pointerIdBits |= idBitsToAssign;</span><br><span class="line">    <span class="keyword">break</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>如果newTouchTarget不为null，就直接结束循环。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span> (dispatchTransformedTouchEvent(ev, <span class="keyword">false</span>, child, idBitsToAssign)) &#123;</span><br><span class="line">    <span class="comment">// Child wants to receive touch within its bounds.</span></span><br><span class="line">    mLastTouchDownTime = ev.getDownTime();</span><br><span class="line">    <span class="keyword">if</span> (preorderedList != <span class="keyword">null</span>) &#123;</span><br><span class="line">        <span class="comment">// childIndex points into presorted list, find original index</span></span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> j = <span class="number">0</span>; j &lt; childrenCount; j++) &#123;</span><br><span class="line">            <span class="keyword">if</span> (children[childIndex] == mChildren[j]) &#123;</span><br><span class="line">                mLastTouchDownIndex = j;</span><br><span class="line">                <span class="keyword">break</span>;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        mLastTouchDownIndex = childIndex;</span><br><span class="line">    &#125;</span><br><span class="line">    mLastTouchDownX = ev.getX();</span><br><span class="line">    mLastTouchDownY = ev.getY();</span><br><span class="line">    <span class="comment">//参数赋值</span></span><br><span class="line">    newTouchTarget = addTouchTarget(child, idBitsToAssign);</span><br><span class="line">    alreadyDispatchedToNewTouchTarget = <span class="keyword">true</span>;</span><br><span class="line">    <span class="keyword">break</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>​    在这里可以看到，如果dispatchTransformedTouchEvent(ev, false, child, idBitsToAssign)返回true，则进入对一些参数进行赋值并且结束循环，如果为false，则继续进行下去开始下一次循环。</p>
<p>​    <strong>在dispatchTransformedTouchEvent方法中，此时child不为null，所以调用child的dispatchTouchEvent方法，即肯定是ViewGroup的。</strong></p>
<p>​    进行完参数赋值之后，newTouchTarget == mFirstTouchTarget，target.next==null，    alreadyDispatchedToNewTouchTarget = true。</p>
<ul>
<li><p>在这个if语句中，如果返回值一直为false，即所有子View都不处理事件，则和拦截的情况没有区别，全部交给当前ViewGroup处理。</p>
</li>
<li><p>如果返回值有true，则带着变化的参数进入第三个if语句</p>
<ul>
<li><p>将进入else</p>
</li>
<li><pre><code class="java"><span class="keyword">while</span> (target != <span class="keyword">null</span>) {
    <span class="keyword">final</span> TouchTarget next = target.next;
    <span class="keyword">if</span> (alreadyDispatchedToNewTouchTarget &amp;&amp; target == newTouchTarget) {
        handled = <span class="keyword">true</span>;
    } <span class="keyword">else</span> {
        <span class="keyword">final</span> <span class="keyword">boolean</span> cancelChild = resetCancelNextUpFlag(target.child)
                || intercepted;
        <span class="keyword">if</span> (dispatchTransformedTouchEvent(ev, cancelChild,
                target.child, target.pointerIdBits)) {
            handled = <span class="keyword">true</span>;
        }
        <span class="keyword">if</span> (cancelChild) {
            <span class="keyword">if</span> (predecessor == <span class="keyword">null</span>) {
                mFirstTouchTarget = next;
            } <span class="keyword">else</span> {
                predecessor.next = next;
            }
            target.recycle();
            target = next;
            <span class="keyword">continue</span>;
        }
    }
    predecessor = target;
    target = next;
}
&lt;!--￼<span class="number">18</span>--&gt;

肯定为<span class="keyword">true</span>，所以handled为<span class="keyword">true</span>，函数最终返回<span class="keyword">true</span>，代表这个事件我不处理。


</code></pre>
</li>
</ul>
</li>
</ul>
<h3 id="1-2-2-ACTION-MOVE"><a href="#1-2-2-ACTION-MOVE" class="headerlink" title="1.2.2 ACTION_MOVE"></a>1.2.2 ACTION_MOVE</h3><p>​    同样是从DecorView这一层的dispatchTouchEvent方法开始。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span> (actionMasked == MotionEvent.ACTION_DOWN</span><br><span class="line">               || mFirstTouchTarget != <span class="keyword">null</span>) &#123;</span><br><span class="line">           <span class="keyword">final</span> <span class="keyword">boolean</span> disallowIntercept = (mGroupFlags &amp; FLAG_DISALLOW_INTERCEPT) != <span class="number">0</span>;</span><br><span class="line">           <span class="keyword">if</span> (!disallowIntercept) &#123;</span><br><span class="line">               intercepted = onInterceptTouchEvent(ev);</span><br><span class="line">               ev.setAction(action); <span class="comment">// restore action in case it was changed</span></span><br><span class="line">           &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">               intercepted = <span class="keyword">false</span>;</span><br><span class="line">           &#125;</span><br></pre></td></tr></table></figure>
<p>​    此时mFirstTouchTarget != null成立，所以还是一样的intercepted赋值。</p>
<p>​    因此进入这个if语句</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span> (!canceled &amp;&amp; !intercepted)</span><br></pre></td></tr></table></figure>

<p>​    第二个if语句因为是MOVE事件，所以无法进入，进入第三个if语句的else</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span> (mFirstTouchTarget == <span class="keyword">null</span>)</span><br></pre></td></tr></table></figure>

<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span> (alreadyDispatchedToNewTouchTarget &amp;&amp; target == newTouchTarget) &#123;</span><br><span class="line">                   handled = <span class="keyword">true</span>;</span><br><span class="line">               &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                   <span class="keyword">final</span> <span class="keyword">boolean</span> cancelChild = resetCancelNextUpFlag(target.child)</span><br><span class="line">                           || intercepted;</span><br><span class="line">                   <span class="keyword">if</span> (dispatchTransformedTouchEvent(ev, cancelChild,</span><br><span class="line">                           target.child, target.pointerIdBits)) &#123;</span><br><span class="line">                       handled = <span class="keyword">true</span>;</span><br><span class="line">                   &#125;</span><br><span class="line">                   <span class="keyword">if</span> (cancelChild) &#123;</span><br><span class="line">                       <span class="keyword">if</span> (predecessor == <span class="keyword">null</span>) &#123;</span><br><span class="line">                           mFirstTouchTarget = next;</span><br><span class="line">                       &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                           predecessor.next = next;</span><br><span class="line">                       &#125;</span><br><span class="line">                       target.recycle();</span><br><span class="line">                       target = next;</span><br><span class="line">                       <span class="keyword">continue</span>;</span><br><span class="line">                   &#125;</span><br><span class="line">               &#125;</span><br></pre></td></tr></table></figure>
<p>​    此时alreadyDispatchedToNewTouchTarget已经被重新赋值为false，所以进入else，cancelchild为false，调用</p>
<p>dispatchTransformedTouchEvent(ev, cancelChild,target.child, target.pointerIdBits)，target.child，直接把事件交给之前已经放入数组中的View或者ViewGroup处理。</p>
<p>​    <strong>由此可以看出，MOVE事件并不分发，因为没有进入第二个if语句中的循环部分。</strong></p>
<h2 id="2-处理事件冲突"><a href="#2-处理事件冲突" class="headerlink" title="2 处理事件冲突"></a>2 处理事件冲突</h2><p>只能在MOVE事件时处理事件冲突！</p>
<h3 id="2-1-内部拦截法（子View进行拦截）"><a href="#2-1-内部拦截法（子View进行拦截）" class="headerlink" title="2.1 内部拦截法（子View进行拦截）"></a>2.1 内部拦截法（子View进行拦截）</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span> (!disallowIntercept) &#123;</span><br><span class="line">                    intercepted = onInterceptTouchEvent(ev);</span><br><span class="line">                    ev.setAction(action); <span class="comment">// restore action in case it was changed</span></span><br><span class="line">                &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                    intercepted = <span class="keyword">false</span>;</span><br><span class="line">                &#125;</span><br></pre></td></tr></table></figure>

<p>​    在父View调用requestDisallowInterceptTouchEvent方法，改动disallowIntercept的值为true，则intercepted就肯定为false，可以进入之后的分发过程。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">    <span class="keyword">if</span> (disallowIntercept == ((mGroupFlags &amp; FLAG_DISALLOW_INTERCEPT) != <span class="number">0</span>)) &#123;</span><br><span class="line">        <span class="comment">// We're already in this state, assume our ancestors are too</span></span><br><span class="line">        <span class="keyword">return</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (disallowIntercept) &#123;</span><br><span class="line">        mGroupFlags |= FLAG_DISALLOW_INTERCEPT;</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        mGroupFlags &amp;= ~FLAG_DISALLOW_INTERCEPT;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// Pass it up to our parent</span></span><br><span class="line">    <span class="keyword">if</span> (mParent != <span class="keyword">null</span>) &#123;</span><br><span class="line">        mParent.requestDisallowInterceptTouchEvent(disallowIntercept);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>​    在这里改动mGroupFlags的值，让disallowIntercept的值也发生改变。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">final</span> <span class="keyword">boolean</span> disallowIntercept = (mGroupFlags &amp; FLAG_DISALLOW_INTERCEPT) != <span class="number">0</span>;</span><br></pre></td></tr></table></figure>

<p><strong>实现：</strong></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">   <span class="comment">// 内部拦截法：子view处理事件冲突</span></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">int</span> mLastX, mLastY;</span><br><span class="line"><span class="comment">//</span></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">boolean</span> <span class="title">dispatchTouchEvent</span><span class="params">(MotionEvent event)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">int</span> x = (<span class="keyword">int</span>) event.getX();</span><br><span class="line">        <span class="keyword">int</span> y = (<span class="keyword">int</span>) event.getY();</span><br><span class="line"></span><br><span class="line">        <span class="keyword">switch</span> (event.getAction()) &#123;</span><br><span class="line">            <span class="keyword">case</span> MotionEvent.ACTION_DOWN: &#123;</span><br><span class="line">                getParent().requestDisallowInterceptTouchEvent(<span class="keyword">true</span>);</span><br><span class="line">                <span class="keyword">break</span>;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">case</span> MotionEvent.ACTION_MOVE: &#123;</span><br><span class="line">                <span class="keyword">int</span> deltaX = x - mLastX;</span><br><span class="line">                <span class="keyword">int</span> deltaY = y - mLastY;</span><br><span class="line">                <span class="keyword">if</span> (Math.abs(deltaX) &gt; Math.abs(deltaY)) &#123;</span><br><span class="line">                    getParent().requestDisallowInterceptTouchEvent(<span class="keyword">false</span>);</span><br><span class="line">                &#125;</span><br><span class="line">                <span class="keyword">break</span>;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">case</span> MotionEvent.ACTION_UP: &#123;</span><br><span class="line">                <span class="keyword">break</span>;</span><br><span class="line"></span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">default</span>:</span><br><span class="line">                <span class="keyword">break</span>;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        mLastX = x;</span><br><span class="line">        mLastY = y;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">super</span>.dispatchTouchEvent(event);</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<ul>
<li><p>在DOWN事件时把disallowIntercept设置为true，让子View可以接受到事件</p>
</li>
<li><p>在MOVE事件时判断滑动方向，如果为左右，就交给父View处理，如果为上下，就自己处理</p>
</li>
<li><p>还有一个小点需要注意</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span> (actionMasked == MotionEvent.ACTION_DOWN) &#123;</span><br><span class="line">               <span class="comment">// Throw away all previous state when starting a new touch gesture.</span></span><br><span class="line">               <span class="comment">// The framework may have dropped the up or cancel event for the previous gesture</span></span><br><span class="line">               <span class="comment">// due to an app switch, ANR, or some other state change.</span></span><br><span class="line">               cancelAndClearTouchTargets(ev);</span><br><span class="line">               resetTouchState();</span><br><span class="line">           &#125;</span><br></pre></td></tr></table></figure>

<p>​    这段代码会清除所有标记，所以在DOWN事件时，会把mGroupFlags的值也清除，因此getParent().requestDisallowInterceptTouchEvent(true)这个设置也就不起作用，我们需要在父View中再进行处理。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span> (event.getAction() == MotionEvent.ACTION_DOWN)&#123;</span><br><span class="line">    <span class="keyword">super</span>.onInterceptTouchEvent(event);</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">false</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">return</span> <span class="keyword">true</span>;</span><br></pre></td></tr></table></figure>

<p>​    在父View中判断为DOWN事件时，就不拦截，交给子View处理。</p>
</li>
</ul>
<h3 id="2-2-外部拦截法（父View进行拦截）"><a href="#2-2-外部拦截法（父View进行拦截）" class="headerlink" title="2.2 外部拦截法（父View进行拦截）"></a>2.2 外部拦截法（父View进行拦截）</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 外部拦截法：父容器处理冲突</span></span><br><span class="line"><span class="comment">// 我想要把事件分发给谁就分发给谁</span></span><br><span class="line"><span class="meta">@Override</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">boolean</span> <span class="title">onInterceptTouchEvent</span><span class="params">(MotionEvent event)</span> </span>&#123;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">    <span class="keyword">int</span> x = (<span class="keyword">int</span>) event.getX();</span><br><span class="line">    <span class="keyword">int</span> y = (<span class="keyword">int</span>) event.getY();</span><br><span class="line"></span><br><span class="line">    <span class="keyword">switch</span> (event.getAction()) &#123;</span><br><span class="line">        <span class="keyword">case</span> MotionEvent.ACTION_DOWN: &#123;</span><br><span class="line">            mLastX = (<span class="keyword">int</span>) event.getX();</span><br><span class="line">            mLastY = (<span class="keyword">int</span>) event.getY();</span><br><span class="line">            <span class="keyword">break</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">case</span> MotionEvent.ACTION_MOVE: &#123;</span><br><span class="line">            <span class="keyword">int</span> deltaX = x - mLastX;</span><br><span class="line">            <span class="keyword">int</span> deltaY = y - mLastY;</span><br><span class="line">            <span class="keyword">if</span> (Math.abs(deltaX) &gt; Math.abs(deltaY)) &#123;</span><br><span class="line">                <span class="keyword">return</span> <span class="keyword">true</span>;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">break</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">case</span> MotionEvent.ACTION_UP: &#123;</span><br><span class="line">            <span class="keyword">break</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">default</span>:</span><br><span class="line">            <span class="keyword">break</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">super</span>.onInterceptTouchEvent(event);</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>​    同样的，如果滑动方向为左右，则返回true拦截事件自己处理，反之分发。</p>
<h3 id="2-3-MOVE过程中的变化"><a href="#2-3-MOVE过程中的变化" class="headerlink" title="2.3 MOVE过程中的变化"></a>2.3 MOVE过程中的变化</h3><p>​    在DOWN事件交给了子View处理，在第一次MOVE事件时就也是交给子View处理，如果在这个时候把滑动方向转换为了左右，那么在disptachTransformTouchEvent这个方法时就会触发CANCEL事件，并且是由子View发起的，在第二次MOVE事件时，mFirstTragetView变为了null，将被拦截，交给父View处理。</p>
<p>​    <strong>所以，如果一个View或者ViewGroup没有拿到DOWN事件，那么他一定拿不到MOVE事件，这句话只对叶子节点是正确的。</strong></p>
]]></content>
  </entry>
  <entry>
    <title>View的创建流程</title>
    <url>/2020/07/24/View%E7%9A%84%E5%88%9B%E5%BB%BA%E6%B5%81%E7%A8%8B/</url>
    <content><![CDATA[<h1 id="View的创建流程"><a href="#View的创建流程" class="headerlink" title="View的创建流程"></a>View的创建流程</h1><h2 id="总览"><a href="#总览" class="headerlink" title="总览"></a>总览</h2><img src="/2020/07/24/View%E7%9A%84%E5%88%9B%E5%BB%BA%E6%B5%81%E7%A8%8B/%E5%B8%83%E5%B1%80%E6%B5%81%E7%A8%8B.png" class title="布局流程">

<h2 id="ActivityThread"><a href="#ActivityThread" class="headerlink" title="ActivityThread"></a>ActivityThread</h2><ul>
<li><p>performLaunchActivity(ActivityClientRecord r, Intent customIntent)，在这个方法中进行一系列初始化工作</p>
</li>
<li><p>ContextImpl appContext = createBaseContextForActivity(r)，进行上下文初始化</p>
</li>
<li><p>activity = mInstrumentation.newActivity(cl, component.getClassName(), r.intent)，进行activity的初始化</p>
</li>
<li><p>window = r.mPendingRemoveWindow，创建一个window，但是此时的window为null</p>
<ul>
<li><p>r.mPendingRemoveWindow = r.window</p>
</li>
<li><p>r.window = r.activity.getWindow()</p>
</li>
<li><p>所以说window是从Activity类中的getWindow()里获取的</p>
</li>
<li><pre><code class="java">    <span class="function"><span class="keyword">public</span> Window <span class="title">getWindow</span><span class="params">()</span> </span>{
        <span class="keyword">return</span> mWindow;
    }
&lt;!--￼<span class="number">0</span>--&gt;
</code></pre>
</li>
</ul>
<p>通过这个方法把window与activity进行绑定，实际上是在attach方法中调用了 mWindow = new PhoneWindow(this, window, activityConfigCallback)，创建了一个新的PhoneWindow。</p>
</li>
<li><p>此时的结构：</p>
<img src="/2020/07/24/View%E7%9A%84%E5%88%9B%E5%BB%BA%E6%B5%81%E7%A8%8B/%E7%AC%AC%E4%B8%80%E6%AC%A1%E7%BB%93%E6%9E%84.png" class title="第一次结构">
</li>
<li><p>mInstrumentation.callActivityOnCreate(activity, r.state, r.persistentState)，呼叫activity调用oncreate方法</p>
</li>
</ul>
<h2 id="PhoneWindow"><a href="#PhoneWindow" class="headerlink" title="PhoneWindow"></a>PhoneWindow</h2><ul>
<li><p>在OnCreate中调用setContentView方法加载布局文件</p>
</li>
<li><p>installDecor()，对decor进行一系列操作</p>
<ul>
<li><p>mDecor = generateDecor(-1)，初始化decor，return new DecorView(context, featureId, this, getAttributes())，返回一个DecorView。</p>
</li>
<li><p>mContentParent = generateLayout(mDecor)，PhoneWindow中的 mContentParent的初始化，其实就是一个DecorView。</p>
</li>
<li><p>generateLayout(mDecor)中，mDecor.onResourcesLoaded(mLayoutInflater, layoutResource)</p>
<ul>
<li>layoutResource就是传入的布局文件</li>
<li>mLayoutInflater为布局加载器</li>
<li>final View root = inflater.inflate(layoutResource, null)</li>
<li>addView(root, 0, new ViewGroup.LayoutParams(MATCH_PARENT, MATCH_PARENT))</li>
<li>通过前两步把布局文件加载为View并且添加进去</li>
</ul>
</li>
<li><p>此时状态：</p>
<img src="/2020/07/24/View%E7%9A%84%E5%88%9B%E5%BB%BA%E6%B5%81%E7%A8%8B/%E7%AC%AC%E4%BA%8C%E6%AC%A1%E7%BB%93%E6%9E%84.png" class title="第二次结构">
</li>
</ul>
</li>
<li><p>mLayoutInflater.inflate(layoutResID, mContentParent)</p>
<ul>
<li>final View temp = createViewFromTag(root, name, inflaterContext, attrs)<ul>
<li>view = createView(name, null, attrs)，在这个函数里通过反射调用有两个参数的构造函数进行初始化</li>
<li>view = mFactory2.onCreateView(parent, name, context, attrs)，Factory2是一个接口，提供OnCreateView方法，让开发者可以继承接口去实现View的初始化过程中的一些操作，只有当mFactory2不为null时，才会调用这个方法</li>
</ul>
</li>
<li>params = root.generateLayoutParams(attrs)，new一个LayoutParams对象</li>
<li>temp.setLayoutParams(params)，给加载出来的view设置LayoutParams</li>
</ul>
</li>
<li><p>此时状态：</p>
<img src="/2020/07/24/View%E7%9A%84%E5%88%9B%E5%BB%BA%E6%B5%81%E7%A8%8B/%E7%AC%AC%E4%B8%89%E6%AC%A1%E7%BB%93%E6%9E%84.png" class title="第三次结构">
</li>
<li><p>关于inflate的attachToRoot参数的解析</p>
<ul>
<li>这里用true,就表示解析出来的view参数用父view的并由系统完成addview</li>
<li>用false,就由用户自己去修改参数后，再手动addview添加到布局中，如果没有写addview，则xml中的属性都获取不到</li>
<li>addview里面有个异常能保证addView中执行一次，也就保证了view的体系结构永远是树型 ，每个节点只有一个父节点</li>
</ul>
</li>
</ul>
]]></content>
  </entry>
  <entry>
    <title>java虚拟机</title>
    <url>/2020/09/07/java%E8%99%9A%E6%8B%9F%E6%9C%BA/</url>
    <content><![CDATA[<h1 id="一、java虚拟机内存机制"><a href="#一、java虚拟机内存机制" class="headerlink" title="一、java虚拟机内存机制"></a>一、java虚拟机内存机制</h1><p>​    程序编译时，先将代码，即.java文件利用javac工具编译成.class文件，再送入JVM，经由类加载器把类信息送入运行时数据区的方法区，最后调用操作系统函数开始运行。</p>
<p>​    从这里可以得出结论，JVM的作用就是一个翻译官，把字节码文件翻译为机器语言。</p>
<img src="/2020/09/07/java%E8%99%9A%E6%8B%9F%E6%9C%BA/%E6%95%B4%E4%BD%93%E6%B5%81%E7%A8%8B.png" class title="整体流程">



<h2 id="1-JVM特性"><a href="#1-JVM特性" class="headerlink" title="1. JVM特性"></a>1. JVM特性</h2><ul>
<li><p>跨平台</p>
<p>跨平台体现在Java一份代码能在各种操作系统上运行，这是因为jdk有不同的版本，能处理不同的操作系统。</p>
</li>
<li><p>跨语言</p>
<p>​    跨语言体现在许多其它语言，如kotlin、groovy等都能在JVM上运行，原因是这些语言最终都被编译为字节码，JVM能够识别并处理。</p>
</li>
</ul>
<h2 id="2-运行时数据区"><a href="#2-运行时数据区" class="headerlink" title="2. 运行时数据区"></a>2. 运行时数据区</h2><p>​    运行时数据区内部也分为了五个部分，其中虚拟机栈，程序计数器，本地方法栈为线程私有，即每个线程单独都有一份，而堆和方法区为线程共享，所有线程都可以共享其中的数据。</p>
<img src="/2020/09/07/java%E8%99%9A%E6%8B%9F%E6%9C%BA/%E8%BF%90%E8%A1%8C%E6%97%B6%E6%95%B0%E6%8D%AE%E5%8C%BA.png" class title="运行时数据区">



<h2 id="3-每个部分详细"><a href="#3-每个部分详细" class="headerlink" title="3.每个部分详细"></a>3.每个部分详细</h2><h3 id="3-1-程序计数器"><a href="#3-1-程序计数器" class="headerlink" title="3.1 程序计数器"></a>3.1 程序计数器</h3><ul>
<li><p>为什么要有程序计数器？</p>
<p>​    由于 Java 是多线程语言，当执行的线程数量超过 CPU 核数时，线程之间会根据时间片轮询争夺 CPU 资源。如果一个线程的时间片用完了，或者是其它原因导致这个线程的 CPU 资源被提前抢夺，那么这个退出的线程就需要单独的一个程序计数器，来记录下一条运行的指令。</p>
<p>​    因为操作系统的时间片轮转机制，方法在运行时可能会因为上下文切换导致中止运行，这时候程序计数器需要记录方法中止时运行到的位置，存下当前字节码指令的地址，在再次开始运行时找到上一次中止运行时的位置。</p>
</li>
<li><p>程序计数器是JVM中唯一不会OOM的</p>
<p>​    因为程序计数器的内存非常小，而一般默认一个虚拟机栈是1M大小，一个虚拟机栈内程序计数器再多也无法达到1M。</p>
</li>
</ul>
<h3 id="3-2-虚拟机栈"><a href="#3-2-虚拟机栈" class="headerlink" title="3.2 虚拟机栈"></a>3.2 虚拟机栈</h3><p>​    虚拟机栈是用于管理一个线程当中的方法的，每有一个方法执行，就会把封装有这个方法信息的栈帧压入虚拟机栈，执行结束后就会把对应栈帧出栈。（就类似于虚拟机栈是一个弹夹，栈帧是子弹）</p>
<p>​    栈帧结构：</p>
<ul>
<li>局部变量表：用于存储八种基本变量类型</li>
<li>操作数栈：在函数执行过程中存放一些中间变量</li>
<li>动态链接：一般在java多态时，如一个接口的两个实现类，调用重写的同一个方法，用动态链接去区分调用了哪一个方法</li>
<li>完成出口：用于存储函数返回值。<strong>（正常返回：程序计数器指向的地址，异常返回：通过异常处理器表确定）</strong></li>
</ul>
<h3 id="3-3-本地方法栈"><a href="#3-3-本地方法栈" class="headerlink" title="3.3 本地方法栈"></a>3.3 本地方法栈</h3><p>​    虚拟机栈是管理java方法，而本地方法栈是管理native方法，当JVM调用到native方法时，不会将其打包成栈帧入虚拟机栈，而是利用动态链接将其交给操作系统执行。</p>
<p>​    <strong>HotSpot直接把本地方法栈和虚拟机栈合二为一</strong></p>
<h3 id="3-4-方法区"><a href="#3-4-方法区" class="headerlink" title="3.4 方法区"></a>3.4 方法区</h3><p>​    方法区存储<strong>常量、静态变量、类信息、即时编译期编译后的内容</strong>。</p>
<p>​    java1.8之后，运行时常量池中的<strong>字符串</strong>都被放入堆区。</p>
<p>​    Java1.8以前：永久代是方法区的一种实现。</p>
<p>​    java1.8以后：用元空间代替永久代。</p>
<ul>
<li><p>缺点：容易发生内存溢出，因为GC时回收率偏低，效率低。</p>
<p>移除永久代是为了融合 HotSpot JVM 与 JRockit VM 而做出的努力，因为 JRockit 没有永久代，所以不需要配置永久代。</p>
</li>
</ul>
<h3 id="3-5-堆"><a href="#3-5-堆" class="headerlink" title="3.5 堆"></a>3.5 堆</h3><p>​    java中的几乎全部对象，以及数组，都是分配在堆中的，他，他们的引用是存储在局部变量表中。</p>
<h2 id="4-直接内存（堆外内存）"><a href="#4-直接内存（堆外内存）" class="headerlink" title="4. 直接内存（堆外内存）"></a>4. 直接内存（堆外内存）</h2><p>​    除了JVM的栈和堆以外，还有一个不被JVM管理的内存区域，当使用了NIO时会用到，其大小不受虚拟机大小影响，但是受本机总内存大小限制。</p>
<h2 id="5-从底层深入理解运行时数据区"><a href="#5-从底层深入理解运行时数据区" class="headerlink" title="5.从底层深入理解运行时数据区"></a>5.从底层深入理解运行时数据区</h2><p>运行如下程序：</p>
<img src="/2020/09/07/java%E8%99%9A%E6%8B%9F%E6%9C%BA/code.png" class title="code">

<p>JVM处理过程如下：</p>
<ul>
<li>JVM 向操作系统申请内存，JVM 第一步就是通过配置参数或者默认配置参数向操作系统申请内存空间</li>
<li>JVM 获得内存空间后，会根据配置参数分配堆、栈以及方法区的内存大小</li>
<li>完成上一个步骤后， JVM 首先会执行构造器，编译器会在.java 文件被编译成.class 文件时，收集所有类的初始化代码，包括静态变量赋值语句、静态代码块、静态方法，静态变量和常量放入方法区</li>
<li>执行方法。启动 main 线程，执行 main 方法，开始执行第一行代码。此时堆内存中会创建一个 Teacher 对象，对象引用 student 就存放在栈中。执行其他方法时，具体的操作：栈帧执行对内存区域的影响</li>
</ul>
<img src="/2020/09/07/java%E8%99%9A%E6%8B%9F%E6%9C%BA/%E8%BF%90%E8%A1%8C%E8%BF%87%E7%A8%8B.png" class title="运行过程">



<p><strong>堆中的内存分配是连续的，且栈内存远小于堆内存。</strong></p>
<h2 id="6-内存溢出"><a href="#6-内存溢出" class="headerlink" title="6. 内存溢出"></a>6. 内存溢出</h2><h3 id="6-1-栈溢出"><a href="#6-1-栈溢出" class="headerlink" title="6.1 栈溢出"></a>6.1 栈溢出</h3><ul>
<li>stackOverFlow：当栈帧数量过多，加起来大于虚拟机栈的1M内存空间时，报stackOverFlow</li>
<li>outOfMemory：当线程数量过多，虚拟机栈数量就过多，加起来大于操作系统分配空间时，报OOM。</li>
</ul>
<h3 id="6-2-方法区溢出"><a href="#6-2-方法区溢出" class="headerlink" title="6.2 方法区溢出"></a>6.2 方法区溢出</h3><ul>
<li>运行时常量池溢出</li>
<li>方法区中保存的Class对象没有被及时回收掉或者Class信息占用的内存超过了我们配置。</li>
</ul>
<p>两者都为OOM</p>
<p><strong><em>注意Class要被回收，条件比较苛刻（仅仅是可以，不代表必然，因为还有一些参数可以进行控制）：</em></strong></p>
<ul>
<li><p>该类所有的实例都已经被回收，也就是堆中不存在该类的任何实例</p>
</li>
<li><p>加载该类的ClassLoader已经被回收</p>
</li>
<li><p>该类对应的java.lang.Class对象没有在任何地方被引用，无法在任何地方通过反射访问该类的方法</p>
</li>
</ul>
<h3 id="6-3-堆溢出"><a href="#6-3-堆溢出" class="headerlink" title="6.3 堆溢出"></a>6.3 堆溢出</h3><p>​    当创建对象大小大于堆内存空间，或者创建对象过多，且都为存活对象时，报OOM。</p>
<h3 id="6-4-本地内存溢出"><a href="#6-4-本地内存溢出" class="headerlink" title="6.4 本地内存溢出"></a>6.4 本地内存溢出</h3><p>​    使用NIO时使用大小超过本地内存，也为OOM。</p>
<h2 id="7-虚拟机优化技术"><a href="#7-虚拟机优化技术" class="headerlink" title="7. 虚拟机优化技术"></a>7. 虚拟机优化技术</h2><ul>
<li><p>编译优化——内联函数：将传入参数已经确定的函数直接拆开放入调用位置，避免真实的方法调用</p>
<img src="/2020/09/07/java%E8%99%9A%E6%8B%9F%E6%9C%BA/%E5%86%85%E8%81%94%E5%87%BD%E6%95%B0.png" class title="内联函数">
</li>
<li><p>栈优化——栈帧之间的数据共享：调用方的操作数栈和被调用方的局部变量表共享，减小操作空间的同时也减少了一些复制操作</p>
<img src="/2020/09/07/java%E8%99%9A%E6%8B%9F%E6%9C%BA/%E6%A0%88%E4%BC%98%E5%8C%96.png" class title="栈优化">

</li>
</ul>
<h1 id="二、虚拟机中的对象"><a href="#二、虚拟机中的对象" class="headerlink" title="二、虚拟机中的对象"></a>二、虚拟机中的对象</h1><h2 id="1-对象分配"><a href="#1-对象分配" class="headerlink" title="1. 对象分配"></a>1. 对象分配</h2><ul>
<li>首先通过类加载，传入JVM</li>
<li>JVM进行对象检查，检查对象是否已经被类加载器加载完毕，如果未加载完毕，就返回到类加载器处重新加载</li>
<li>检查通过后，就开始内存分配</li>
<li>内存空间初始化</li>
<li>设置（设置对象的对象头的一些参数）</li>
<li>对象初始化（构造函数执行）</li>
</ul>
<img src="/2020/09/07/java%E8%99%9A%E6%8B%9F%E6%9C%BA/%E5%AF%B9%E8%B1%A1%E5%88%9B%E5%BB%BA.png" class title="对象创建">



<h3 id="1-1-对象的内存布局"><a href="#1-1-对象的内存布局" class="headerlink" title="1.1 对象的内存布局"></a>1.1 对象的内存布局</h3><ul>
<li>对象头：对象头包含了<strong>Mark Word 、类型指针、对象数组长度</strong>这三个部分。（其中数组长度只有在是数组对象时才会存在）</li>
<li>实例数据</li>
<li>对齐填充：作用为将对象大小补齐为字节的倍数，如对象头和实例数据加起来为30位，就需要对齐填充2位</li>
</ul>
<img src="/2020/09/07/java%E8%99%9A%E6%8B%9F%E6%9C%BA/%E5%AF%B9%E8%B1%A1%E5%86%85%E5%AD%98%E5%B8%83%E5%B1%80.png" class title="对象内存布局">



<h3 id="1-2-内存分配"><a href="#1-2-内存分配" class="headerlink" title="1.2 内存分配"></a>1.2 内存分配</h3><ul>
<li><p>划分内存的方法：如堆中内存空间整齐，采用指针碰撞（指针依次向后移动），如不整齐，采用空闲列表，记录每个位置是否已经存放对象</p>
</li>
<li><p>并发安全：<strong>CAS机制</strong>，如果有线程在访问，就自旋等待，再次访问时如访问位置不为空闲，就去访问下一个位置。或者采用<strong>本地线程缓存（TLAB）</strong>，在Eden区中划分一个内存大小为Eden百分之一的TLAB区，把每个线程对应的对象数据放入TLAB区，类似于ThreadLocal</p>
</li>
<li><p>堆中内存区域：</p>
<ul>
<li>新生代：占堆的三分之一大小，又细分为Eden，From，To三个区，内存比例为8：1：1，因为经过大数据调查显示，对象进入Eden区第一次GC，百分之九十都会被回收，而为了避免From或者To内存溢出，只有一半是有效内存</li>
<li>老年代：占堆的三分之二大小</li>
<li>新生代和老年代有着各自的垃圾回收器，新生代为Minor GC，老年代为Major GC，或者全局的Full GC</li>
</ul>
</li>
<li><p>具体内存分配过程：</p>
<img src="/2020/09/07/java%E8%99%9A%E6%8B%9F%E6%9C%BA/%E5%86%85%E5%AD%98%E5%88%86%E9%85%8D.png" class title="内存分配">

<p>(1)优先进行逃逸分析，判断对象是否可以在栈中分配（如对象只存在于一个方法体中，没有其他方法调用，没有其他线程调用，就可以分配在栈中，因为对象会随着栈帧的出栈而回收）。</p>
<p>(2)无法分配到栈中的对象就送入堆中，如果为大对象，直接存入老年代,如果不为大对象，但是采用了本地线程缓存，就存入TLAB区，如果都不满足，就存入Eden区的其余位置。</p>
<p>(3)放入Eden区中的对象，在GC时，没有被回收的对象将会放入From区，再次GC还未被回收的对象就送入To区，一直来回存放，目的是避免From区和To区内存溢出，每进行一次GC，就把未回收的对象放入空的区中，将不为空的区全部回收，下一次继续进行这样的操作。存活的对象每进行一次位置转移，就把对象头中的age信息加一，age为四位，最大为15，所以在15次GC后要是对象还存活，就放入老年代。但是有一个例外，JVM为了防止From区和To区内存不足，如果GC时几个对象加起来大小很大，就直接放入老年代，无需等到age达到15，这个称为<strong>对象动态年龄判定</strong>。</p>
<p>(4)为了保证老年代内存空间不溢出，JVM会在每一次有对象进入老年代时进行一次老年代GC，但是这样做很消耗内存，所以有一个<strong>空间分配担保</strong>，对象放入老年代时先不进行垃圾回收，如果老年代内存不够用了，再进行垃圾回收。还有一种<strong>空间分配担保</strong>，就是当From或者To区放不下时，让对象直接进入老年区。</p>
</li>
<li><p>内存分配的规则</p>
<ul>
<li>对象优先在Eden区分配</li>
<li>大对象直接分配到老年代</li>
<li>空间分配担保</li>
<li>长期存活对象进入老年代</li>
<li>对象年龄动态判定</li>
</ul>
</li>
<li><p>优化： 本地线程缓冲（TLAB）</p>
</li>
</ul>
<h3 id="1-3-内存空间初始化"><a href="#1-3-内存空间初始化" class="headerlink" title="1.3 内存空间初始化"></a>1.3 内存空间初始化</h3><p>​    内存分配完成后，虚拟机需要将分配到的内存空间都初始化为零值(如int值为0，boolean值为false等等)。这一步操作保证了对象的实例字段在Java代码中可以不赋初始值就直接使用，程序能访问到这些字段的数据类型所对应的零值。</p>
<h2 id="2-对象访问定位"><a href="#2-对象访问定位" class="headerlink" title="2. 对象访问定位"></a>2. 对象访问定位</h2><ul>
<li><p>句柄访问：每个对象的引用持有一个句柄，句柄中存放了到对象实例数据、对象类型数据的指针</p>
</li>
<li><p>直接指针访问：每个对象的的引用直接持有对象实例数据，还有对象类型指针</p>
</li>
<li><p>对比：</p>
<p>优点：句柄访问可以在对象实例位置发生改变时不用重新分配引用的指针，直接指针访问可以只进行一次指针访问，速度快，效率高。</p>
<p>缺点：句柄访问要进行两次指针访问，效率低，速度慢，直接指针访问在对象实例位置发生改变时需要重新定位。</p>
</li>
</ul>
<img src="/2020/09/07/java%E8%99%9A%E6%8B%9F%E6%9C%BA/%E5%AF%B9%E8%B1%A1%E8%AE%BF%E9%97%AE%E5%AE%9A%E4%BD%8D.png" class title="对象访问定位">



<h2 id="3-对象存活判断"><a href="#3-对象存活判断" class="headerlink" title="3. 对象存活判断"></a>3. 对象存活判断</h2><ul>
<li>引用计数算法（无法解决对象互相引用的问题）：JVM维护一个变量，每创建一个对象就加一，回收一个就减一，直到为0就是全部回收完毕</li>
<li>可达性分析：JVM将一些特别的对象（静态变量、常量池、虚拟机栈中引用的对象、JNI指针等等），定义为GC Root，当持有引用时就建立了关联，只要对象到GC Root之间有一条有关联的道路，就判断为对象存活</li>
<li>但是不是经历了可达性分析不存活的对象就一定会被回收，在第一次可达性分析要被回收后，会进行一次筛选，如果类内部重写了finalized方法，就会执行一次此方法，可以在这个方法中自行救活对象，但是第二次回收就会直接回收。<strong>注意：finalized方法优先级很低，所以不建议使用</strong></li>
</ul>
<img src="/2020/09/07/java%E8%99%9A%E6%8B%9F%E6%9C%BA/%E5%AF%B9%E8%B1%A1%E5%AD%98%E6%B4%BB.png" class title="对象存活">



<h2 id="4-各种引用"><a href="#4-各种引用" class="headerlink" title="4. 各种引用"></a>4. 各种引用</h2><ul>
<li><p>强引用：用 = 创建的对象，无论如何都不会被回收</p>
</li>
<li><p>软引用（softReference）：在JVM发生OOM时GC才会被回收</p>
<img src="/2020/09/07/java%E8%99%9A%E6%8B%9F%E6%9C%BA/%E8%BD%AF%E5%BC%95%E7%94%A8-1.png" class title="软引用-1">

<img src="/2020/09/07/java%E8%99%9A%E6%8B%9F%E6%9C%BA/%E8%BD%AF%E5%BC%95%E7%94%A8-2.png" class title="软引用-2">
</li>
<li><p>弱引用（WeakReference）：在GC时就会回收</p>
<img src="/2020/09/07/java%E8%99%9A%E6%8B%9F%E6%9C%BA/%E5%BC%B1%E5%BC%95%E7%94%A8-1.png" class title="弱引用-1">

<img src="/2020/09/07/java%E8%99%9A%E6%8B%9F%E6%9C%BA/%E5%BC%B1%E5%BC%95%E7%94%A8-2.png" class title="弱引用-2">
</li>
<li><p>虚引用：随时都会被回收，唯一作用就是监视垃圾回收器是否正常运行</p>
</li>
<li><p><strong><em>注意：</em></strong>软引用和弱引用，可以用在内存资源紧张的情况下以及创建不是很重要的数据缓存。当系统内存不足的时候，缓存中的内容是可以被释放（比如图片缓存）。实际运用（WeakHashMap、ThreadLocal）</p>
</li>
</ul>
<h1 id="三、-垃圾回收器"><a href="#三、-垃圾回收器" class="headerlink" title="三、 垃圾回收器"></a>三、 垃圾回收器</h1><h2 id="1-垃圾回收算法"><a href="#1-垃圾回收算法" class="headerlink" title="1.垃圾回收算法"></a>1.垃圾回收算法</h2><h3 id="1-1-复制算法（Copying）"><a href="#1-1-复制算法（Copying）" class="headerlink" title="1.1 复制算法（Copying）"></a>1.1 复制算法（Copying）</h3><p>​    将可用内存按容量划分为大小相等的两块，每次只使用其中的一块。当这一块的内存用完了，就将还存活着的对象复制到另外一块上面，然后再把已使用过的内存空间一次清理掉。这样使得每次都是对整个半区进行内存回收，内存分配时也就不用考虑内存碎片等复杂情况，只要按顺序分配内存即可，实现简单，运行高效。只是这种算法的代价是将内存缩小为了原来的一半。</p>
<p>​    专门研究表明，新生代中的对象98%是“朝生夕死”的，所以并不需要按照1:1的比例来划分内存空间，而是将内存分为一块较大的Eden空间和两块较小的Survivor空间，每次使用Eden和其中一块Survivor[1]。当回收时，将Eden和Survivor中还存活着的对象一次性地复制到另外一块Survivor空间上，最后清理掉Eden和刚才用过的Survivor空间。（分代收集理论）</p>
<p>​    HotSpot虚拟机默认Eden和Survivor的大小比例是8:1，也就是每次新生代中可用内存空间为整个新生代容量的90%（80%+10%），只有10%的内存会被“浪费”。当然，98%的对象可回收只是一般场景下的数据，我们没有办法保证每次回收都只有不多于10%的对象存活，当Survivor空间不够用时，需要依赖其他内存（这里指老年代）进行分配担保（Handle Promotion）</p>
<h3 id="1-2-标记-清除算法（Mark-Sweep）"><a href="#1-2-标记-清除算法（Mark-Sweep）" class="headerlink" title="1.2 标记-清除算法（Mark-Sweep）"></a>1.2 标记-清除算法（Mark-Sweep）</h3><p>​    算法分为“标记”和“清除”两个阶段：首先标记出所有需要回收的对象，在标记完成后统一回收所有被标记的对象。它的主要不足空间问题，标记清除之后会产生大量不连续的内存碎片，空间碎片太多可能会导致以后在程序运行过程中需要分配较大对象时，无法找到足够的连续内存而不得不提前触发另一次垃圾收集动作。</p>
<h3 id="1-3-标记-整理算法（Mark-Compact）"><a href="#1-3-标记-整理算法（Mark-Compact）" class="headerlink" title="1.3 标记-整理算法（Mark-Compact）"></a>1.3 标记-整理算法（Mark-Compact）</h3><p>​    首先标记出所有需要回收的对象，在标记完成后，后续步骤不是直接对可回收对象进行清理，而是让所有存活的对象都向一端移动，然后直接清理掉端边界以外的内存。标记整理算法虽然没有内存碎片，但是效率偏低。</p>
<h2 id="2-JVM中常见的垃圾收集器"><a href="#2-JVM中常见的垃圾收集器" class="headerlink" title="2. JVM中常见的垃圾收集器"></a>2. JVM中常见的垃圾收集器</h2><h3 id="2-1-分代收集的思想"><a href="#2-1-分代收集的思想" class="headerlink" title="2.1 分代收集的思想"></a>2.1 分代收集的思想</h3><p>​    在新生代中，每次垃圾收集时都发现有大批对象死去，只有少量存活，那就选用复制算法，只需要付出少量存活对象的复制成本就可以完成收集。</p>
<p>​    而老年代中因为对象存活率高、没有额外空间对它进行分配担保，就必须使用“标记—清理”或者“标记—整理”算法来进行回收。</p>
<p>​    <strong>请记住下图的垃圾收集器和之间的连线关系。</strong></p>




<table>
<thead>
<tr>
<th align="center"><strong><em>收集器</em></strong></th>
<th><strong><em>收集对象和算法</em></strong></th>
<th><strong><em>收集器类型</em></strong></th>
<th><strong><em>说明</em></strong></th>
<th><strong><em>适用场景</em></strong></th>
</tr>
</thead>
<tbody><tr>
<td align="center">Serial</td>
<td>新生代，复制算法</td>
<td>单线程</td>
<td></td>
<td>简单高效；适合内存不大的情况；</td>
</tr>
<tr>
<td align="center">ParNew</td>
<td>新生代，复制算法</td>
<td>并行的多线程收集器</td>
<td>ParNew垃圾收集器是Serial收集器的多线程版本</td>
<td>搭配CMS垃圾回收器的首选</td>
</tr>
<tr>
<td align="center">Parallel Scavenge吞吐量优先收集器</td>
<td>新生代，复制算法</td>
<td>并行的多线程收集器</td>
<td>类似ParNew，更加关注吞吐量，达到一个可控制的吞吐量；</td>
<td>本身是Server级别多CPU机器上的默认GC方式，主要适合后台运算不需要太多交互的任务；</td>
</tr>
</tbody></table>
<table>
<thead>
<tr>
<th><strong><em>收集器</em></strong></th>
<th><strong><em>收集对象和算法</em></strong></th>
<th><strong><em>收集器类型</em></strong></th>
<th><strong><em>说明</em></strong></th>
<th><strong><em>适用场景</em></strong></th>
</tr>
</thead>
<tbody><tr>
<td>Serial Old</td>
<td>老年代，标记整理算法</td>
<td>单线程</td>
<td></td>
<td>Client模式下虚拟机使用</td>
</tr>
<tr>
<td>Parallel Old</td>
<td>老年代，标记整理算法</td>
<td>并行的多线程收集器</td>
<td>Parallel Scavenge收集器的老年代版本，为了配合Parallel Scavenge的面向吞吐量的特性而开发的对应组合；</td>
<td>在注重吞吐量以及CPU资源敏感的场合采用</td>
</tr>
<tr>
<td>CMS</td>
<td>老年代，标记清除算法</td>
<td>并行与并发收集器</td>
<td>尽可能的缩短垃圾收集时用户线程停止时间；缺点在于： 1.内存碎片 2.需要更多cpu资源 3.浮动垃圾问题，需要更大的堆空间</td>
<td>重视服务的响应速度、系统停顿时间和用户体验的互联网网站或者B/S系统。互联网后端目前cms是主流的垃圾回收器；</td>
</tr>
<tr>
<td>G1</td>
<td>跨新生代和老年代；标记整理 + 化整为零</td>
<td>并行与并发收集器</td>
<td>JDK1.7才正式引入，采用分区回收的思维，基本不牺牲吞吐量的前提下完成低停顿的内存回收；可预测的停顿是其最大的优势；</td>
<td>面向服务端应用的垃圾回收器，目标为取代CMS</td>
</tr>
</tbody></table>
<p>并行：垃圾收集的多线程的同时进行。</p>
<p>并发：垃圾收集的多线程和应用的多线程同时进行。</p>
<p>注：吞吐量=运行用户代码时间/(运行用户代码时间+ 垃圾收集时间)</p>
<p>垃圾收集时间= 垃圾回收频率 * 单次垃圾回收时间</p>
<h3 id="2-2-垃圾回收器工作示意图"><a href="#2-2-垃圾回收器工作示意图" class="headerlink" title="2.2 垃圾回收器工作示意图"></a>2.2 垃圾回收器工作示意图</h3><ul>
<li><p>Serial/Serial Old</p>
<p>最古老的，单线程，独占式，成熟，适合单CPU  服务器</p>
<p>-XX:+UseSerialGC 新生代和老年代都用串行收集器</p>
<p>-XX:+UseParNewGC 新生代使用ParNew，老年代使用Serial Old</p>
<p>-XX:+UseParallelGC 新生代使用ParallerGC，老年代使用Serial Old</p>
</li>
<li><p>ParNew</p>
<p>和Serial基本没区别，唯一的区别：多线程，多CPU的，停顿时间比Serial少</p>
<p>-XX:+UseParNewGC 新生代使用ParNew，老年代使用Serial Old</p>
</li>
<li><p>Parallel Scavenge（ParallerGC）/Parallel Old</p>
<p>​    关注吞吐量的垃圾收集器，高吞吐量则可以高效率地利用CPU时间，尽快完成程序的运算任务，主要适合在后台运算而不需要太多交互的任务。</p>
<p>​    所谓吞吐量就是CPU用于运行用户代码的时间与CPU总消耗时间的比值，即吞吐量=运行用户代码时间/（运行用户代码时间+垃圾收集时间），虚拟机总共运行了100分钟，其中垃圾收集花掉1分钟，那吞吐量就是99%。</p>
</li>
<li><p>Concurrent Mark Sweep （CMS）</p>
 

<p>​    CMS收集器是一种以获取<strong>最短回收停顿时间</strong>为目标的收集器。目前很大一部分的Java应用集中在互联网站或者B/S系统的服务端上，这类应用尤其重视服务的响应速度，希望系统停顿时间最短，以给用户带来较好的体验。CMS收集器就非常符合这类应用的需求。</p>
<p>​    从名字（包含“Mark Sweep”）上就可以看出，CMS收集器是基于<strong>“标记—清除”算法</strong>实现的，它的运作过程相对于前面几种收集器来说更复杂一些，整个过程分为4个步骤，包括：</p>
<p><strong>l</strong> <strong><em>初始标记</em></strong>——短暂，仅仅只是标记一下GC Roots能直接关联到的对象，速度很快</p>
<p><strong>l</strong> <strong><em>并发标记</em></strong>——长，和用户的应用程序同时进行，进行GC Roots追踪的过程</p>
<p><strong>l</strong> <strong><em>重新标记</em></strong>——短暂，为了修正并发标记期间因用户程序继续运作而导致标记产生变动的那一部分对象的标记记录，这个阶段的停顿时间一般会比初始标记阶段稍长一些，但远比并发标记的时间短</p>
<p><strong>l</strong> <strong><em>并发清除</em></strong>——长，清除掉标记的对象</p>
<p><strong>l 优点：</strong></p>
<p>​    由于整个过程中耗时最长的并发标记和并发清除过程收集器线程都可以与用户线程一起工作，所以，从总体上来说，CMS收集器的内存回收过程是与用户线程一起并发执行的，用户体验好。</p>
<p>-XX:+UseConcMarkSweepGC ，表示新生代使用ParNew，老年代的用CMS</p>
<p><strong>l 缺点：</strong></p>
<ul>
<li><p>浮动垃圾</p>
<p>​    由于CMS并发清理阶段用户线程还在运行着，伴随程序运行自然就还会有新的垃圾不断产生，这一部分垃圾出现在标记过程之后，CMS无法在当次收集中处理掉它们，只好留待下一次GC时再清理掉。这一部分垃圾就称为“浮动垃圾”。</p>
<p>​    为了防止浮动垃圾导致OOM，每次在老年代内存被占了80%时就会进行GC，如果还是OOM了，就让Serial Old 进行处理，这是STW会特别多，影响用户体验。</p>
</li>
<li><p>内存碎片</p>
<p>因为CMS采用的是标记——清除算法，所以会存在内存碎片，导致GC提前进行。</p>
</li>
<li><p>CPU敏感：CPU小于4时影响性能</p>
</li>
</ul>
</li>
<li><p>G1</p>
  

<p>-XX:+UseG1GC</p>
<ul>
<li><p>并发标记周期</p>
<ul>
<li><p>初始标记</p>
<p>​    短暂，仅仅只是标记一下GC Roots能直接关联到的对象，速度很快，产生一个全局停顿，都伴随有一次新生代的GC。</p>
</li>
<li><p>根区域扫描</p>
<p>扫描survivor区可以直接到达的老年代区域。</p>
</li>
<li><p>并发标记阶段</p>
<p>扫描和查找整个堆的存活对象，并标记。</p>
</li>
<li><p>重新标记</p>
<p>会产生全局停顿，对并发标记阶段的结果进行修正。</p>
</li>
<li><p>独占清理</p>
<p>会产生全局停顿，对GC回收比例进行排序，供混合收集阶段使用。</p>
</li>
<li><p>并发清理</p>
<p>识别并清理完全空闲的区域，并发进行。</p>
</li>
</ul>
</li>
<li><p>并行与并发</p>
<p>​    G1能充分利用多CPU、多核环境下的硬件优势，使用多个CPU（CPU或者CPU核心）来缩短Stop-The-World停顿的时间，部分其他收集器原本需要停顿Java线程执行的GC动作，G1收集器仍然可以通过并发的方式让Java程序继续执行。</p>
</li>
<li><p>分代收集</p>
<p>​    与其他收集器一样，分代概念在G1中依然得以保留。虽然G1可以不需要其他收集器配合就能独立管理整个GC堆，但它能够采用不同的方式去处理新创建的对象和已经存活了一段时间、熬过多次GC的旧对象以获取更好的收集效果。</p>

</li>
<li><p>空间整合</p>
<p>​    与CMS的“标记—清理”算法不同，G1从整体来看是基于“标记—整理”算法实现的收集器，从局部（两个Region之间）上来看是基于“复制”算法实现的，但无论如何，这两种算法都意味着G1运作期间不会产生内存空间碎片，收集后能提供规整的可用内存。这种特性有利于程序长时间运行，分配大对象时不会因为无法找到连续内存空间而提前触发下一次GC。</p>
</li>
<li><p>内存布局</p>
<p>​    在G1之前的其他收集器进行收集的范围都是整个新生代或者老年代，而G1不再是这样。使用G1收集器时，Java堆的内存布局就与其他收集器有很大差别，它将整个Java堆划分为多个大小相等的独立区域（Region），虽然还保留有新生代和老年代的概念，但新生代和老年代不再是物理隔离的了，它们都是一部分Region（不需要连续）的集合。</p>
</li>
<li><p>混合收集</p>
<ul>
<li><p>对含有垃圾比例较高的Region进行回收。</p>
</li>
<li><p>G1当出现内存不足的的情况，也可能进行的FullGC回收。</p>
</li>
<li><p>G1中重要的参数：</p>
<p>-XX:MaxGCPauseMillis 指定目标的最大停顿时间，G1尝试调整新生代和老年代的比例，堆大小，晋升年龄来达到这个目标时间。</p>
<p>-XX:ParallerGCThreads：设置GC的工作线程数量</p>
</li>
</ul>
</li>
</ul>
</li>
</ul>
<h2 id="3-Stop-The-World现象"><a href="#3-Stop-The-World现象" class="headerlink" title="3. Stop The World现象"></a>3. Stop The World现象</h2><p>STW就是暂停用户线程。</p>
<p>GC收集器和我们GC调优的目标就是尽可能的减少STW的时间和次数。</p>
<h2 id="4-未来的垃圾回收"><a href="#4-未来的垃圾回收" class="headerlink" title="4. 未来的垃圾回收"></a>4. 未来的垃圾回收</h2><p>​    ZGC通过技术手段把stw的情况控制在仅有一次，就是第一次的初始标记才会发生，这样也就不难理解为什么GC停顿时间不随着堆增大而上升了，再大我也是通过并发的时间去回收了。</p>
<p>​    关键技术：</p>
<p>​    <strong>1.</strong> <strong><em>有色指针（Colored Pointers）</em></strong></p>
<p>​    <strong>2.</strong> <strong><em>加载屏障（Load Barrier）</em></strong></p>
<h2 id="5-常量池与String"><a href="#5-常量池与String" class="headerlink" title="5. 常量池与String"></a>5. 常量池与String</h2><p>​    常量池有很多概念，包括运行时常量池、class常量池、字符串常量池。</p>
<p>​    虚拟机规范只规定以上区域属于方法区，并没有规定虚拟机厂商的实现。</p>
<p>​    严格来说是静态常量池和运行时常量池，静态常量池是存放字符串字面量、符号引用以及类和方法的信息，而运行时常量池存放的是运行时一些直接引用。<br>​    运行时常量池是在类加载完成之后，将静态常量池中的符号引用值转存到运行时常量池中，类在解析之后，将符号引用替换成直接引用。<br>​    这两个常量池在JDK1.7版本之后，就移到堆内存中了，这里指的是物理空间，而逻辑上还是属于方法区（方法区是逻辑分区）。</p>
<ul>
<li><p>字面量</p>
<p>给基本类型变量赋值的方式就叫做字面量或者字面值。</p>
<p>比如：int i=120; long j=10L;</p>
</li>
<li><p>符号引用</p>
<p>​    包括类和方法的全限定名（例如 String 这个类，它的全限定名就是 Java/lang/String）、字段的名称和描述符以及方法的名称和描述符。</p>
</li>
<li><p>直接引用</p>
<p>具体对象的索引值。</p>
</li>
<li><p>String的初始赋值</p>
 

<pre><code>而如果调用了insern方法，就是直接在字符串常量池中找字符串，如果没有再创建，返回的是字符串常量池的引用。</code></pre></li>
</ul>
<h1 id="四、类加载"><a href="#四、类加载" class="headerlink" title="四、类加载"></a>四、类加载</h1><h2 id="1-ART和Dalvik"><a href="#1-ART和Dalvik" class="headerlink" title="1. ART和Dalvik"></a>1. ART和Dalvik</h2><p>​    Android应用程序运行在Dalvik/ART虚拟机，并且每一个应用程序对应有一个单独的Dalvik虚拟机实例。Dalvik虚拟机实则也算是一个Java虚拟机，只不过它执行的不是class文件，而是dex文件。</p>
<p>​    Dalvik虚拟机与Java虚拟机共享有差不多的特性，差别在于两者执行的指令集是不一样的，前者的指令集是基本寄存器的，而后者的指令集是基于堆栈的。</p>




<ul>
<li><p>基于寄存器的虚拟机：</p>
<p>​    相当于把基于堆栈的虚拟机的局部变量表和操作数栈合并为了一个虚拟寄存器，模仿CPU寄存器操作，将指令运行步骤大大减少。</p>
</li>
</ul>
<p>​    （1）Dalvik虚拟机执行的是dex字节码，是解释执行，在Android2.2后，加入了JIT（即时编译技术），在运行时可以选择经常执行的代码进行编译优化，在本次程序执行完毕后会清除记录。</p>
<p>​    （2）在Android5.0后，默认使用ART虚拟机，直接执行本地机器码，而把dex字节码编译成机器码的东西就是AOT（预先编译技术），在程序安装时进行预编译，将应用直接编译为机器码，但是这样的弊端就是安装会非常慢。</p>
<p>​    （3）在安装时，dalvik将.dex文件优化为.odex文件，ART将.dex文件优化为机器码文件。</p>
<p>​    （4）Android N ：混合编译，在第一次安装时不执行AOT（安装速度变快），在运行的同时进行JIT，将经常运行的代码进行编译，并保存到Profile文件下。在手机充电或者闲置时，通过AOT技术将Profile文件下的代码进行编译，保存机器码，下次运行时直接使用。</p>
<h2 id="2-Android类加载器"><a href="#2-Android类加载器" class="headerlink" title="2. Android类加载器"></a>2. Android类加载器</h2>

<p>全局getClassLoader获取到的就是PathClassLoader</p>
<p>​    类加载过程：</p>
<ul>
<li><p>loadClass</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">protected</span> Class&lt;?&gt; loadClass(String name, <span class="keyword">boolean</span> resolve)                         <span class="keyword">throws</span> ClassNotFoundException</span><br><span class="line">    &#123;</span><br><span class="line">    Class&lt;?&gt; c = findLoadedClass(name);<span class="comment">//寻找是否已经加载</span></span><br><span class="line">    <span class="keyword">if</span>(c == <span class="keyword">null</span>)&#123;</span><br><span class="line">        <span class="comment">//如果没有加载，首先找父加载器，这里的父加载器是BootClassLoader</span></span><br><span class="line">        c = parent.loadClass(name, <span class="keyword">false</span>);</span><br><span class="line">        <span class="keyword">if</span> (c == <span class="keyword">null</span>) &#123;</span><br><span class="line">        	<span class="comment">//如果还是未加载，就使用自己的类加载器进行加载</span></span><br><span class="line">        	c = findClass(name);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>这里采用的是双亲委派模型，先让父加载器加载，再让自己加载。</p>
<p>原因：</p>
<ul>
<li>安全性：无法恶意篡改系统已有的类，因为会被BootClassLoader直接加载，加载的是系统层的类</li>
<li>避免重复加载</li>
</ul>
</li>
<li><p>findLoadedClass 调用的是VMClassLoader类里的方法，直接是native层</p>
</li>
<li><p>parent.loadClass</p>
<p>调用到BaseDexClassLoader的findClass方法。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">protected</span> Class&lt;?&gt; findClass(String name) <span class="keyword">throws</span> ClassNotFoundException&#123;</span><br><span class="line">    Class c = pathList.findClass(name, suppressedExceptions);</span><br><span class="line">    <span class="keyword">return</span> c;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>而pathList是一个DexPathList对象。</p>
<p>在pathList中又有一个element数组用于存放多个dex文件。</p>
<p>最后遍历编译。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"> <span class="keyword">private</span> Element[] dexElements;</span><br><span class="line"><span class="keyword">public</span> Class&lt;?&gt; findClass(String name, List&lt;Throwable&gt; suppressed) &#123;</span><br><span class="line">     <span class="keyword">for</span> (Element element : dexElements) &#123;</span><br><span class="line">            Class&lt;?&gt; clazz = element.findClass(name, definingContext, suppressed);</span><br><span class="line">            <span class="keyword">if</span> (clazz != <span class="keyword">null</span>) &#123;</span><br><span class="line">                <span class="keyword">return</span> clazz;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> Class&lt;?&gt; findClass(String name, ClassLoader definingContext,</span><br><span class="line">                List&lt;Throwable&gt; suppressed) &#123;</span><br><span class="line">            <span class="keyword">return</span> dexFile != <span class="keyword">null</span> ? dexFile.loadClassBinaryName(name, definingContext, suppressed)</span><br><span class="line">                    : <span class="keyword">null</span>;</span><br><span class="line">        &#125;</span><br></pre></td></tr></table></figure>

<p>直接loadClassBinaryName把dex文件编译加载好。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">private</span> <span class="keyword">static</span> Element[] makeDexElements(List&lt;File&gt; files, File optimizedDirectory,</span><br><span class="line">           List&lt;IOException&gt; suppressedExceptions, ClassLoader loader, <span class="keyword">boolean</span> isTrusted)</span><br></pre></td></tr></table></figure>

<p>通过这个方法把element数组初始化，在热修复时也可以通过这个方法，把dex文件构造成element数组，放在pathList中dexElements的最前面。</p>
</li>
</ul>
  

<ul>
<li><p>findClass</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">protected</span> Class&lt;?&gt; findClass(String name) <span class="keyword">throws</span> ClassNotFoundException &#123;</span><br><span class="line">       <span class="keyword">return</span> Class.classForName(name, <span class="keyword">false</span>, <span class="keyword">null</span>);</span><br><span class="line">   &#125;</span><br></pre></td></tr></table></figure>

<p>直接寻找类名并加载。</p>
</li>
</ul>
]]></content>
  </entry>
  <entry>
    <title>插件化换肤</title>
    <url>/2020/07/26/%E6%8F%92%E4%BB%B6%E5%8C%96%E6%8D%A2%E8%82%A4/</url>
    <content><![CDATA[<h1 id="插件化换肤"><a href="#插件化换肤" class="headerlink" title="插件化换肤"></a>插件化换肤</h1>]]></content>
  </entry>
  <entry>
    <title>数据结构与算法</title>
    <url>/2020/10/09/%E6%95%B0%E6%8D%AE%E7%BB%93%E6%9E%84%E4%B8%8E%E7%AE%97%E6%B3%95/</url>
    <content><![CDATA[<h1 id="一、概论"><a href="#一、概论" class="headerlink" title="一、概论"></a>一、概论</h1><p>数据结构分为两个部分，逻辑结构与物理结构。</p>
<ul>
<li>逻辑结构：指数据对象间的关系，类似于集合、树形结构、线性结构、图形结构</li>
<li>物理结构：在计算机中存储的方式，类似于顺序存储、链式存储</li>
</ul>
<h1 id="二、线性表"><a href="#二、线性表" class="headerlink" title="二、线性表"></a>二、线性表</h1><h2 id="1、-顺序表（顺序存储）"><a href="#1、-顺序表（顺序存储）" class="headerlink" title="1、 顺序表（顺序存储）"></a>1、 顺序表（顺序存储）</h2><h3 id="1-1-常用的数组"><a href="#1-1-常用的数组" class="headerlink" title="1.1 常用的数组"></a>1.1 常用的数组</h3><ul>
<li>使用起来简单</li>
<li>修改和查询时时间效率高</li>
<li>占据连续空间：初始化数组时需要指定数组大小，分配一段连续空间</li>
<li>空间效率低：会存在很多空间没有使用的情况</li>
<li>操作麻烦：在增加、删除时会移动大量数据</li>
</ul>
<h3 id="1-2-ArrayList扩容机制"><a href="#1-2-ArrayList扩容机制" class="headerlink" title="1.2 ArrayList扩容机制"></a>1.2 ArrayList扩容机制</h3><p>如果其大小超过了内部数组大小，就进行判断：</p>
<ul>
<li><p>ArrayList大小 &lt;= 12 </p>
<p>就把数组扩大12</p>
</li>
<li><p>ArrayList大小 &gt; 12 </p>
<p>就把数组扩大为原来的1.5倍</p>
</li>
</ul>
<h3 id="1-3-源码解析"><a href="#1-3-源码解析" class="headerlink" title="1.3 源码解析"></a>1.3 源码解析</h3><ul>
<li><p>继承关系</p>
<img src="/2020/10/09/%E6%95%B0%E6%8D%AE%E7%BB%93%E6%9E%84%E4%B8%8E%E7%AE%97%E6%B3%95/%E9%A1%BA%E5%BA%8F%E8%A1%A8%E7%BB%A7%E6%89%BF%E5%85%B3%E7%B3%BB.png" class title="顺序表继承关系">
</li>
<li><p>迭代器</p>
<p>可以通过迭代器顺序删除顺序表中的元素，避免使用for循环，造成很多数据的移动。</p>
<ul>
<li>hasNext()：判断是否还有下一个数据</li>
<li>next()：获取下一个节点的数据</li>
<li>remove()：删除最近访问的节点，必须和next()一起使用，在next()之后使用</li>
</ul>
</li>
</ul>
<h2 id="2、链表（链式存储）"><a href="#2、链表（链式存储）" class="headerlink" title="2、链表（链式存储）"></a>2、链表（链式存储）</h2><h3 id="2-1-双向链表LinkedList"><a href="#2-1-双向链表LinkedList" class="headerlink" title="2.1 双向链表LinkedList"></a>2.1 双向链表LinkedList</h3><ul>
<li><p>继承关系</p>
<img src="/2020/10/09/%E6%95%B0%E6%8D%AE%E7%BB%93%E6%9E%84%E4%B8%8E%E7%AE%97%E6%B3%95/%E5%8F%8C%E9%93%BE%E8%A1%A8%E7%BB%A7%E6%89%BF%E5%85%B3%E7%B3%BB.png" class title="双链表继承关系">
</li>
<li><p>同样也可以利用迭代器进行顺序移除</p>
</li>
</ul>
<h3 id="2-2-线性表对比"><a href="#2-2-线性表对比" class="headerlink" title="2.2 线性表对比"></a>2.2 线性表对比</h3><img src="/2020/10/09/%E6%95%B0%E6%8D%AE%E7%BB%93%E6%9E%84%E4%B8%8E%E7%AE%97%E6%B3%95/%E7%BA%BF%E6%80%A7%E8%A1%A8%E5%AF%B9%E6%AF%94.png" class title="线性表对比">

<h2 id="3、面试题总结"><a href="#3、面试题总结" class="headerlink" title="3、面试题总结"></a>3、面试题总结</h2><ul>
<li><p>ArrayList与LinkedList的区别？</p>
<ul>
<li>前者存储空间连续，后者可以连续也可以不连续</li>
<li>前者大小固定，后者大小不固定</li>
<li>前者尾插、尾删效率高，查找效率高，删除、插入效率低，后者查找效率低，删除、插入效率高</li>
</ul>
</li>
<li><p>ArrayList的大小是如何自动增加的？</p>
<p>如果其大小超过了内部数组大小，就进行判断：</p>
<ul>
<li><p>ArrayList大小 &lt;= 12 </p>
<p>就把数组扩大12</p>
</li>
<li><p>ArrayList大小 &gt; 12 </p>
<p>就把数组扩大为原来的1.5倍</p>
</li>
</ul>
</li>
<li><p>在索引中ArrayList的增加或者删除某个对象的运行过程？效率很低吗？解释一下为什么？</p>
<p>对的，因为会调用arrayCopy方法，有大量的数据移动操作。</p>
</li>
<li><p>ArrayList如何顺序删除节点？</p>
<p>用迭代器，remove必须在next之后使用，因为是利用一个cursor标签来标记当前节点。</p>
</li>
</ul>
<h1 id="三、队列"><a href="#三、队列" class="headerlink" title="三、队列"></a>三、队列</h1><p>为了解决队列的假溢出，便采用了循环队列。</p>
<h2 id="1、循环队列（顺序存储结构）"><a href="#1、循环队列（顺序存储结构）" class="headerlink" title="1、循环队列（顺序存储结构）"></a>1、循环队列（顺序存储结构）</h2><img src="/2020/10/09/%E6%95%B0%E6%8D%AE%E7%BB%93%E6%9E%84%E4%B8%8E%E7%AE%97%E6%B3%95/%E5%BE%AA%E7%8E%AF%E9%98%9F%E5%88%97.png" class title="循环队列">

<h2 id="2、链式队列（链式存储结构）"><a href="#2、链式队列（链式存储结构）" class="headerlink" title="2、链式队列（链式存储结构）"></a>2、链式队列（链式存储结构）</h2><p>就相当于单链表，但是只能从尾部插入，从头部移除。</p>
<h2 id="3、队列的变形"><a href="#3、队列的变形" class="headerlink" title="3、队列的变形"></a>3、队列的变形</h2><h3 id="3-1-双端队列Deque"><a href="#3-1-双端队列Deque" class="headerlink" title="3.1 双端队列Deque"></a>3.1 双端队列Deque</h3><p>Deque是一种具有<strong>队列和栈的性质</strong>的数据结构。双端队列中的元素可以从两端弹出，其限定插入和删除操作在表的两端进行。</p>
<img src="/2020/10/09/%E6%95%B0%E6%8D%AE%E7%BB%93%E6%9E%84%E4%B8%8E%E7%AE%97%E6%B3%95/%E5%8F%8C%E7%AB%AF%E9%98%9F%E5%88%97.png" class title="双端队列">

<h3 id="3-2-优先级队列"><a href="#3-2-优先级队列" class="headerlink" title="3.2 优先级队列"></a>3.2 优先级队列</h3><p>​    优先级队列和通常的栈和队列一样，只不过里面的每一个元素都有一个”优先级”，在处理的时候，<strong>首先处理优先级最高的</strong>。如果两个元素具有相同的优先级，则按照他们插入到队列中的先后顺序处理。</p>
<img src="/2020/10/09/%E6%95%B0%E6%8D%AE%E7%BB%93%E6%9E%84%E4%B8%8E%E7%AE%97%E6%B3%95/%E4%BC%98%E5%85%88%E7%BA%A7%E9%98%9F%E5%88%97.png" class title="优先级队列">



<h1 id="四、栈"><a href="#四、栈" class="headerlink" title="四、栈"></a>四、栈</h1><h2 id="1、面试题"><a href="#1、面试题" class="headerlink" title="1、面试题"></a>1、面试题</h2><ul>
<li><p>Java中的Stack是通过Vector来实现的，这种设计被认为是不良的设计，说说你的看法？</p>
<p>因为vector中还有很多不符合栈的特性的方法，类似add。</p>
</li>
</ul>
<h2 id="2、逆波兰表达式"><a href="#2、逆波兰表达式" class="headerlink" title="2、逆波兰表达式"></a>2、逆波兰表达式</h2><ul>
<li>有两个栈，操作数栈和运算符栈</li>
<li>如果为操作数，则把操作数入操作数栈</li>
<li>如果为运算符<ul>
<li>为左括号时，把左括号入运算符栈</li>
<li>为右括号时，把运算符栈中所有运算符出栈，且放入操作数栈中，直到遇到左括号，抛弃左括号</li>
<li>为非括号运算符时，将其与运算符栈栈顶的元素对比，如果优先级更小或者相等，将栈顶运算符出栈，且入栈到操作数栈，直到优先级相等或更大，入运算符栈</li>
</ul>
</li>
</ul>
<h1 id="五、Hash表"><a href="#五、Hash表" class="headerlink" title="五、Hash表"></a>五、Hash表</h1><h2 id="1、结构"><a href="#1、结构" class="headerlink" title="1、结构"></a>1、结构</h2><img src="/2020/10/09/%E6%95%B0%E6%8D%AE%E7%BB%93%E6%9E%84%E4%B8%8E%E7%AE%97%E6%B3%95/Hash%E8%A1%A8%E7%BB%93%E6%9E%84.png" class title="Hash表结构">

<p>可以看出哈希表是由数组和链表组合构成的结构，在java8之后，将链表改成了红黑树（为了提高查找性能，O(n）-&gt; O(logn)）</p>
<img src="/2020/10/09/%E6%95%B0%E6%8D%AE%E7%BB%93%E6%9E%84%E4%B8%8E%E7%AE%97%E6%B3%95/%E7%BA%A2%E9%BB%91%E6%A0%91.png" class title="红黑树">



<p>而每个结点就可以存储一个数据，节点内部的组成：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">static</span> <span class="class"><span class="keyword">class</span> <span class="title">Node</span>&lt;<span class="title">K</span>,<span class="title">V</span>&gt; <span class="keyword">implements</span> <span class="title">Map</span>.<span class="title">Entry</span>&lt;<span class="title">K</span>,<span class="title">V</span>&gt;</span>&#123;</span><br><span class="line">    <span class="keyword">int</span> hash;<span class="comment">//哈希值，hashcode</span></span><br><span class="line">    K key;</span><br><span class="line">    V value;</span><br><span class="line">    Node&lt;K,V&gt; next;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<h2 id="2、流程-此处展示java8以前"><a href="#2、流程-此处展示java8以前" class="headerlink" title="2、流程(此处展示java8以前)"></a>2、流程(此处展示java8以前)</h2><h3 id="2-1-put"><a href="#2-1-put" class="headerlink" title="2.1 put"></a>2.1 put</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> V <span class="title">put</span><span class="params">(K key, V value)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> putVal(hash(key), key, value, <span class="keyword">false</span>, <span class="keyword">true</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">final</span> V <span class="title">putVal</span><span class="params">(<span class="keyword">int</span> hash, K key, V value, <span class="keyword">boolean</span> onlyIfAbsent,</span></span></span><br><span class="line"><span class="function"><span class="params">                   <span class="keyword">boolean</span> evict)</span> </span>&#123;</span><br><span class="line">    <span class="comment">//1、根据key计算hashcode</span></span><br><span class="line">    <span class="keyword">int</span> hash = hash(key);</span><br><span class="line">    <span class="comment">//2、根据hashcode计算index值，找到应该在数组table中存放的下标</span></span><br><span class="line">    <span class="keyword">int</span> index = indexFor(hash);</span><br><span class="line">    <span class="comment">//3、遍历对应下标处的结点所在的链表，将新的结点插入尾部</span></span><br><span class="line">    Node&lt;K,V&gt; e = table[index];</span><br><span class="line">    <span class="keyword">while</span>(e.next!=<span class="keyword">null</span>)&#123;</span><br><span class="line">        e = e.next;</span><br><span class="line">    &#125;</span><br><span class="line">    e.next = <span class="keyword">new</span> Node&lt;K,V&gt;(key,value);</span><br><span class="line">    <span class="comment">/*4、判断，如果目前table的已用大小大于阈值，就进行扩容（threshold = table.length *  DEFAULT_LOAD_FACTOR）</span></span><br><span class="line"><span class="comment">     *static final float DEFAULT_LOAD_FACTOR = 0.75f;</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="keyword">if</span> (++size &gt; threshold)</span><br><span class="line">            resize();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="2-2-get"><a href="#2-2-get" class="headerlink" title="2.2 get"></a>2.2 get</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> V <span class="title">get</span><span class="params">(Object key)</span> </span>&#123;</span><br><span class="line">        Node&lt;K,V&gt; e;</span><br><span class="line">        <span class="keyword">return</span> (e = getNode(hash(key), key)) == <span class="keyword">null</span> ? <span class="keyword">null</span> : e.value;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">final</span> Node&lt;K,V&gt; <span class="title">getNode</span><span class="params">(<span class="keyword">int</span> hash, Object key)</span> </span>&#123;</span><br><span class="line">    <span class="comment">//1、根据key计算hashcode</span></span><br><span class="line">    <span class="keyword">int</span> hash = hash(key);</span><br><span class="line">    <span class="comment">//2、根据hashcode计算index值，找到应该在数组table中存放的下标</span></span><br><span class="line">    <span class="keyword">int</span> index = indexFor(hash);</span><br><span class="line">    <span class="comment">//3、对该下标对应的结点进行遍历，寻找对应key的结点，是通过key计算出的hashcode来寻找的，所以才会在Node中存放一个hash值</span></span><br><span class="line">     <span class="keyword">do</span> &#123;</span><br><span class="line">     	<span class="keyword">if</span> (e.hash == hash &amp;&amp;</span><br><span class="line">        ((k = e.key) == key || (key != <span class="keyword">null</span> &amp;&amp; key.equals(k))))</span><br><span class="line">        	<span class="keyword">return</span> e;</span><br><span class="line">     &#125; <span class="keyword">while</span> ((e = e.next) != <span class="keyword">null</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="2-3-indexFor"><a href="#2-3-indexFor" class="headerlink" title="2.3 indexFor"></a>2.3 indexFor</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line">index = hash &amp; (table.length - <span class="number">1</span>);<span class="comment">//相当于取模</span></span><br></pre></td></tr></table></figure>



<h2 id="3、问题解决"><a href="#3、问题解决" class="headerlink" title="3、问题解决"></a>3、问题解决</h2><ul>
<li><p>什么是HashMap？</p>
<p>HashMap就是一个以数组和链表组合构成的数据结构。</p>
</li>
<li><p>HashMap的工作原理？</p>
<p>put时：</p>
<ul>
<li>先计算出key对应的hash值</li>
<li>再通过hash值计算出index</li>
<li>再找出table数组对应位置的结点进行遍历</li>
<li>在链表尾部进行插入</li>
</ul>
<p>get时：</p>
<ul>
<li>先计算出key对应的hash值</li>
<li>再通过hash值计算出index</li>
<li>再找出table数组对应位置的结点进行遍历</li>
<li>返回hash值与查找的hash值相同的结点的value</li>
</ul>
</li>
<li><p>当两个对象的hashcode/index相同时会发生什么？</p>
<p>会发生hash碰撞，java8以前采用链表解决，java8以后采用红黑树解决。</p>
</li>
<li><p>如果HashMap的大小超过了负载因子定义的容量，会怎么做？</p>
<p>在put方法时会进行扩容，将大小扩大为原来的一倍，并且将之前的结点全部重新分配，减少hash碰撞。</p>
<p>负载因子为0.75。</p>
</li>
<li><p>重新调整HashMap大小存在什么问题吗？</p>
<p>以前存放的结点就都无法使用了，需要将所有结点重新存放。</p>
</li>
<li><p>为什么String，Integer这样的类适合用于作为key？</p>
<p>因为在存放和取出HashMap中的数据时，都需要计算hashcode，来快速定位，而上面两个类重写了hashcode方法，所以适合。</p>
<p>同理，如果自定义的类也想作为key，就需要重写hashcode方法。</p>
</li>
</ul>
<h2 id="4、应用"><a href="#4、应用" class="headerlink" title="4、应用"></a>4、应用</h2><p>ThreadLocal，SparseArray等。</p>
<h1 id="六、java容器总结"><a href="#六、java容器总结" class="headerlink" title="六、java容器总结"></a>六、java容器总结</h1><h2 id="1、集合类图"><a href="#1、集合类图" class="headerlink" title="1、集合类图"></a>1、集合类图</h2><img src="/2020/10/09/%E6%95%B0%E6%8D%AE%E7%BB%93%E6%9E%84%E4%B8%8E%E7%AE%97%E6%B3%95/%E9%9B%86%E5%90%88%E7%B1%BB%E5%9B%BE.png" class title="集合类图">

<p>有三个主要的数据结构类：Map，List，Set。</p>
<p>还有一个工具类：Iterator，Enumeration。</p>
<h2 id="2、Collection"><a href="#2、Collection" class="headerlink" title="2、Collection"></a>2、Collection</h2><img src="/2020/10/09/%E6%95%B0%E6%8D%AE%E7%BB%93%E6%9E%84%E4%B8%8E%E7%AE%97%E6%B3%95/collection.png" class title="collection">

<p>List、Set继承于Collection，且都实现了Iterator。</p>
<h2 id="3、Map"><a href="#3、Map" class="headerlink" title="3、Map"></a>3、Map</h2><img src="/2020/10/09/%E6%95%B0%E6%8D%AE%E7%BB%93%E6%9E%84%E4%B8%8E%E7%AE%97%E6%B3%95/Map.png" class title="Map">



<h1 id="七、树"><a href="#七、树" class="headerlink" title="七、树"></a>七、树</h1><h2 id="1、树的基本知识"><a href="#1、树的基本知识" class="headerlink" title="1、树的基本知识"></a>1、树的基本知识</h2><ul>
<li><p>树的基本要点</p>
<img src="/2020/10/09/%E6%95%B0%E6%8D%AE%E7%BB%93%E6%9E%84%E4%B8%8E%E7%AE%97%E6%B3%95/%E6%A0%91.png" class title="树">

<ul>
<li><p>结点的度：结点的子树个数</p>
</li>
<li><p>树的度：树中所有结点中最大的度</p>
</li>
<li><p>叶结点：度为0的结点</p>
</li>
<li><p>父结点：有子树的结点是其子树的根结点的父结点</p>
</li>
<li><p>子结点：若A是B的父结点，B就是A的子结点</p>
</li>
</ul>
</li>
<li><p>树与非树</p>
<ul>
<li>子树是不相交的</li>
<li>除了根结点之外，每个结点之有且只有一个父结点</li>
<li>一个N个结点的树只有N-1条边</li>
</ul>
</li>
<li><p>二叉树</p>
<ul>
<li>度为2的树（树中所有结点中最大的度）</li>
<li>子树有左右顺序之分 </li>
</ul>
</li>
</ul>
<h2 id="2、哈夫曼树与哈夫曼编码"><a href="#2、哈夫曼树与哈夫曼编码" class="headerlink" title="2、哈夫曼树与哈夫曼编码"></a>2、哈夫曼树与哈夫曼编码</h2><h3 id="2-1-问题"><a href="#2-1-问题" class="headerlink" title="2.1 问题"></a>2.1 问题</h3><p>给定一段字符串，如何对字符串进行编码，可以使得该字符串的编码存储空间最少？</p>
<table>
<thead>
<tr>
<th><strong>字符</strong></th>
<th><strong>a</strong></th>
<th><strong>b</strong></th>
<th><strong>c</strong></th>
<th><strong>d</strong></th>
<th><strong>e</strong></th>
<th><strong>f</strong></th>
<th><strong>g</strong></th>
</tr>
</thead>
<tbody><tr>
<td>频次</td>
<td>10</td>
<td>15</td>
<td>12</td>
<td>3</td>
<td>4</td>
<td>13</td>
<td>1</td>
</tr>
</tbody></table>
<h3 id="2-2-分析"><a href="#2-2-分析" class="headerlink" title="2.2 分析"></a>2.2 分析</h3><ul>
<li>用等长ASCII编码：58×8=464位；</li>
<li>用等长3位编码：58×3=174位；</li>
<li>不等长编码：出现频次高的字符用的编码短些，出现频次低的编码长些 </li>
</ul>
<h3 id="2-3-哈夫曼树（最优二叉树）"><a href="#2-3-哈夫曼树（最优二叉树）" class="headerlink" title="2.3 哈夫曼树（最优二叉树）"></a>2.3 哈夫曼树（最优二叉树）</h3><ul>
<li>构造一棵二叉树，该树的带权路径长度达到最小，</li>
<li>称为最优二叉树，也称为哈夫曼树(Huffman Tree)</li>
</ul>
<h2 id="3、二叉排序树"><a href="#3、二叉排序树" class="headerlink" title="3、二叉排序树"></a>3、二叉排序树</h2><h3 id="3-1-定义"><a href="#3-1-定义" class="headerlink" title="3.1 定义"></a>3.1 定义</h3><p>定义：一颗二叉树，满足以下属性</p>
<ul>
<li>左子树的所有的值小于根节点的值</li>
<li>右子树的所有的值大于根节点的值</li>
<li>左、右子树满足以上两点</li>
</ul>
<h3 id="3-2-查找"><a href="#3-2-查找" class="headerlink" title="3.2 查找"></a>3.2 查找</h3><p>查找的值X从根节点开始：</p>
<ul>
<li>如果X小于根节点值，则在左子树中继续查找</li>
<li>如果X大于根节点值，则在右子树中继续查找</li>
<li>如果X值等于根节点值，则返回该节点</li>
<li>如果都查不到，则返回空Null</li>
</ul>
<h3 id="3-3-插入"><a href="#3-3-插入" class="headerlink" title="3.3 插入"></a>3.3 插入</h3><p>插入的值X从根节点开始查找：</p>
<ul>
<li>X值小于该节点值，在左子树中继续</li>
<li>X值大于该节点值，在右子树中继续</li>
<li>如果节点是叶节点，X值小于该节点值则插入左子节点，否则插入右节点</li>
</ul>
<h3 id="3-4-删除"><a href="#3-4-删除" class="headerlink" title="3.4 删除"></a>3.4 删除</h3><p>插入的值X从根节点开始查找：</p>
<ul>
<li>如果节点的值等于X，则删除</li>
<li>X值小于该节点值，在左子树中继续</li>
<li>X值大于该节点值，在右子树中继续</li>
</ul>
<h2 id="4、平衡二叉树"><a href="#4、平衡二叉树" class="headerlink" title="4、平衡二叉树"></a>4、平衡二叉树</h2><h3 id="4-1-定义"><a href="#4-1-定义" class="headerlink" title="4.1 定义"></a>4.1 定义</h3><p>它是一 棵二叉排序树，它的左右两个子树的高度差（平衡因子）的绝对值不超过1，并且左右两个子树都是一棵平衡二叉树。<br>目的：使得树的高度最低，因为树查找的效率决定于树的高度。</p>
<h3 id="4-2-平衡二叉树的调整"><a href="#4-2-平衡二叉树的调整" class="headerlink" title="4.2 平衡二叉树的调整"></a>4.2 平衡二叉树的调整</h3><p>如果A是一颗平衡二叉树，如果新插入一个元素，会有两个结果：</p>
<ul>
<li>平衡没有被打破，不用调整</li>
<li>平衡被打破，需要调整</li>
</ul>
<p>调整原则：根据插入节点与失衡结点的位置关系来划分</p>
<ul>
<li><p>LL旋转</p>
<ul>
<li>插入节点在失衡结点的左子树的左边</li>
<li>只需要经过一次左旋即可达到平衡</li>
</ul>
<img src="/2020/10/09/%E6%95%B0%E6%8D%AE%E7%BB%93%E6%9E%84%E4%B8%8E%E7%AE%97%E6%B3%95/LL.png" class title="LL">
</li>
<li><p>RR旋转</p>
<ul>
<li>插入节点在失衡结点的右子树的右边</li>
<li>只需要经过一次右旋即可达到平衡</li>
</ul>
<img src="/2020/10/09/%E6%95%B0%E6%8D%AE%E7%BB%93%E6%9E%84%E4%B8%8E%E7%AE%97%E6%B3%95/RR.png" class title="RR">
</li>
<li><p>LR旋转</p>
<ul>
<li>插入节点在失衡结点的左子树的右边</li>
<li>失衡结点的左子树先做RR旋转</li>
<li>失衡结点再做LL旋转也可达到平衡</li>
</ul>
<img src="/2020/10/09/%E6%95%B0%E6%8D%AE%E7%BB%93%E6%9E%84%E4%B8%8E%E7%AE%97%E6%B3%95/LR.png" class title="LR">
</li>
<li><p>RL旋转</p>
<ul>
<li>插入节点在失衡结点的右子树的左边</li>
<li>失衡结点的右子树先做LL旋转</li>
<li>失衡结点再做RR旋转也可达到平衡</li>
</ul>
<img src="/2020/10/09/%E6%95%B0%E6%8D%AE%E7%BB%93%E6%9E%84%E4%B8%8E%E7%AE%97%E6%B3%95/RL.png" class title="RL">



</li>
</ul>
<h1 id="八、图"><a href="#八、图" class="headerlink" title="八、图"></a>八、图</h1><h2 id="1、图的定义"><a href="#1、图的定义" class="headerlink" title="1、图的定义"></a>1、图的定义</h2><ul>
<li>图由顶点（vertex）和边（edge）组成的一种结构。</li>
<li>顶点的集合V，边的集合是E，所以图记为G = （V，E）</li>
<li>两类信息：顶点信息，边的信息</li>
</ul>
<h2 id="2、图的数据存储结构（邻接矩阵）"><a href="#2、图的数据存储结构（邻接矩阵）" class="headerlink" title="2、图的数据存储结构（邻接矩阵）"></a>2、图的数据存储结构（邻接矩阵）</h2><img src="/2020/10/09/%E6%95%B0%E6%8D%AE%E7%BB%93%E6%9E%84%E4%B8%8E%E7%AE%97%E6%B3%95/%E6%97%A0%E5%90%91%E5%9B%BE%E9%82%BB%E6%8E%A5%E7%9F%A9%E9%98%B5.png" class title="无向图邻接矩阵">

<img src="/2020/10/09/%E6%95%B0%E6%8D%AE%E7%BB%93%E6%9E%84%E4%B8%8E%E7%AE%97%E6%B3%95/%E6%9C%89%E5%90%91%E5%9B%BE%E9%82%BB%E6%8E%A5%E7%9F%A9%E9%98%B5.png" class title="有向图邻接矩阵">

<img src="/2020/10/09/%E6%95%B0%E6%8D%AE%E7%BB%93%E6%9E%84%E4%B8%8E%E7%AE%97%E6%B3%95/%E5%B8%A6%E6%9D%83%E6%9C%89%E5%90%91%E5%9B%BE%E9%82%BB%E6%8E%A5%E7%9F%A9%E9%98%B5.png" class title="带权有向图邻接矩阵">

<h2 id="3、图的遍历"><a href="#3、图的遍历" class="headerlink" title="3、图的遍历"></a>3、图的遍历</h2><img src="/2020/10/09/%E6%95%B0%E6%8D%AE%E7%BB%93%E6%9E%84%E4%B8%8E%E7%AE%97%E6%B3%95/%E5%9B%BE.png" class title="图">

<h3 id="3-1-深度优先遍历"><a href="#3-1-深度优先遍历" class="headerlink" title="3.1 深度优先遍历"></a>3.1 深度优先遍历</h3><ul>
<li>第1步：访问A。 </li>
<li>第2步：A之后有去向“C,D,F”中的三条路，随意选择一个—-(A的邻接点)C   </li>
<li>第3步： C之后有去向“B,D”中的两条路，随意选择一个—-(C的邻接点)B </li>
<li>第4步：B已是尽头，无路可走，于是回溯到上一步的C</li>
<li>第5步： C之后去向“B”中的路已走过了，现在只有选择D</li>
<li>第6步：D之后的路通向A，A已访问过，于是回溯到C</li>
<li>第7步：C的两条路都走过了，不通，继续回溯到A</li>
<li>第8步：A的（”C,D”）都走过了，不通，只剩下F可走，走到F</li>
<li>第9步：F有路（”G”）可走，走到G</li>
</ul>
<p>访问顺序是： A -&gt; C -&gt; B -&gt; D -&gt; F -&gt; G -&gt; E</p>
<h3 id="3-2-广度优先遍历"><a href="#3-2-广度优先遍历" class="headerlink" title="3.2 广度优先遍历"></a>3.2 广度优先遍历</h3><ul>
<li>第1步：访问A</li>
<li>第2步：访问上一步A的邻接点，有“C,D,F” ，依次访问C,D,F</li>
<li>第3步： 访问上一步（ C,D,F ）的邻接点，有B，G</li>
<li>第4步：访问上一步（B,G）的邻接点，有E</li>
<li>第5步： 访问上一步E的邻接点<br>访问顺序是： A -&gt; C -&gt; D -&gt; F -&gt; B -&gt; G -&gt; E</li>
</ul>
<h2 id="4、图的最短路径算法（Dijkstra）"><a href="#4、图的最短路径算法（Dijkstra）" class="headerlink" title="4、图的最短路径算法（Dijkstra）"></a>4、图的最短路径算法（Dijkstra）</h2><img src="/2020/10/09/%E6%95%B0%E6%8D%AE%E7%BB%93%E6%9E%84%E4%B8%8E%E7%AE%97%E6%B3%95/%E5%9B%BE%E8%B7%AF%E5%BE%84.png" class title="图路径">

<ul>
<li>扫描邻接点，记录邻接点权重值</li>
<li>找出邻接点里最小的那个值</li>
</ul>
<h2 id="5、AOV网络拓补排序"><a href="#5、AOV网络拓补排序" class="headerlink" title="5、AOV网络拓补排序"></a>5、AOV网络拓补排序</h2><img src="/2020/10/09/%E6%95%B0%E6%8D%AE%E7%BB%93%E6%9E%84%E4%B8%8E%E7%AE%97%E6%B3%95/AOV.png" class title="AOV">

<ul>
<li>计算出各个节点的入度</li>
<li>入度为0节点入队</li>
<li>入队节点的邻接点入度-1</li>
<li>重复2-3步骤</li>
<li>拓补序列：AA,A,B,D,G</li>
</ul>
<h2 id="6、-关键路径算法"><a href="#6、-关键路径算法" class="headerlink" title="6、 关键路径算法"></a>6、 关键路径算法</h2><img src="/2020/10/09/%E6%95%B0%E6%8D%AE%E7%BB%93%E6%9E%84%E4%B8%8E%E7%AE%97%E6%B3%95/AOV.png" class title="关键路径">

<p>各节点的最早施工时间：</p>
<table>
<thead>
<tr>
<th>AA</th>
<th>A</th>
<th>B</th>
<th><strong>D</strong></th>
<th><strong>G</strong></th>
</tr>
</thead>
<tbody><tr>
<td>0</td>
<td>3</td>
<td>2</td>
<td>7</td>
<td>8</td>
</tr>
</tbody></table>
<p>正向推理，出发点到该结点的最长路径就为最早时间。</p>
<p>各节点的最晚施工时间：</p>
<table>
<thead>
<tr>
<th>AA</th>
<th>A</th>
<th>B</th>
<th>D</th>
<th>G</th>
</tr>
</thead>
<tbody><tr>
<td>0</td>
<td>3</td>
<td>5</td>
<td>7</td>
<td>8</td>
</tr>
</tbody></table>
<p>逆向推理，终点返回到该结点的最长路径就是最晚时间。</p>
<h1 id="九、排序"><a href="#九、排序" class="headerlink" title="九、排序"></a>九、排序</h1><h2 id="1、冒泡排序"><a href="#1、冒泡排序" class="headerlink" title="1、冒泡排序"></a>1、冒泡排序</h2><p>​    一种简单的排序算法。它反复地走访过要排序的数列，一次比较两个元素，如果它们的顺序错误就把它们交换过来。这个工作重复地进行直到没有元素再需要交换，也就是说该数列已经排序完成。这个算法的名字由来是因为元素会经由交换慢慢“浮”到数列的顶端。</p>
<ul>
<li>从数组头开始，比较相邻的元素。如果第一个比第二个大(小)，就交换它们两个</li>
<li>对每一对相邻元素作同样的工作，从开始第一对到尾部的最后一对，这样在最后的元素应该会是最大(小)的数</li>
<li>重复步骤1~2，重复次数等于数组的长度，直到排序完成</li>
</ul>
<img src="/2020/10/09/%E6%95%B0%E6%8D%AE%E7%BB%93%E6%9E%84%E4%B8%8E%E7%AE%97%E6%B3%95/%E5%86%92%E6%B3%A1%E6%8E%92%E5%BA%8F.png" class title="冒泡排序">

<h2 id="2、简单选择排序"><a href="#2、简单选择排序" class="headerlink" title="2、简单选择排序"></a>2、简单选择排序</h2><ul>
<li>首先，找到数组中最大（小）的那个元素</li>
<li>其次，将它和数组的第一个元素交换位置（如果第一个元素就是最大（小）元素那么它就和自己交换）</li>
<li>再次，在剩下的元素中找到最大（小）的元素，将它与数组的第二个元素交换位置。如此往复，直到将整个数组排序</li>
<li>这种方法叫做选择排序，因为它在不断地选择剩余元素之中的最大（小）者</li>
</ul>
<img src="/2020/10/09/%E6%95%B0%E6%8D%AE%E7%BB%93%E6%9E%84%E4%B8%8E%E7%AE%97%E6%B3%95/%E7%AE%80%E5%8D%95%E9%80%89%E6%8B%A9%E6%8E%92%E5%BA%8F.png" class title="简单选择排序">

<h2 id="3、简单插入排序"><a href="#3、简单插入排序" class="headerlink" title="3、简单插入排序"></a>3、简单插入排序</h2><ul>
<li>对于未排序数据，在已排序序列中从后向前扫描，找到相应位置并插入</li>
<li>为了给要插入的元素腾出空间，我们需要将插入位置之后的已排序元素在都向后移动一位</li>
<li>插入排序所需的时间取决于输入中元素的初始顺序。例如，对一个很大且其中的元素已经有序（或接近有序）的数组进行排序将会比对随机顺序的数组或是逆序数组进行排序要快得多</li>
<li>总的来说，插入排序对于部分有序的数组十分高效，也很适合小规模数组</li>
</ul>
<img src="/2020/10/09/%E6%95%B0%E6%8D%AE%E7%BB%93%E6%9E%84%E4%B8%8E%E7%AE%97%E6%B3%95/%E7%AE%80%E5%8D%95%E6%8F%92%E5%85%A5%E6%8E%92%E5%BA%8F.png" class title="简单插入排序">

<h2 id="4、希尔排序"><a href="#4、希尔排序" class="headerlink" title="4、希尔排序"></a>4、希尔排序</h2><ul>
<li>一种基于插入排序的快速的排序算法。简单插入排序对于大规模乱序数组很慢，因为元素只能一点一点地从数组的一端移动到另一端。例如，如果主键最小的元素正好在数组的尽头，要将它挪到正确的位置就需要N-1 次移动</li>
<li>希尔排序为了加快速度简单地改进了插入排序，也称为缩小增量排序，同时该算法是突破O(n^2）的第一批算法之一</li>
<li>希尔排序是把待排序数组按一定数量的分组，对每组使用直接插入排序算法排序；然后缩小数量继续分组排序，随着数量逐渐减少，每组包含的元素越来越多，当数量减至 1 时，整个数组恰被分成一组，排序便完成了。这个不断缩小的数量，就构成了一个增量序列。</li>
</ul>
<img src="/2020/10/09/%E6%95%B0%E6%8D%AE%E7%BB%93%E6%9E%84%E4%B8%8E%E7%AE%97%E6%B3%95/%E5%B8%8C%E5%B0%94%E6%8E%92%E5%BA%8F1.png" class title="希尔排序1">

<img src="/2020/10/09/%E6%95%B0%E6%8D%AE%E7%BB%93%E6%9E%84%E4%B8%8E%E7%AE%97%E6%B3%95/%E5%B8%8C%E5%B0%94%E6%8E%92%E5%BA%8F2.png" class title="希尔排序2">

<h2 id="5、归并排序"><a href="#5、归并排序" class="headerlink" title="5、归并排序"></a>5、归并排序</h2><ul>
<li>归并排序是建立在归并操作上的一种有效的排序算法。该算法是采用分治法的一个非常典型的应用</li>
<li>对于给定的一组数据，利用递归与分治技术将数据序列划分成为越来越小的半子表，在对半子表排序后，再用递归方法将排好序的半子表合并成为越来越大的有序序列</li>
<li>为了提升性能，有时我们在半子表的个数小于某个数（比如15）的情况下，对半子表的排序采用其他排序算法，比如插入排序</li>
<li>若将两个有序表合并成一个有序表，称为2-路归并，与之对应的还有多路归并</li>
</ul>
<img src="/2020/10/09/%E6%95%B0%E6%8D%AE%E7%BB%93%E6%9E%84%E4%B8%8E%E7%AE%97%E6%B3%95/%E5%BD%92%E5%B9%B6%E6%8E%92%E5%BA%8F1.png" class title="归并排序1">

<img src="/2020/10/09/%E6%95%B0%E6%8D%AE%E7%BB%93%E6%9E%84%E4%B8%8E%E7%AE%97%E6%B3%95/%E5%BD%92%E5%B9%B6%E6%8E%92%E5%BA%8F2.png" class title="归并排序2">

<h2 id="6、快速排序"><a href="#6、快速排序" class="headerlink" title="6、快速排序"></a>6、快速排序</h2><ul>
<li><p>快速排序（Quicksort）是对冒泡排序的一种改进，也是采用分治法的一个典型的应用</p>
</li>
<li><p>首先任意选取一个数据（比如数组的第一个数）作为关键数据，我们称为基准数(Pivot)，然后将所有比它小的数都放到它前面，所有比它大的数都放到它后面，这个过程称为一趟快速排序，也称为分区（partition）操作</p>
</li>
<li><p>通过一趟快速排序将要排序的数据分割成独立的两部分，其中一部分的所有数据都比另外一部分的所有数据都要小，然后再按此方</p>
<p>对这两部分数据分别进行快速排序，整个排序过程可以递归进行，以此达到整个数组变成有序序列</p>
</li>
<li><p>为了提升性能，有时我们在分割后独立的两部分的个数小于某个数（比如15）的情况下，会采用其他排序算法，比如插入排序</p>
</li>
</ul>
<img src="/2020/10/09/%E6%95%B0%E6%8D%AE%E7%BB%93%E6%9E%84%E4%B8%8E%E7%AE%97%E6%B3%95/%E5%BF%AB%E9%80%9F%E6%8E%92%E5%BA%8F%E5%8E%9F%E7%90%86.png" class title="快速排序原理">

<img src="/2020/10/09/%E6%95%B0%E6%8D%AE%E7%BB%93%E6%9E%84%E4%B8%8E%E7%AE%97%E6%B3%95/%E5%BF%AB%E9%80%9F%E6%8E%92%E5%BA%8F1.png" class title="快速排序1">

<img src="/2020/10/09/%E6%95%B0%E6%8D%AE%E7%BB%93%E6%9E%84%E4%B8%8E%E7%AE%97%E6%B3%95/%E5%BF%AB%E9%80%9F%E6%8E%92%E5%BA%8F2.png" class title="快速排序2">

<img src="/2020/10/09/%E6%95%B0%E6%8D%AE%E7%BB%93%E6%9E%84%E4%B8%8E%E7%AE%97%E6%B3%95/%E5%BF%AB%E9%80%9F%E6%8E%92%E5%BA%8F3.png" class title="快速排序3">

<h2 id="7、堆排序"><a href="#7、堆排序" class="headerlink" title="7、堆排序"></a>7、堆排序</h2><ul>
<li>许多应用程序都需要处理有序的元素，但不一定要求他们全部有序，或者不一定要一次就将他们排序，很多时候，我们每次只需要操作数据中的最大元素（最小元素），那么有一种基于二叉堆的数据结构可以提供支持</li>
<li>所谓二叉堆，是一个完全二叉树的结构，同时满足堆的性质：即子结点的键值或索引总是小于（或者大于）它的父节点。在一个二叉堆中，根节点总是最大（或者最小）节点</li>
<li>堆排序算法就是抓住了这一特点，每次都取堆顶的元素，然后将剩余的元素重新调整为最大（最小）堆，依次类推，最终得到排序的序列</li>
</ul>
<h3 id="7-1-完全二叉树"><a href="#7-1-完全二叉树" class="headerlink" title="7.1 完全二叉树"></a>7.1 完全二叉树</h3><p>所有结点要与满二叉树一一对应，满二叉树一定是完全二叉树。</p>
<p><strong>推论：</strong></p>
<ul>
<li>对于位置为K的结点   左子结点=2<em>k+1  右子结点=2</em>(k+1)</li>
<li>最后一个非叶节点的位置为 (N/2)-1，N为数组长度</li>
</ul>
<h3 id="7-2-排序过程"><a href="#7-2-排序过程" class="headerlink" title="7.2 排序过程"></a>7.2 排序过程</h3><img src="/2020/10/09/%E6%95%B0%E6%8D%AE%E7%BB%93%E6%9E%84%E4%B8%8E%E7%AE%97%E6%B3%95/%E5%A0%86%E6%8E%92%E5%BA%8F1.png" class title="堆排序1">

<img src="/2020/10/09/%E6%95%B0%E6%8D%AE%E7%BB%93%E6%9E%84%E4%B8%8E%E7%AE%97%E6%B3%95/%E5%A0%86%E6%8E%92%E5%BA%8F2.png" class title="堆排序2">

<img src="/2020/10/09/%E6%95%B0%E6%8D%AE%E7%BB%93%E6%9E%84%E4%B8%8E%E7%AE%97%E6%B3%95/%E5%A0%86%E6%8E%92%E5%BA%8F3.png" class title="堆排序3">

<h2 id="8、计数排序"><a href="#8、计数排序" class="headerlink" title="8、计数排序"></a>8、计数排序</h2><ul>
<li>计数排序对一定范围内的整数排序时候的速度非常快，一般快于其他排序算法。但计数排序局限性比较大，只限于对整数进行排序，而且待排序元素值分布较连续、跨度小的情况</li>
<li>计数排序是一个排序时不比较元素大小的排序算法</li>
<li>如果一个数组里所有元素都是整数，而且都在0-K以内。对于数组里每个元素来说，如果能知道数组里有多少项小于或等于该元素，就能准确地给出该元素在排序后的数组的位置</li>
</ul>
<img src="/2020/10/09/%E6%95%B0%E6%8D%AE%E7%BB%93%E6%9E%84%E4%B8%8E%E7%AE%97%E6%B3%95/%E8%AE%A1%E6%95%B0%E6%8E%92%E5%BA%8F1.png" class title="计数排序1">

<img src="/2020/10/09/%E6%95%B0%E6%8D%AE%E7%BB%93%E6%9E%84%E4%B8%8E%E7%AE%97%E6%B3%95/%E8%AE%A1%E6%95%B0%E6%8E%92%E5%BA%8F2.png" class title="计数排序2">

<h2 id="9、桶排序"><a href="#9、桶排序" class="headerlink" title="9、桶排序"></a>9、桶排序</h2><p>​    桶排序 (Bucket sort)的工作的原理：假设输入数据服从均匀分布，利用某种函数的映射关系将数据分到有限数量的桶里，每个桶再分别排序（有可能再使用别的排序算法或是以递归方式继续使用桶排序）。 </p>
<img src="/2020/10/09/%E6%95%B0%E6%8D%AE%E7%BB%93%E6%9E%84%E4%B8%8E%E7%AE%97%E6%B3%95/%E6%A1%B6%E6%8E%92%E5%BA%8F.png" class title="桶排序">

<h2 id="10、基数排序"><a href="#10、基数排序" class="headerlink" title="10、基数排序"></a>10、基数排序</h2><ul>
<li>常见的数据元素一般是由若干位组成的，比如字符串由若干字符组成，整数由若干位0~9数字组成。基数排序按照从右往左的顺序，依次将每一位都当做一次关键字，然后按照该关键字对数组排序，同时每一轮排序都基于上轮排序后的结果；当我们将所有的位排序后，整个数组就达到有序状态。基数排序不是基于比较的算法</li>
<li>基数是什么意思？对于十进制整数，每一位都只可能是0~9中的某一个，总共10种可能。那10就是它的基，同理二进制数字的基为2；对于字符串，如果它使用的是8位的扩展ASCII字符集，那么它的基就是256。</li>
</ul>
<img src="/2020/10/09/%E6%95%B0%E6%8D%AE%E7%BB%93%E6%9E%84%E4%B8%8E%E7%AE%97%E6%B3%95/%E5%9F%BA%E6%95%B0%E6%8E%92%E5%BA%8F.png" class title="基数排序">

<ul>
<li>基数排序 vs 计数排序 vs 桶排序<ul>
<li>基数排序：根据键值的每位数字来分配桶</li>
<li>计数排序：每个桶只存储单一键值</li>
<li>桶排序：每个桶存储一定范围的数值</li>
</ul>
</li>
</ul>
<h2 id="11、排序算法总结"><a href="#11、排序算法总结" class="headerlink" title="11、排序算法总结"></a>11、排序算法总结</h2><table>
<thead>
<tr>
<th></th>
<th><strong>平均时间复杂度</strong></th>
<th><strong>最好情况</strong></th>
<th><strong>最坏情况</strong></th>
<th><strong>空间复杂度</strong></th>
<th><strong>稳定性</strong></th>
<th><strong>比较算法</strong></th>
</tr>
</thead>
<tbody><tr>
<td>冒泡排序</td>
<td>O(n^2)</td>
<td>O(n)</td>
<td>O(n^2)</td>
<td>O(1)</td>
<td>稳定</td>
<td>是</td>
</tr>
<tr>
<td>选择排序</td>
<td>O(n^2)</td>
<td>O(n^2)</td>
<td>O(n^2)</td>
<td>O(1)</td>
<td>不稳定</td>
<td>是</td>
</tr>
<tr>
<td>插入排序</td>
<td>O(n^2)</td>
<td>O(n)</td>
<td>O(n^2)</td>
<td>O(1)</td>
<td>稳定</td>
<td>是</td>
</tr>
<tr>
<td>希尔排序</td>
<td>不确定</td>
<td>O(n^1.3)</td>
<td>O(n^2)</td>
<td>O(1)</td>
<td>不稳定</td>
<td>是</td>
</tr>
<tr>
<td>归并排序</td>
<td>O(nlog n)</td>
<td>O(nlog n)</td>
<td>O(nlog n)</td>
<td>O(n)</td>
<td>稳定</td>
<td>是</td>
</tr>
<tr>
<td>快速排序</td>
<td>O(nlog n)</td>
<td>O(nlog n)</td>
<td>O(n^2)</td>
<td>O(log n)</td>
<td>不稳定</td>
<td>是</td>
</tr>
<tr>
<td>堆排序</td>
<td>O(nlog n)</td>
<td>O(nlog n)</td>
<td>O(nlog n)</td>
<td>O(1)</td>
<td>不稳定</td>
<td>是</td>
</tr>
<tr>
<td>计数排序</td>
<td>O(n+k)</td>
<td>O(n+k)</td>
<td>O(n+k)</td>
<td>O(k)</td>
<td>稳定</td>
<td>否</td>
</tr>
<tr>
<td>桶排序</td>
<td>O(n+k)</td>
<td>O(n+k)</td>
<td>O(n^2)</td>
<td>O(n+k)</td>
<td>稳定</td>
<td>否</td>
</tr>
<tr>
<td>基数排序</td>
<td>O(n*k)</td>
<td>O(n*k)</td>
<td>O(n*k)</td>
<td>O(n+k)</td>
<td>稳定</td>
<td>否</td>
</tr>
</tbody></table>
<ul>
<li><p>算法的稳定性</p>
<ul>
<li>稳定：如果a原本在b前面，而a=b，排序之后a仍然在b的前面</li>
<li>不稳定：如果a原本在b的前面，而a=b，排序之后a可能会出现在b的后面</li>
<li>排序算法如果是稳定的，那么从一个键上排序，然后再从另一个键上排序，前一个键排序的结果可以为后一个键排序所用</li>
</ul>
</li>
<li><p>算法的复杂度</p>
<p>算法的复杂度往往取决于数据的规模大小和数据本身分布性质。</p>
<ul>
<li>时间复杂度： 一个算法执行所耗费的时间</li>
<li>空间复杂度：对一个算法在运行过程中临时占用存储空间大小的量度</li>
<li>常见复杂度由小到大：O(1) &lt; O(logn) &lt; O(n) &lt; O(nlogn) &lt; O(n^2) &lt; O(n^3) &lt; O(2^n)</li>
</ul>
</li>
<li><p>时间复杂度记忆 </p>
<ul>
<li>冒泡、选择、插入排序需要两个for循环，每次只关注一个元素，平均时间复杂度为（一遍找元素O(n)，一遍找位置O(n)）</li>
<li>快速、归并、堆基于分治思想，log以2为底，平均时间复杂度往往和O(nlogn)（一遍找元素O(n)，一遍找位置O(logn)）相关</li>
<li>而希尔排序依赖于所取增量序列的性质，但是到目前为止还没有一个最好的增量序列 。例如希尔增量序列时间复杂度为O(n²)，而Hibbard增量序列的希尔排序的时间复杂度为 O(n^1.5)，有人在大量的实验后得出结论;当n在某个特定的范围后希尔排序的最小时间复杂度大约为n^1.3</li>
</ul>
</li>
<li><p>从平均时间来看，快速排序是效率最高的</p>
</li>
</ul>
<h1 id="十、字符串"><a href="#十、字符串" class="headerlink" title="十、字符串"></a>十、字符串</h1><h2 id="1、定义："><a href="#1、定义：" class="headerlink" title="1、定义："></a>1、定义：</h2><p>串（string）是由零个或多个字符组成的有限序列，又名叫字符串。 </p>
<ul>
<li>一般地，由n个字符串构成的串记作:  <strong>S=”a<sub>0</sub>a<sub>1</sub>……a<sub>n-1</sub>“(n≥0),其中a_i（1≤i≤n）</strong><ul>
<li>n是一个有限的数值</li>
<li>串一般记为S是串的名称，用双引号或单引号括起来的字符序列是串的值（引号不属于串的内容）。</li>
<li>可以是字母、数字或其他字符，i就是该字符在串中的位置。串中的字符数目n称为串的长度，n是一个有限的数值。</li>
</ul>
</li>
</ul>
<h2 id="2、特征："><a href="#2、特征：" class="headerlink" title="2、特征："></a>2、特征：</h2><ul>
<li>结构简单，(以二进制字符为例，仅 0,1两种字符构成)</li>
<li>规模庞大，</li>
<li>元素(字符)重复率高</li>
</ul>
<h2 id="3、子串"><a href="#3、子串" class="headerlink" title="3、子串"></a>3、子串</h2><p>在对字符串S做处理时，经常需要取出其中某一连续的片段，称为S的子串(substring)</p>
<ul>
<li>具体地，由串S中起始于位置i的连续k个字符组成的子串记作<pre><code>**substr(S,i,k) = &quot;a&lt;sub&gt;i&lt;/sub&gt;a&lt;sub&gt;i+1&lt;/sub&gt;...a&lt;sub&gt;i+k-1&lt;/sub&gt;&quot;,0≤i 〈 n,0≤k**</code></pre></li>
<li>前缀 <strong>prefix(S,k) = substr(S,0,k);</strong></li>
<li>后缀 <strong>suffix(S,k) = substr(S,n-k,k)</strong></li>
<li>空格串：只包含空格的串。 </li>
</ul>
<h2 id="4、结论"><a href="#4、结论" class="headerlink" title="4、结论"></a>4、结论</h2><ol>
<li>空串是任何字符串的子串，也是任何字符串的前缀和后缀</li>
<li>任何字符串都是自己的子串，也是自己的前缀和后缀。此类子串，前缀和后缀分别称为平凡子串，平凡前缀，平凡后缀</li>
<li>字符串本身之外的所以非空子串，前缀，后缀，分别称为真子串，真前缀，真后缀</li>
</ol>
<h2 id="5、判等"><a href="#5、判等" class="headerlink" title="5、判等"></a>5、判等</h2><p>一对字符串S=”a<sub>0</sub>a<sub>1</sub>……a<sub>n-1</sub>“ 和 T=”b<sub>0</sub>b<sub>1</sub>……b<sub>m-1</sub>“,当且仅当二者长度相等(n=m),<br>且对应的字符分别相等(对任何0≤i 〈n,都有a<sub>i</sub> = b<sub>i</sub>)</p>
<h2 id="6、模式匹配算法"><a href="#6、模式匹配算法" class="headerlink" title="6、模式匹配算法"></a>6、模式匹配算法</h2><h3 id="6-1-朴素的模式匹配算法-BF-Brute-Force-算法"><a href="#6-1-朴素的模式匹配算法-BF-Brute-Force-算法" class="headerlink" title="6.1 朴素的模式匹配算法(BF(Brute-Force)算法)"></a>6.1 朴素的模式匹配算法(BF(Brute-Force)算法)</h3><ul>
<li>基本思想是：<ol>
<li>从主串的第一个字符起与子串的第一个字符进行比较，若相等，则继续逐对字符进行后续的比较；</li>
<li>若不相等，则从主串第二个字符起与子串的第一个字符重新比较，以此类推，<br>直到子串中每个字符依次和主串中的一个连续的字符序列相等为止，此时称为匹配成功。</li>
<li>如果不能在主串中找到与子串相同的字符序列，则匹配失败。</li>
</ol>
</li>
<li>BF算法是最原始、最暴力的求解过程，但也是其他匹配算法的基础</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">bruteForce</span><span class="params">(String s, String p)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">int</span> index = -<span class="number">1</span>;<span class="comment">// 成功匹配的位置</span></span><br><span class="line">        <span class="keyword">int</span> sLength = s.length();<span class="comment">// 主串长度</span></span><br><span class="line">        <span class="keyword">int</span> pLength = p.length();<span class="comment">// 子串长度</span></span><br><span class="line">        <span class="keyword">if</span> (sLength &lt; pLength) &#123;</span><br><span class="line">            System.out.println(<span class="string">"Error.The main string is greater than the sub string length."</span>);</span><br><span class="line">            <span class="keyword">return</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">int</span> i = <span class="number">0</span>;</span><br><span class="line">        <span class="keyword">int</span> j = <span class="number">0</span>;</span><br><span class="line">        <span class="keyword">while</span> (i &lt; sLength &amp;&amp; j &lt; pLength) &#123;</span><br><span class="line">            <span class="keyword">if</span> (s.charAt(i) == p.charAt(j)) &#123;<span class="comment">// 判断对应位置的字符是否相等</span></span><br><span class="line">                i++;<span class="comment">// 若相等，主串、子串继续依次比较</span></span><br><span class="line">                j++;</span><br><span class="line">            &#125; <span class="keyword">else</span> &#123;<span class="comment">// 若不相等</span></span><br><span class="line">                i = i - j + <span class="number">1</span>;<span class="comment">// 主串回溯到上次开始匹配的下一个字符</span></span><br><span class="line">                j = <span class="number">0</span>;<span class="comment">// 子串从头开始重新匹配</span></span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">if</span> (j &gt;= pLength) &#123;<span class="comment">// 匹配成功</span></span><br><span class="line">            index = i - j;</span><br><span class="line">            System.out.println(<span class="string">"Successful match,index is:"</span> + index);</span><br><span class="line">        &#125; <span class="keyword">else</span> &#123;<span class="comment">// 匹配失败</span></span><br><span class="line">            System.out.println(<span class="string">"Match failed."</span>);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<h3 id="6-2-KMP"><a href="#6-2-KMP" class="headerlink" title="6.2 KMP"></a>6.2 KMP</h3><p>P模式匹配算法，是一个效率非常高的字符串匹配算法。其全称是Knuth–Morris–Pratt string searching algorithm</p>
<ul>
<li>其核心思想就是主串不回溯，模式串尽量多地往右移动</li>
<li>即: <strong>N(P, j) = {t | prefix(prefix(P, j), t) = suffix(prefix(P, j), t), 0≤ t 〈 j } shift = j -t</strong> shift就是本次移动<br>地最大步长</li>
</ul>
<p><strong>构建next表</strong></p>
<ol>
<li>因为空串是任何非空串的真字串，真前缀，真后缀，故只要 j &gt; 0,则必有 <strong>0 ∈ N(P, j)</strong>。<br>此时<strong>N(P, j)</strong> 必非空，从而保证“在其中取最大值”这一操作可行。反之。若j=0,则前缀prefix(P， j)<br>本身就是空串，它没有真子串，于是必有集合<strong>N(P, j) = φ</strong>。此种情况下，<strong>next[0]</strong> 该如何定义呢？</li>
<li>按照串匹配算法的构思，倘若某轮迭代中第一对字符即失配，则应该将模式串P直接右移一位，然后从其首字符继续下一轮对比<br>就实际效果而言，这一处理方法完全等价于“令<strong>next[0] = -1</strong>”。    </li>
<li>下面以模式串 <strong>“ABCDABD”</strong>  为例来详细说明是如何构建<strong>next表</strong>的   </li>
</ol>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">P &#x3D; ABCDABD</span><br><span class="line">j &#x3D; 0, prefix(P, 0) &#x3D; φ</span><br><span class="line">next[0] &#x3D; -1;&#x2F;&#x2F;规定如此</span><br><span class="line"></span><br><span class="line">P &#x3D; ABCDABD</span><br><span class="line">j &#x3D; 1, prefix(P, 1) &#x3D; A</span><br><span class="line">真前缀: φ</span><br><span class="line">真后缀: φ</span><br><span class="line">next[1] &#x3D; 0;</span><br><span class="line"></span><br><span class="line">P &#x3D; ABCDABD</span><br><span class="line">j &#x3D; 2, prefix(P, 2) &#x3D; AB</span><br><span class="line">真前缀: A</span><br><span class="line">真后缀: B</span><br><span class="line">next[2] &#x3D; 0;</span><br><span class="line"></span><br><span class="line">P &#x3D; ABCDABD</span><br><span class="line">j &#x3D; 3, prefix(P, 3) &#x3D; ABC</span><br><span class="line">真前缀: A,AB</span><br><span class="line">真后缀: BC,C</span><br><span class="line">next[3] &#x3D; 0;</span><br><span class="line"></span><br><span class="line">P &#x3D; ABCDABD</span><br><span class="line">j &#x3D; 4, prefix(P, 4) &#x3D; ABCD</span><br><span class="line">真前缀: A,AB,ABC</span><br><span class="line">真后缀: BCD,CD,D</span><br><span class="line">next[4] &#x3D; 0;</span><br><span class="line"></span><br><span class="line">P &#x3D; ABCDABD</span><br><span class="line">j &#x3D; 5, prefix(P, 5) &#x3D; ABCDA</span><br><span class="line">真前缀: A,AB,ABC,ABCD</span><br><span class="line">真后缀: BCDA,CDA,DA,A</span><br><span class="line">next[5] &#x3D; 1;</span><br><span class="line"></span><br><span class="line">P &#x3D; ABCDABD</span><br><span class="line">j &#x3D; 6, prefix(P, 6) &#x3D; ABCDAB</span><br><span class="line">真前缀: A,AB,ABC,ABCD,ABCDA</span><br><span class="line">真后缀: BCDAB,CDAB,DAB,AB,B</span><br><span class="line">next[6] &#x3D; 2;</span><br><span class="line"></span><br><span class="line">得出next表为:</span><br><span class="line">[-1, 0, 0, 0, 0, 1, 2]</span><br></pre></td></tr></table></figure>

<p>接下来就是代码实现:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">//第一步构造next表</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">int</span>[] buildNext(String p)&#123;</span><br><span class="line">        <span class="comment">//构建next表就是查找真前缀 == 真后缀的最大长度，以获取模式串尽量多地往右移动</span></span><br><span class="line">        <span class="keyword">int</span>[] N = <span class="keyword">new</span> <span class="keyword">int</span>[p.length()];</span><br><span class="line">        <span class="keyword">int</span> m = p.length(),j = <span class="number">0</span>;<span class="comment">//主串位置</span></span><br><span class="line">        <span class="keyword">int</span> t = N[<span class="number">0</span>] = -<span class="number">1</span>;<span class="comment">//字串位置</span></span><br><span class="line">        <span class="keyword">while</span>(j &lt; m -<span class="number">1</span>)&#123;</span><br><span class="line">            <span class="keyword">if</span>(t &lt; <span class="number">0</span> || p.charAt(j) == p.charAt(t))&#123;</span><br><span class="line">                j++;t++;</span><br><span class="line">                N[j] = t;</span><br><span class="line">            &#125;<span class="keyword">else</span>&#123;<span class="comment">//失配</span></span><br><span class="line">                t = N[t];</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> N;</span><br><span class="line">    &#125;</span><br><span class="line"><span class="comment">//第二步利用next表尽量多地往右移动</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">kmp</span><span class="params">(String s, String p)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">int</span>[] next = buildNext(p);<span class="comment">// 调用next（String p）方法</span></span><br><span class="line">        <span class="keyword">int</span> index = -<span class="number">1</span>;<span class="comment">// 成功匹配的位置</span></span><br><span class="line">        <span class="keyword">int</span> sLength = s.length();<span class="comment">// 主串长度</span></span><br><span class="line">        <span class="keyword">int</span> pLength = p.length();<span class="comment">// 子串长度</span></span><br><span class="line">        <span class="keyword">if</span> (sLength &lt; pLength) &#123;</span><br><span class="line">            System.out.println(<span class="string">"Error.The main string is greater than the sub string length."</span>);</span><br><span class="line">            <span class="keyword">return</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">int</span> i = <span class="number">0</span>;</span><br><span class="line">        <span class="keyword">int</span> j = <span class="number">0</span>;</span><br><span class="line">        <span class="keyword">while</span> (i &lt; sLength &amp;&amp; j &lt; pLength) &#123;</span><br><span class="line">            <span class="comment">/*</span></span><br><span class="line"><span class="comment">             * 如果j = -1， 或者当前字符匹配成功（即s.charAt(i) == p.charAt(j)）， 都令i++，j++</span></span><br><span class="line"><span class="comment">             * 这两个条件能否交换次序？</span></span><br><span class="line"><span class="comment">             */</span></span><br><span class="line">            <span class="keyword">if</span> (j == -<span class="number">1</span> || s.charAt(i) == p.charAt(j)) &#123;</span><br><span class="line">                i++;</span><br><span class="line">                j++;</span><br><span class="line">            &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                <span class="comment">/*</span></span><br><span class="line"><span class="comment">                 * 如果j != -1，且当前字符匹配失败， 则令 i 不变，j = next[j], next[j]即为j所对应的next值</span></span><br><span class="line"><span class="comment">                 */</span></span><br><span class="line">                j = next[j];</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">if</span> (j &gt;= pLength) &#123;<span class="comment">// 匹配成功</span></span><br><span class="line">            index = i - j;</span><br><span class="line">            System.out.println(<span class="string">"Successful match,index is:"</span> + index);</span><br><span class="line">        &#125; <span class="keyword">else</span> &#123;<span class="comment">// 匹配失败</span></span><br><span class="line">            System.out.println(<span class="string">"Match failed."</span>);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

]]></content>
  </entry>
  <entry>
    <title>自定义View</title>
    <url>/2020/07/21/%E8%87%AA%E5%AE%9A%E4%B9%89View/</url>
    <content><![CDATA[<h1 id="自定义View"><a href="#自定义View" class="headerlink" title="自定义View"></a>自定义View</h1><h2 id="1-1-什么是自定义View？"><a href="#1-1-什么是自定义View？" class="headerlink" title="1.1 什么是自定义View？"></a>1.1 什么是自定义View？</h2><p>​    自定义View大体上就是三个步骤：</p>
<ul>
<li>布局：onMeasure , onLayout</li>
<li>绘制: onDraw</li>
<li>事件分发：onTouchEvent</li>
</ul>
<h2 id="1-2-自定义View的分类"><a href="#1-2-自定义View的分类" class="headerlink" title="1.2 自定义View的分类"></a>1.2 自定义View的分类</h2><ul>
<li>View类型：实现OnMeasure和OnDraw</li>
<li>ViewGroup类型：实现OnMeasure和OnLayout</li>
</ul>
<h2 id="1-3-基础"><a href="#1-3-基础" class="headerlink" title="1.3 基础"></a>1.3 基础</h2><h3 id="1-3-1-构造函数"><a href="#1-3-1-构造函数" class="headerlink" title="1.3.1 构造函数"></a>1.3.1 构造函数</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 如果View是在Java代码里面new的，则调用第一个构造函数 </span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="title">CarsonView</span><span class="params">(Context context)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">super</span>(context); </span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">    </span><br><span class="line"><span class="comment">// 如果View是在.xml里声明的，则调用第二个构造函数 </span></span><br><span class="line"><span class="comment">// 自定义属性是从AttributeSet参数传进来的 </span></span><br><span class="line">   <span class="function"><span class="keyword">public</span> <span class="title">CarsonView</span><span class="params">(Context context, AttributeSet attrs)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">super</span>(context, attrs); </span><br><span class="line">&#125; </span><br><span class="line"></span><br><span class="line"><span class="comment">// 不会自动调用 </span></span><br><span class="line"><span class="comment">// 一般是在第二个构造函数里主动调用 </span></span><br><span class="line"><span class="comment">// 如View有style属性时 </span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="title">CarsonView</span><span class="params">(Context context, AttributeSet attrs, <span class="keyword">int</span> defStyleAttr)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">super</span>(context, attrs, defStyleAttr); </span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">//API21之后才使用 </span></span><br><span class="line"><span class="comment">// 不会自动调用 </span></span><br><span class="line"><span class="comment">// 一般是在第二个构造函数里主动调用 </span></span><br><span class="line"><span class="comment">// 如View有style属性时 </span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="title">CarsonView</span><span class="params">(Context context, AttributeSet attrs, <span class="keyword">int</span> defStyleAttr, <span class="keyword">int</span> defStyleRes&#123; </span></span></span><br><span class="line"><span class="function"><span class="params">    <span class="keyword">super</span>(context, attrs, defStyleAttr, defStyleRes)</span></span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<h3 id="1-3-2-View的视图结构"><a href="#1-3-2-View的视图结构" class="headerlink" title="1.3.2 View的视图结构"></a>1.3.2 View的视图结构</h3><ol>
<li>PhoneWindow是Android系统中最基本的窗口系统，继承自Windows类，负责管理界面显示以及事件响应。它是Activity与View系统交互的接口。</li>
<li>DecorView是PhoneWindow中的起始节点View，继承于View类，作为整个视图容器来使用。用于设置窗口属<br>性。它本质上是一个FrameLayout。</li>
<li>ViewRoot在Activtiy启动时创建，负责管理、布局、渲染窗口UI等等。</li>
</ol>
<h3 id="1-3-3-坐标系"><a href="#1-3-3-坐标系" class="headerlink" title="1.3.3 坐标系"></a>1.3.3 坐标系</h3><ul>
<li><p>Android坐标系</p>
<p>(1)以左上角为坐标原点</p>
<p>(2)向右为x轴正方向，向下为y轴正方向</p>
<img src="/2020/07/21/%E8%87%AA%E5%AE%9A%E4%B9%89View/%E5%9B%BE%E7%89%872.png" class title="Android系统坐标系">
</li>
<li><p>View位置坐标系</p>
<ul>
<li><p>Top：子View上边界到父view上边界的距离</p>
</li>
<li><p>Left：子View左边界到父view左边界的距离</p>
</li>
<li><p>Bottom：子View下边距到父View上边界的距离</p>
</li>
<li><p>Right：子View右边界到父view左边界的距离</p>
</li>
</ul>
<img src="/2020/07/21/%E8%87%AA%E5%AE%9A%E4%B9%89View/%E5%9B%BE%E7%89%873.png" class title="视图坐标系">
</li>
<li><p>获取位置的方法</p>
<ul>
<li>调用get方法获取相对于父View的距离</li>
<li>在MotionEvent中，get方法获取相对于所在控件坐标系的位置，getRaw方法获取相对于系统坐标系的位置</li>
</ul>
</li>
</ul>
<h3 id="1-3-4-View绘制的生命周期"><a href="#1-3-4-View绘制的生命周期" class="headerlink" title="1.3.4 View绘制的生命周期"></a>1.3.4 View绘制的生命周期</h3><img src="/2020/07/21/%E8%87%AA%E5%AE%9A%E4%B9%89View/%E5%9B%BE%E7%89%874.png" class title="生命周期">

<h3 id="1-3-5-View的层级结构"><a href="#1-3-5-View的层级结构" class="headerlink" title="1.3.5 View的层级结构"></a>1.3.5 View的层级结构</h3><p>​    View绘制是一个树形结构进行递归绘制，以DecorView为根结点。</p>
<img src="/2020/07/21/%E8%87%AA%E5%AE%9A%E4%B9%89View/%E5%9B%BE%E7%89%871.png" class title="层级结构">



<h2 id="1-4-MeasureSpec"><a href="#1-4-MeasureSpec" class="headerlink" title="1.4 MeasureSpec"></a>1.4 MeasureSpec</h2><ul>
<li><p>MeasureSpec是一个int类型的变量，用于存储View的大小和模式信息，最高两位，即31，32位，存储mode信息，其余位数存储size信息。</p>
</li>
<li><p>其中mode类型包括：</p>
<ul>
<li>UNSPECIFIED：不对View大小做限制，系统使用</li>
<li>EXACTLY：确切的大小，如：100dp</li>
<li>AT_MOST：大小不可超过某数值，如：matchParent, 最大不能超过你爸爸</li>
</ul>
</li>
</ul>
<h2 id="1-5-LayoutParams"><a href="#1-5-LayoutParams" class="headerlink" title="1.5 LayoutParams"></a>1.5 LayoutParams</h2><p>​    LayoutParams的作用就是告诉父布局你自己的大小，可以通过LayoutParams获取到MeasureSpec。</p>
<p>​    <strong>getChildMeasureSpec(int spec, int padding, int childDimension)</strong></p>
<p>​    这个函数就是获取子布局的MeasureSpec的方法，第一个参数传入父布局的MeasureSpec，第三个就传入子布局LayoutParams中的width或者Height。</p>
<ul>
<li><p>原理</p>
<img src="/2020/07/21/%E8%87%AA%E5%AE%9A%E4%B9%89View/%E5%9B%BE%E7%89%875.png" class title="规则">



</li>
</ul>
<p>  通俗易懂的一个例子：</p>
  <img src="/2020/07/21/%E8%87%AA%E5%AE%9A%E4%B9%89View/%E5%9B%BE%E7%89%876.png" class title="例子">

<h2 id="1-6-流式布局绘制实战"><a href="#1-6-流式布局绘制实战" class="headerlink" title="1.6 流式布局绘制实战"></a>1.6 流式布局绘制实战</h2><h3 id="1-6-1-onMeasure"><a href="#1-6-1-onMeasure" class="headerlink" title="1.6.1 onMeasure"></a>1.6.1 onMeasure</h3><ul>
<li><p>先度量孩子</p>
<figure class="highlight kotlin"><table><tr><td class="code"><pre><span class="line"><span class="keyword">val</span> childView = getChildAt(i)</span><br><span class="line"><span class="keyword">val</span> layoutParams = childView.layoutParams</span><br><span class="line"><span class="keyword">val</span> childWidthMeasureSpec = getChildMeasureSpec(</span><br><span class="line">                    widthMeasureSpec,</span><br><span class="line">                    paddingLeft + paddingRight,</span><br><span class="line">                    layoutParams.width</span><br><span class="line">                )</span><br><span class="line">                <span class="keyword">val</span> childHeightMeasureSpec = getChildMeasureSpec(</span><br><span class="line">                    heightMeasureSpec,</span><br><span class="line">                    paddingBottom + paddingTop,</span><br><span class="line">                    layoutParams.height</span><br><span class="line">                )</span><br><span class="line">childView.measure(childWidthMeasureSpec, childHeightMeasureSpec)</span><br></pre></td></tr></table></figure>



</li>
</ul>
<ul>
<li><p>再度量自己</p>
<figure class="highlight kotlin"><table><tr><td class="code"><pre><span class="line"><span class="keyword">val</span> realWidth =</span><br><span class="line">            <span class="keyword">if</span> (MeasureSpec.getMode(widthMeasureSpec) == MeasureSpec.EXACTLY) selfWidth <span class="keyword">else</span> maxLineWidth</span><br><span class="line"><span class="keyword">val</span> realHeight =</span><br><span class="line">            <span class="keyword">if</span> (MeasureSpec.getMode(heightMeasureSpec) == MeasureSpec.EXACTLY) selfHeight <span class="keyword">else</span> maxLineHeight</span><br><span class="line">setMeasuredDimension(realWidth, realHeight)</span><br></pre></td></tr></table></figure>

<p>注意：度量自己的时候需要判断自己的mode，如果为EXACTLY，就直接使用自己的大小，不必使用孩子度量后的大小。</p>
</li>
</ul>
<h3 id="1-6-2-onLayout"><a href="#1-6-2-onLayout" class="headerlink" title="1.6.2 onLayout"></a>1.6.2 onLayout</h3><figure class="highlight kotlin"><table><tr><td class="code"><pre><span class="line"><span class="keyword">val</span> left = curL</span><br><span class="line"><span class="keyword">val</span> top = curT</span><br><span class="line"><span class="keyword">val</span> right = left + view.measuredWidth</span><br><span class="line"><span class="keyword">val</span> bottom = top + view.measuredHeight</span><br><span class="line">view.layout(left, top, right, bottom)</span><br></pre></td></tr></table></figure>

<p>调用layout方法进行布局</p>
<h2 id="1-7-ViewPager的wrap-content失效分析"><a href="#1-7-ViewPager的wrap-content失效分析" class="headerlink" title="1.7 ViewPager的wrap_content失效分析"></a>1.7 ViewPager的wrap_content失效分析</h2><h3 id="1-7-1-原因"><a href="#1-7-1-原因" class="headerlink" title="1.7.1 原因"></a>1.7.1 原因</h3><p>先度量了自己，并且设置为满屏。</p>
<h3 id="1-7-2-改正方法"><a href="#1-7-2-改正方法" class="headerlink" title="1.7.2 改正方法"></a>1.7.2 改正方法</h3><p>重写viewPager的onMeasure方法，先度量孩子。</p>
<h2 id="1-8-FlexBoxLayout"><a href="#1-8-FlexBoxLayout" class="headerlink" title="1.8 FlexBoxLayout"></a>1.8 FlexBoxLayout</h2><h3 id="1-8-1-使用"><a href="#1-8-1-使用" class="headerlink" title="1.8.1 使用"></a>1.8.1 使用</h3><p> <a href="https://www.jianshu.com/p/0723ff4123e1" target="_blank" rel="noopener">https://www.jianshu.com/p/0723ff4123e1</a> </p>
]]></content>
  </entry>
  <entry>
    <title>设计模式</title>
    <url>/2020/10/27/%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F/</url>
    <content><![CDATA[<h1 id="一、设计模式六大原则"><a href="#一、设计模式六大原则" class="headerlink" title="一、设计模式六大原则"></a>一、设计模式六大原则</h1><ul>
<li>单一职责原则：一个类就只负责一个工作</li>
<li>里氏替换原则：子类可以拓展功能，但是不要替换父类的功能</li>
<li>依赖倒置原则：面向接口编程</li>
<li>迪米特法则：耦合度降低，不能在方法中新增没有的类</li>
<li>开闭原则：对扩展开放，对修改关闭，即新增功能时最好不要修改原有代码</li>
<li>接口隔离原则：接口功能要最小单元化</li>
</ul>
<h1 id="二、设计模式"><a href="#二、设计模式" class="headerlink" title="二、设计模式"></a>二、设计模式</h1><h2 id="1、简单工厂模式"><a href="#1、简单工厂模式" class="headerlink" title="1、简单工厂模式"></a>1、简单工厂模式</h2><img src="/2020/10/27/%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F/%E7%AE%80%E5%8D%95%E5%B7%A5%E5%8E%82%E6%A8%A1%E5%BC%8F.png" class title="简单工厂模式">

<h2 id="2、工厂方法模式"><a href="#2、工厂方法模式" class="headerlink" title="2、工厂方法模式"></a>2、工厂方法模式</h2><img src="/2020/10/27/%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F/%E5%B7%A5%E5%8E%82%E6%96%B9%E6%B3%95%E6%A8%A1%E5%BC%8F.png" class title="工厂方法模式">

<p>​    当水果数量越来越多时，简单工厂模式会越来越复杂，违背了单一职责原则、开闭原则，所以需要把每个水果单一创建一个工厂。</p>
<h2 id="3、抽象工厂模式"><a href="#3、抽象工厂模式" class="headerlink" title="3、抽象工厂模式"></a>3、抽象工厂模式</h2><p>​    工厂方法模式有一个缺点，就是当新加入了一个工厂后，同一个对象可能会调用出现错误，为了避免这种错误，可以使用抽象工厂模式。</p>
<img src="/2020/10/27/%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F/%E6%8A%BD%E8%B1%A1%E5%B7%A5%E5%8E%82%E6%A8%A1%E5%BC%8F.png" class title="抽象工厂模式">

<h2 id="4、建造者模式"><a href="#4、建造者模式" class="headerlink" title="4、建造者模式"></a>4、建造者模式</h2><p>​    建造者模式实际上也是工厂模式的一种，Builder就是一个工厂，它和工厂模式的不同之处就在于它是有一个模板的，所有事情都会按照这个模板来进行。</p>
<img src="/2020/10/27/%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F/%E5%BB%BA%E9%80%A0%E8%80%85%E6%A8%A1%E5%BC%8F.png" class title="建造者模式">

<h2 id="5、工厂模式总结"><a href="#5、工厂模式总结" class="headerlink" title="5、工厂模式总结"></a>5、工厂模式总结</h2><ul>
<li>原则<ul>
<li>解耦：把对象的创建和使用的过程分开</li>
<li>工厂负责对象的创建，包括其init方法的调用，黑盒创建过程</li>
<li>面向接口编程： 使用者只管使用，只知其接口而不知其实现类</li>
</ul>
</li>
<li>对比<ul>
<li>静态工厂：把所有对象的创建逻辑集中到一个（专业）类里处理</li>
<li>工厂方法模式：一个工厂负责创建一个产品类的创建</li>
<li>将一个系列的产品的工厂合并成一个工厂，负责生产这个系列的产品</li>
<li>对象的创建比较复杂时，按步骤一块块创建，让创建过程模板化</li>
</ul>
</li>
</ul>
<h2 id="6、单例模式"><a href="#6、单例模式" class="headerlink" title="6、单例模式"></a>6、单例模式</h2><p>​    分为懒汉式和饿汉式，懒汉式就是在要用的时候再初始化，需要加锁。</p>
<p>​    还有静态内部类实现单例，这种方法是因为类加载时不会立即加载内部类，在用到时才会加载，并且也保证了线程安全。</p>
<h2 id="7、适配器模式"><a href="#7、适配器模式" class="headerlink" title="7、适配器模式"></a>7、适配器模式</h2><img src="/2020/10/27/%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F/%E9%80%82%E9%85%8D%E5%99%A8%E6%A8%A1%E5%BC%8F.png" class title="适配器模式">

<h2 id="8、桥接模式"><a href="#8、桥接模式" class="headerlink" title="8、桥接模式"></a>8、桥接模式</h2><img src="/2020/10/27/%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F/%E6%A1%A5%E6%8E%A5%E6%A8%A1%E5%BC%8F.png" class title="桥接模式">

<p>适配器模式是为了合并，桥接模式是为了分离。</p>
<h2 id="9、装饰器模式"><a href="#9、装饰器模式" class="headerlink" title="9、装饰器模式"></a>9、装饰器模式</h2><p>动态地给一个对象增加一些额外的职责，为的是功能增强。</p>
<img src="/2020/10/27/%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F/%E8%A3%85%E9%A5%B0%E5%99%A8%E6%A8%A1%E5%BC%8F.png" class title="装饰器模式">

<h2 id="10、代理模式"><a href="#10、代理模式" class="headerlink" title="10、代理模式"></a>10、代理模式</h2><p>一个类代表另一个类的功能。</p>
<img src="/2020/10/27/%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F/%E4%BB%A3%E7%90%86%E6%A8%A1%E5%BC%8F.png" class title="代理模式">

<h2 id="11、组合模式"><a href="#11、组合模式" class="headerlink" title="11、组合模式"></a>11、组合模式</h2><p>把一组相似的对象当作一个单一的对象，为的是减少数据类型。</p>


<h2 id="12、外观模式"><a href="#12、外观模式" class="headerlink" title="12、外观模式"></a>12、外观模式</h2><p>糅合功能，对外只提供一个入口。</p>
<img src="/2020/10/27/%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F/%E5%A4%96%E8%A7%82%E6%A8%A1%E5%BC%8F.png" class title="外观模式">

<h2 id="13、模板方法模式"><a href="#13、模板方法模式" class="headerlink" title="13、模板方法模式"></a>13、模板方法模式</h2><p>在父类中编排主流程，将步骤实现延迟到子类去实现。</p>
<img src="/2020/10/27/%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F/%E6%A8%A1%E6%9D%BF%E6%96%B9%E6%B3%95%E6%A8%A1%E5%BC%8F.png" class title="模板方法模式">

<h2 id="14、策略模式"><a href="#14、策略模式" class="headerlink" title="14、策略模式"></a>14、策略模式</h2><p>通过选择策略类，来执行不同算法分支。核心是通过注入对象，改变行为。</p>
<img src="/2020/10/27/%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F/%E7%AD%96%E7%95%A5%E6%A8%A1%E5%BC%8F.png" class title="策略模式">

<h2 id="15、责任链模式"><a href="#15、责任链模式" class="headerlink" title="15、责任链模式"></a>15、责任链模式</h2><p>将请求传给一个接收者链，由链将请求流转给目标对象。</p>
<img src="/2020/10/27/%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F/%E8%B4%A3%E4%BB%BB%E9%93%BE%E6%A8%A1%E5%BC%8F.png" class title="责任链模式">

<h2 id="16、观察者模式"><a href="#16、观察者模式" class="headerlink" title="16、观察者模式"></a>16、观察者模式</h2><p>一个对象状态改变时通知其他对象。</p>
<img src="/2020/10/27/%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F/%E8%A7%82%E5%AF%9F%E8%80%85%E6%A8%A1%E5%BC%8F.png" class title="观察者模式">

<h2 id="17、命令模式"><a href="#17、命令模式" class="headerlink" title="17、命令模式"></a>17、命令模式</h2><p>通过增加Command抽象命令类，将不符合抽象编程的handler 调用，转为抽象编程。</p>
<img src="/2020/10/27/%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F/%E5%91%BD%E4%BB%A4%E6%A8%A1%E5%BC%8F.png" class title="命令模式">

<h2 id="18、访问者模式"><a href="#18、访问者模式" class="headerlink" title="18、访问者模式"></a>18、访问者模式</h2><p>利用双重分派机制，弥补java多态中的方法重载是静态化的不足。</p>
<img src="/2020/10/27/%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F/%E8%AE%BF%E9%97%AE%E8%80%85%E6%A8%A1%E5%BC%8F.png" class title="访问者模式">]]></content>
  </entry>
  <entry>
    <title>网络框架</title>
    <url>/2020/11/20/%E7%BD%91%E7%BB%9C%E6%A1%86%E6%9E%B6/</url>
    <content><![CDATA[<h1 id="一、网络基础"><a href="#一、网络基础" class="headerlink" title="一、网络基础"></a>一、网络基础</h1><h2 id="1-计算机网络分层"><a href="#1-计算机网络分层" class="headerlink" title="1. 计算机网络分层"></a>1. 计算机网络分层</h2><img src="/2020/11/20/%E7%BD%91%E7%BB%9C%E6%A1%86%E6%9E%B6/tcpip.jpg" class title="tcpip">

<p><strong>OSI七层网络模型(从下往上)</strong>：</p>
<blockquote>
<ul>
<li><strong>物理层(Physical)</strong>：设备之间的数据通信提供传输媒体及互连设备，为数据传输提供可靠的 环境。可以理解为网络传输的物理媒体部分，比如<strong>网卡，网线，集线器，中继器，调制解调器</strong>等！ 在这一层，数据还没有被组织，仅作为原始的位流或电气电压处理，这一层的单位是:<strong>bit比特</strong></li>
<li><strong>数据链路层(Datalink)</strong>：可以理解为数据通道，主要功能是如何在不可靠的物理线路上进行 数据的可靠传递，改层作用包括：物理地址寻址，数据的成帧，流量控制，数据检错以及重发等！ 另外这个<strong>数据链路指的是</strong>：物理层要为终端设备间的数据通信提供传输媒体及其连接。媒体是 长期的，连接是有生存期的。在连接生存期内，收发两端可以进行不等的一次或多次数据通信。 每次通信都要经过建立通信联络和拆除通信联络两过程！这种建立起来的<strong>数据收发关系</strong>~ 该层的设备有：<strong>网卡，网桥，网路交换机</strong>，另外该层的单位为：<strong>帧</strong></li>
<li><strong>网络层(Network)</strong>：主要功能是将网络地址翻译成对应的物理地址，并决定如何将数据从发 送方路由到接收方，所谓的路由与寻径：一台终端可能需要与多台终端通信，这样就产生的了 把任意两台终端设备数据链接起来的问题！简单点说就是：建立网络连接和为上层提供服务！ 该层的设备有：<strong>路由</strong>！该层的单位为：<strong>数据包</strong>，另外IP协议就在这一层！</li>
<li><strong>传输层(Transport)</strong>：向上面的应用层提供通信服务，面向通信部分的最高层，同时也是 用户功能中的最低层。接收会话层数据，在必要时将数据进行分割，并将这些数据交给网络 层，并且保证这些数据段有效的到达对端！所以这层的单位是：<strong>数据段</strong>；而这层有两个很重要 的协议就是：<strong>TCP传输控制协议</strong>与<strong>UDP用户数据报协议</strong>，这也是本章节核心讲解的部分！</li>
<li><strong>会话层(Session)</strong>：负责在网络中的两节点之间建立、维持和终止通信。建立通信链接， 保持会话过程通信链接的畅通，同步两个节点之间的对话，决定通信是否被中断以及通信中断时 决定从何处重新发送，即不同机器上的用户之间会话的建立及管理！</li>
<li><strong>表示层(Presentation)</strong>：对来自应用层的命令和数据进行解释，对各种语法赋予相应 的含义，并按照一定的格式传送给会话层。其主要功能是”处理用户信息的表示问题，如编码、 数据格式转换和加密解密，压缩解压缩”等</li>
<li><strong>应用层(Application)</strong>：OSI参考模型的最高层，为用户的应用程序提供网络服务。 它在其他6层工作的基础上，负责完成网络中应用程序与网络操作系统之间的联系，建立与结束使用者之间的联系，并完成网络用户提出的各种网络服务及应用所需的监督、管理和服务等各种协议。此外，该层还负责协调各个应用程序间的工作。应用层为用户提供的服务和协议有：文件服务、目录服务、文件传输服务（FTP）、远程登录服务（Telnet）、电子邮件服务（E-mail）、打印服务、安全服务、网络管理服务、数据库服务等。</li>
</ul>
</blockquote>
<blockquote>
<p>TCP/IP是一组协议的代名词，它还包括许多协议，组成了TCP/IP协议簇。 TCP/IP协议簇分为四层，IP位于协议簇的第二层(对应OSI的第三层)，TCP位于协议簇的第三层 (对应OSI的第四层)。TCP/IP通讯协议采用了4层的层级结构，每一层都呼叫它的下一层所提供 的网络来完成自己的需求。这4层分别为：</p>
<ul>
<li><strong>应用层</strong>：应用程序间沟通的层，如简单电子邮件传输（SMTP）、文件传输协议（FTP）、 网络远程访问协议（Telnet）等。</li>
<li><strong>传输层</strong>：在此层中，它提供了节点间的数据传送服务，如传输控制协议（TCP）、 用户数据报协议（UDP）等，TCP和UDP给数据包加入传输数据并把它传输到下一层中， 这一层负责传送数据，并且确定数据已被送达并接收。</li>
<li><strong>网络互连层</strong>：负责提供基本的数据封包传送功能，让每一块数据包都能够到达目 的主机（但不检查是否被正确接收），如网际协议（IP）。</li>
<li><strong>主机到网络层</strong>：对实际的网络媒体的管理，定义如何使用实际网络 （如Ethernet、Serial Line等）来传送数据。</li>
</ul>
</blockquote>
<h2 id="2-IP地址-amp-端口号"><a href="#2-IP地址-amp-端口号" class="headerlink" title="2. IP地址&amp;端口号"></a>2. IP地址&amp;端口号</h2><h3 id="2-1-IP地址"><a href="#2-1-IP地址" class="headerlink" title="2.1 IP地址"></a>2.1 IP地址</h3><img src="/2020/11/20/%E7%BD%91%E7%BB%9C%E6%A1%86%E6%9E%B6/ip.jpg" class title="ip">

<h3 id="2-2-端口号"><a href="#2-2-端口号" class="headerlink" title="2.2 端口号"></a>2.2 端口号</h3><img src="/2020/11/20/%E7%BD%91%E7%BB%9C%E6%A1%86%E6%9E%B6/ip&port.jpg" class title="ip&amp;port">

<p>端口号规定为16位，即允许一个IP主机有2的16次方65535个不同的端口。其中：</p>
<ul>
<li><p>0~1023：分配给系统的端口号</p>
<blockquote>
<p>我们不可以乱用<br>常用协议使用的端口：HTTP:80，FTP：21，TELNET：23</p>
</blockquote>
</li>
<li><p>1024~49151：登记端口号，主要是让第三方应用使用</p>
<blockquote>
<p>但是必须在IANA（互联网数字分配机构）按照规定手续登记，</p>
</blockquote>
</li>
<li><p>49152~65535：短暂端口号，是留给客户进程选择暂时使用，一个进程使用完就可以供其他进程使用。</p>
</li>
</ul>
<blockquote>
<p>在Socket使用时，可以用1024~65535的端口号</p>
</blockquote>
<h3 id="2-3-C-S结构"><a href="#2-3-C-S结构" class="headerlink" title="2.3 C/S结构"></a>2.3 C/S结构</h3><ul>
<li><p>定义：即客户端/服务器结构，是软件系统体系结构</p>
</li>
<li><p>作用：充分利用两端硬件环境的优势，将任务合理分配到Client端和Server端来实现，降低了系统的通讯开销。</p>
<blockquote>
<p>Socket正是使用这种结构建立连接的，一个套接字接客户端，一个套接字接服务器。</p>
</blockquote>
</li>
</ul>
<p>如图：</p>
<img src="/2020/11/20/%E7%BD%91%E7%BB%9C%E6%A1%86%E6%9E%B6/c&s.png" class title="c&amp;s">

<p>可以看出，Socket的使用可以基于TCP或者UDP协议。</p>
<h2 id="3-TCP-amp-UDP"><a href="#3-TCP-amp-UDP" class="headerlink" title="3. TCP&amp;UDP"></a>3. TCP&amp;UDP</h2><h3 id="3-1-TCP"><a href="#3-1-TCP" class="headerlink" title="3.1 TCP"></a>3.1 TCP</h3><ul>
<li><p>定义：Transmission Control Protocol，即传输控制协议，是一种传输层通信协议</p>
<blockquote>
<p>基于TCP的应用层协议有FTP、Telnet、SMTP、HTTP、POP3与DNS。</p>
</blockquote>
</li>
<li><p>特点：面向连接、面向字节流、全双工通信、可靠</p>
<ul>
<li><strong>面向连接</strong>：指的是要使用TCP传输数据，必须先建立TCP连接，传输完成后释放连接，就像打电话一样必须先拨号建立一条连接，打完后挂机释放连接。</li>
<li><strong>全双工通信</strong>：即一旦建立了TCP连接，通信双方可以在任何时候都能发送数据。</li>
<li><strong>可靠的</strong>：指的是通过TCP连接传送的数据，无差错，不丢失，不重复，并且按序到达。</li>
<li><strong>面向字节流</strong>：流，指的是流入到进程或从进程流出的字符序列。简单来说，虽然有时候要传输的数据流太大，TCP报文长度有限制，不能一次传输完，要把它分为好几个数据块，但是由于可靠性保证，接收方可以按顺序接收数据块然后重新组成分块之前的数据流，所以TCP看起来就像直接互相传输字节流一样，面向字节流。</li>
</ul>
</li>
<li><p>TCP建立连接<br>必须进行<strong>三次握手</strong>：若A要与B进行连接，则必须 </p>
<ul>
<li>第一次握手：建立连接。客户端发送连接请求报文段，将SYN位置为1，Sequence Number为x；然后，客户端进入SYN_SEND状态，等待服务器的确认。即A发送信息给B</li>
<li>第二次握手：服务器收到客户端的SYN报文段，需要对这个SYN报文段进行确认。即B收到连接信息后向A返回确认信息</li>
<li>第三次握手：客户端收到服务器的（SYN+ACK）报文段，并向服务器发送ACK报文段。即A收到确认信息后再次向B返回确认连接信息 </li>
</ul>
<blockquote>
<p>此时，A告诉自己上层连接建立；B收到连接信息后告诉上层连接建立。</p>
</blockquote>
</li>
</ul>
<img src="/2020/11/20/%E7%BD%91%E7%BB%9C%E6%A1%86%E6%9E%B6/3ciwoshou.png" class title="3ciwoshou">

<p>这样就完成TCP三次握手 = 一条TCP连接建立完成 = 可以开始发送数据</p>
<blockquote>
<ol>
<li>三次握手期间任何一次未收到对面回复都要重发。</li>
<li>最后一个确认报文段发送完毕以后，客户端和服务器端都进入ESTABLISHED状态。</li>
</ol>
</blockquote>
<h4 id="为什么TCP建立连接需要三次握手？"><a href="#为什么TCP建立连接需要三次握手？" class="headerlink" title="为什么TCP建立连接需要三次握手？"></a>为什么TCP建立连接需要三次握手？</h4><p>答：防止服务器端因为接收了<strong>早已失效的连接请求报文</strong>从而一直等待客户端请求，从而浪费资源</p>
<blockquote>
<ul>
<li>“已失效的连接请求报文段”的产生在这样一种情况下：Client发出的第一个连接请求报文段并没有丢失，而是在某个网络结点长时间的滞留了，以致延误到连接释放以后的某个时间才到达server。</li>
<li>这是一个早已失效的报文段。但Server收到此失效的连接请求报文段后，就误认为是Client再次发出的一个新的连接请求。</li>
<li>于是就向Client发出确认报文段，同意建立连接。</li>
<li>假设不采用“三次握手”：只要Server发出确认，新的连接就建立了。</li>
<li>由于现在Client并没有发出建立连接的请求，因此不会向Server发送数据。</li>
<li>但Server却以为新的运输连接已经建立，并一直等待Client发来数据。&gt;- 这样，Server的资源就白白浪费掉了。</li>
</ul>
</blockquote>
<p><strong>以上答案只是表象，没有说到本质上去</strong></p>
<blockquote>
<p>那么本质是 因为tcp是全双工，为保证传输的可靠性，需要给每次传输的数据段添加序号，那么初始的序列号就是tcp三次握手真正的意义所在，而为了确保交换双方的初始序号，最少需要三次才行</p>
</blockquote>
<p>采用“三次握手”的办法可以防止上述现象发生：</p>
<ul>
<li><p>Client不会向Server的确认发出确认</p>
</li>
<li><p>Server由于收不到确认，就知道Client并没有要求建立连接</p>
</li>
<li><p>所以Server不会等待Client发送数据，资源就没有被浪费</p>
</li>
<li><p>TCP释放连接<br>TCP释放连接需要<strong>四次挥手</strong>过程，现在假设A主动释放连接：（数据传输结束后，通信的双方都可释放连接）</p>
<ul>
<li><p>第一次挥手：A发送释放信息到B；（发出去之后，A-&gt;B发送数据这条路径就断了）</p>
</li>
<li><p>第二次挥手：B收到A的释放信息之后，回复确认释放的信息：我同意你的释放连接请求</p>
</li>
<li><p>第三次挥手：B发送“请求释放连接“信息给A</p>
</li>
<li><p>第四次挥手：A收到B发送的信息后向B发送确认释放信息：我同意你的释放连接请求</p>
<blockquote>
<p>B收到确认信息后就会正式关闭连接； A等待2MSL后依然没有收到回复，则证明B端已正常关闭，于是A关闭连接</p>
</blockquote>
</li>
</ul>
</li>
</ul>


<h4 id="为什么TCP释放连接需要四次挥手？"><a href="#为什么TCP释放连接需要四次挥手？" class="headerlink" title="为什么TCP释放连接需要四次挥手？"></a>为什么TCP释放连接需要四次挥手？</h4><p>为了保证双方都能通知对方“需要释放连接”，即在释放连接后都无法接收或发送消息给对方</p>
<ul>
<li><p>需要明确的是：TCP是全双工模式，这意味着是双向都可以发送、接收的</p>
</li>
<li><p>释放连接的定义是：双方都无法接收或发送消息给对方，是双向的</p>
</li>
<li><p>当主机1发出“释放连接请求”（FIN报文段）时，只是表示主机1已经没有数据要发送 / 数据已经全部发送完毕；</p>
<blockquote>
<p>但是，这个时候主机1还是可以接受来自主机2的数据。</p>
</blockquote>
</li>
<li><p>当主机2返回“确认释放连接”信息（ACK报文段）时，表示它已经知道主机1没有数据发送了<br>但此时主机2还是可以发送数据给主机1</p>
</li>
<li><p>当主机2也发送了FIN报文段时，即告诉主机1我也没有数据要发送了<br>此时，主机1和2已经无法进行通信：主机1无法发送数据给主机2，主机2也无法发送数据给主机1，此时，TCP的连接才算释放</p>
</li>
</ul>
<h4 id="三次握手-amp-四次挥手面试题总结"><a href="#三次握手-amp-四次挥手面试题总结" class="headerlink" title="三次握手&amp;四次挥手面试题总结"></a>三次握手&amp;四次挥手面试题总结</h4><ol>
<li><p>三次握手是什么或者流程？四次握手呢？答案前面分析就是。</p>
</li>
<li><p>为什么建立连接是三次握手，而关闭连接却是四次挥手呢？</p>
<blockquote>
<p>这是因为服务端在LISTEN状态下，收到建立连接请求的SYN报文后，把ACK和SYN放在一个报文里发送给客户端。而关闭连接时，当收到对方的FIN报文时，仅仅表示对方不再发送数据了但是还能接收数据，己方也未必全部数据都发送给对方了，所以己方可以立即close，也可以发送一些数据给对方后，再发送FIN报文给对方来表示同意现在关闭连接，因此，己方ACK和FIN一般都会分开发送。</p>
</blockquote>
</li>
</ol>
<h3 id="3-2-UPD"><a href="#3-2-UPD" class="headerlink" title="3.2 UPD"></a>3.2 UPD</h3><ul>
<li><p>定义：User Datagram Protocol，即用户数据报协议，是一种传输层通信协议。</p>
<blockquote>
<p>基于UDP的应用层协议有TFTP、SNMP与DNS。</p>
</blockquote>
</li>
<li><p>特点：无连接的、不可靠的、面向报文、没有拥塞控制</p>
<ul>
<li><strong>无连接的</strong>：和TCP要建立连接不同，UDP传输数据不需要建立连接，就像写信，在信封写上收信人名称、地址就可以交给邮局发送了，至于能不能送到，就要看邮局的送信能力和送信过程的困难程度了。</li>
<li><strong>不可靠的</strong>：因为UDP发出去的数据包发出去就不管了，不管它会不会到达，所以很可能会出现丢包现象，使传输的数据出错。</li>
<li><strong>面向报文</strong>：数据报文，就相当于一个数据包，应用层交给UDP多大的数据包，UDP就照样发送，不会像TCP那样拆分。</li>
<li><strong>没有拥塞控制</strong>：拥塞，是指到达通信子网中某一部分的分组数量过多，使得该部分网络来不及处理，以致引起这部分乃至整个网络性能下降的现象，严重时甚至会导致网络通信业务陷入停顿，即出现死锁现象，就像交通堵塞一样。TCP建立连接后如果发送的数据因为信道质量的原因不能到达目的地，它会不断重发，有可能导致越来越塞，所以需要一个复杂的原理来控制拥塞。而UDP就没有这个烦恼，发出去就不管了。</li>
</ul>
</li>
<li><p>应用场景 很多的实时应用（如IP电话、实时视频会议、某些多人同时在线游戏等）要求源主机以很定的速率发送数据，并且允许在网络发生拥塞时候丢失一些数据，但是要求不能有太大的延时，UDP就刚好适合这种要求。</p>
</li>
</ul>
<h2 id="4-Java中对于网络提供的几个关键类"><a href="#4-Java中对于网络提供的几个关键类" class="headerlink" title="4. Java中对于网络提供的几个关键类"></a>4. Java中对于网络提供的几个关键类</h2><p>针对不同的网络通信层次，Java给我们提供的网络功能有四大类：</p>
<ul>
<li>InetAddress： 用于标识网络上的硬件资源</li>
<li>URL： 统一资源定位符，通过URL可以直接读取或者写入网络上的数据</li>
<li>Socket和ServerSocket： 使用TCP协议实现网络通信的Socket相关的类</li>
<li>Datagram： 使用UDP协议，将数据保存在数据报中，通过网络进行通信</li>
</ul>
<h2 id="5-Socket"><a href="#5-Socket" class="headerlink" title="5. Socket"></a>5. Socket</h2><h3 id="什么是Socket？"><a href="#什么是Socket？" class="headerlink" title="什么是Socket？"></a>什么是Socket？</h3><ul>
<li><p>即套接字，<strong>是一个对 TCP / IP协议进行封装 的编程调用接口（API）</strong><br>用來描述IP地址和端口，是通信链的句柄，应用程序可以通过Socket向网络发送请求或者<br>应答网络请求！Socket是支持TCP/IP协议的网络通信的基本操作单元，是对网络通信过程<br>中端点的抽象表示，包含了进行网络通信所必须的五种信息</p>
<ol>
<li>连接所使用的的协议</li>
<li>本地主机的IP地址</li>
<li>本地远程的协议端口</li>
<li>远程主机的IP地址</li>
<li>远地进程的协议端口</li>
</ol>
<blockquote>
<ol>
<li>即通过<code>Socket</code>，我们才能在Andorid平台上通过 <code>TCP/IP</code>协议进行开发</li>
<li><code>Socket</code>不是一种协议，而是一个编程调用接口（<code>API</code>），属于传输层（主要解决数据如何在网络中传输）</li>
<li>成对出现，一对套接字</li>
</ol>
</blockquote>
</li>
</ul>
<h3 id="Socket通信模型"><a href="#Socket通信模型" class="headerlink" title="Socket通信模型"></a>Socket通信模型</h3><img src="/2020/11/20/%E7%BD%91%E7%BB%9C%E6%A1%86%E6%9E%B6/socket_cs.jpg" class title="socket_cs">

<h3 id="Socket通信步骤"><a href="#Socket通信步骤" class="headerlink" title="Socket通信步骤"></a>Socket通信步骤</h3><ul>
<li>Step 1：创建ServerSocket和Socket</li>
<li>Step 2：打开连接到的Socket的输入/输出流</li>
<li>Step 3：按照协议对Socket进行读/写操作</li>
<li>Step 4：关闭输入输出流，以及Socket</li>
</ul>
<h4 id="Socket服务端的编写"><a href="#Socket服务端的编写" class="headerlink" title="Socket服务端的编写"></a>Socket服务端的编写</h4><ul>
<li>Step 1：创建ServerSocket对象，绑定监听的端口</li>
<li>Step 2：调用accept()方法监听客户端的请求</li>
<li>Step 3：连接建立后，通过输入流读取客户端发送的请求信息</li>
<li>Step 4：通过输出流向客户端发送响应信息</li>
<li>Step 5：关闭相关资源</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> <span class="keyword">throws</span> IOException </span>&#123;</span><br><span class="line">       <span class="comment">//1.创建一个服务器端Socket，即ServerSocket，指定绑定的端口，并监听此端口</span></span><br><span class="line">       ServerSocket serverSocket = <span class="keyword">new</span> ServerSocket(<span class="number">12345</span>);</span><br><span class="line">       InetAddress address = InetAddress.getLocalHost();</span><br><span class="line">       String ip = address.getHostAddress();</span><br><span class="line">       Socket socket = <span class="keyword">null</span>;</span><br><span class="line">       <span class="comment">//2.调用accept()等待客户端连接</span></span><br><span class="line">       System.out.println(<span class="string">"~~~服务端已就绪，等待客户端接入~，服务端ip地址: "</span> + ip);</span><br><span class="line">       socket = serverSocket.accept();</span><br><span class="line">       <span class="comment">//3.连接后获取输入流，读取客户端信息</span></span><br><span class="line">       InputStream is=<span class="keyword">null</span>;</span><br><span class="line">       InputStreamReader isr=<span class="keyword">null</span>;</span><br><span class="line">       BufferedReader br=<span class="keyword">null</span>;</span><br><span class="line">       OutputStream os=<span class="keyword">null</span>;</span><br><span class="line">       PrintWriter pw=<span class="keyword">null</span>;</span><br><span class="line">       is = socket.getInputStream();     <span class="comment">//获取输入流</span></span><br><span class="line">       isr = <span class="keyword">new</span> InputStreamReader(is,<span class="string">"UTF-8"</span>);</span><br><span class="line">       br = <span class="keyword">new</span> BufferedReader(isr);</span><br><span class="line">       String info = <span class="keyword">null</span>;</span><br><span class="line">       <span class="keyword">while</span>((info=br.readLine())!=<span class="keyword">null</span>)&#123;<span class="comment">//循环读取客户端的信息</span></span><br><span class="line">           System.out.println(<span class="string">"客户端发送过来的信息"</span> + info);</span><br><span class="line">       &#125;</span><br><span class="line">       socket.shutdownInput();<span class="comment">//关闭输入流</span></span><br><span class="line">       socket.close();</span><br><span class="line">   &#125;</span><br></pre></td></tr></table></figure>

<h4 id="Socket客户端的编写"><a href="#Socket客户端的编写" class="headerlink" title="Socket客户端的编写"></a>Socket客户端的编写</h4><ul>
<li>Step 1：创建Socket对象，指明需要链接的服务器的地址和端号</li>
<li>Step 2：链接建立后，通过输出流向服务器发送请求信息</li>
<li>Step 3：通过输出流获取服务器响应的信息</li>
<li>Step 4：关闭相关资源</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String ... args)</span> <span class="keyword">throws</span> Exception</span>&#123;</span><br><span class="line">        <span class="comment">//1.创建客户端Socket，指定服务器地址和端口</span></span><br><span class="line">        Socket socket = <span class="keyword">new</span> Socket(<span class="string">"127.0.0.1"</span>, <span class="number">12345</span>);</span><br><span class="line">        <span class="comment">//2.获取输出流，向服务器端发送信息</span></span><br><span class="line">        OutputStream os = socket.getOutputStream();<span class="comment">//字节输出流</span></span><br><span class="line">        PrintWriter pw = <span class="keyword">new</span> PrintWriter(os);<span class="comment">//将输出流包装为打印流</span></span><br><span class="line">        <span class="comment">//获取客户端的IP地址</span></span><br><span class="line">        InetAddress address = InetAddress.getLocalHost();</span><br><span class="line">        String ip = address.getHostAddress();</span><br><span class="line">        pw.write(<span class="string">"客户端：~"</span> + ip + <span class="string">"~ 接入服务器！！"</span>);</span><br><span class="line">        pw.flush();</span><br><span class="line">        socket.shutdownOutput();<span class="comment">//关闭输出流</span></span><br><span class="line">        socket.close();</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>







]]></content>
  </entry>
</search>
